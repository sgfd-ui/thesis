# 机制1：基于历史验证的增量初始负载规划代码流程说明

## 1. 模块职责

代码层将本机制实现为执行前控制模块，负责读取历史控制状态、判定可复用范围、扫描残余分片、合成稳定统计贡献，并把历史计划种子和残余识别范围传递给第 6 章模块。

该模块不执行 join，不缓存 join 输出，也不直接复用上一轮物理任务计划。它只产生控制面结果：

```text
HistoryReuseResult {
  reuse_mode: FULL_REUSE | PARTIAL_REUSE | REBUILD
  K_reuse
  K_check
  Omega_residual
  plan_seed
  validation_summary
  resource_context
}
```

## 2. 输入状态

当前轮输入由版本化分片目录描述：

```text
ShardMeta {
  relation_id
  partition_id
  version
  snapshot_id
  row_count
  query_signature
  summary_pointer
}
```

历史状态按分片保存：

```text
ShardStats {
  meta
  candidate_summary
  exact_count_map
  sketch
  valid_until
}
```

查询级历史状态保存：

```text
HistoryControlState {
  structural_signature
  shard_version_vector
  H_R
  H_S
  state_combination_map
  work_estimate
  strategy_template
  parameter_rules
  execution_feedback
  confidence
}
```

代码层的历史版本发布对象可以显式保存构建与提交字段：

```text
HistoryVersion {
  history_id
  query_signature
  source_snapshot
  shard_version_vector
  state_version
  build_status: BUILDING | COMMITTED | INVALID
  committed_at
  payload_pointer
}
```

查询入口只能通过 committed history pointer 读取 `build_status=COMMITTED` 的完整版本。`BUILDING` 版本、发布失败版本和半写入 payload 不得进入复用判定。

## 3. 执行流程

整体入口流程如下：

```text
build_structural_signature(query)
  -> lookup HistoryControlState
  -> compare shard version vectors
  -> split stable shards and residual shards
  -> validate reusable historical keys
  -> scan residual shards if needed
  -> query stable contribution for new/boundary candidates
  -> decide FULL_REUSE / PARTIAL_REUSE / REBUILD
  -> emit HistoryReuseResult
```

结构签名不匹配、历史状态缺失或分片版本无法验证时，直接返回 `REBUILD`。分片版本稳定时，代码复用分片摘要和精确候选计数；残余分片必须重新扫描连接键列，不能只检查旧候选键。

## 4. 稳定贡献与残余贡献

对每个关系 $X\in\{R,S\}$，代码先构造：

```text
stable_shards_X
residual_shards_X
```

残余分片扫描输出：

```text
ResidualStats {
  residual_candidates
  residual_exact_counts
  residual_summary
}
```

对候选键 `k`，稳定贡献按优先级获取：

```text
if k in ExactMap of stable shards:
    use exact stable count
else if stable shard has key index:
    batch lookup count(k)
else:
    use Sketch estimate with error bound
```

最终输出频率可以是精确值，也可以是区间：

```text
plan_freq_lower = stable_lower + residual_exact
plan_freq_upper = stable_upper + residual_exact
```

若区间跨越热点阈值或 top-k 边界，键进入 `K_check`，交给第 6 章做边界确认。

## 5. 复用模式判定

`FULL_REUSE` 条件：

```text
structural_signature_match
all required shards stable
historical summaries valid
no unresolved boundary keys
new_hotspot_risk low
```

输出：

```text
Omega_residual = empty
K_check = empty
plan_seed = historical strategy templates
```

`PARTIAL_REUSE` 条件：

```text
signature match
some shards stable
some shards residual or uncertain
stable contribution query available
partial cost < rebuild cost
```

输出：

```text
K_reuse = verified stable historical keys
K_check = boundary or uncertain keys
Omega_residual = residual shard ranges
```

`REBUILD` 条件：

```text
signature mismatch
no matching history
version vector unavailable
summary incompatible
boundary lookup too expensive
residual ratio too high
```

输出：

```text
K_reuse = empty
Omega_residual = full current input
```

## 6. 成本触发回退

进入 `PARTIAL_REUSE` 前估计：

```text
C_partial =
  metadata_check
  + scan(residual_shards)
  + merge_summaries
  + query_stable_contribution(candidates)
  + boundary_validation

C_rebuild =
  scan(current_input)
  + full_candidate_detection
```

只有当 `C_partial < C_rebuild` 时才执行部分复用。若边界键过多、稳定分片回查接近全量读取、残余范围接近完整输入，则直接返回 `REBUILD`。

## 7. 输出给第 6 章

代码模块不生成完整初始计划，只输出：

```text
HistoryReuseResult {
  reuse_mode
  K_reuse
  K_check
  Omega_residual
  plan_seed: strategy_template + valid_scope
  validation_summary
  resource_context
}
```

第 6 章模块负责读取该结果，完成残余范围的双侧负载识别、边界回查和完整初始负载计划合成。

## 8. 后台历史状态更新流程

当前查询的 join 输出、运行时再平衡和结果校验完成后，执行线程只提交后台更新任务，不在当前查询关键路径内同步写历史状态：

```text
submit HistoryUpdateJob {
  query_signature
  committed_shard_versions
  verified_counts
  detected_H_R_H_S
  state_combination_map
  strategy_template
  parameter_rules
  execution_feedback
  rebalance_feedback
}
```

后台 worker 负责构建下一轮可读取的历史状态：

```text
run HistoryUpdateJob
  -> read verified counts and execution feedback
  -> inherit unchanged stable ShardStats
  -> merge materialized residual shard statistics
  -> merge execution and rebalance feedback
  -> build new HistoryControlState
  -> write temp history version
  -> fsync / memory-fence if required by implementation
  -> atomically publish committed history version
```

后台更新不为了刷新历史状态重新扫描完整原始输入。新状态由三类来源合并：

```text
next_state = stable_committed_state
           + materialized_residual_stats
           + execution_feedback
```

其中 `stable_committed_state` 来自上一版已提交历史状态，`materialized_residual_stats` 来自当前轮残余扫描和边界确认过程中已经收集的统计，`execution_feedback` 来自本轮连接执行、输出规模、长尾和再平衡反馈。若某些残余摘要在当前轮没有物化完成，后台任务只能基于已经收集的统计补充构建；不能重新读取完整关系来制造“免费”的历史状态。

查询入口只允许读取 committed history version。若后台任务失败、超时、被取消或尚未发布完成，下一轮查询不得读取临时版本；代码应继续使用上一版 committed state，或将历史状态视为缺失/低置信度并返回 `PARTIAL_REUSE` 或 `REBUILD`。后台更新耗时可单独记录为 `history_update_background_time`，但不计入当前查询的 `modeled_end_to_end_time`。
