# 机制3：快照驱动的剩余工作再平衡代码流程说明

## 1. 模块职责

代码层将机制3实现为运行时再平衡模块。输入是初始任务计划生成的可划分任务边界，以及执行过程中维护的本地进度状态。模块不关心第 5/6 章如何得到初始计划，只要求任务边界可定位、可划分、可迁移且输出互斥。

输出是运行时被空闲节点接管的尾部微批次。

## 2. 核心状态

每个节点维护公开队列状态：

```text
QueueState_i = [Head_i | Tail_i]
```

其中：

```text
Head_i: 本地已经领取或声明的边界
Tail_i: 仍可远端领取的尾部边界
W_i = Tail_i - Head_i
```

`W_i` 只表示可让渡剩余工作量估计。

每个节点还维护活动计数：

```text
InFlight_i
```

`InFlight_i` 是节点 `i` 的原子活动计数，记录正在发起领取或已经领取但尚未执行及输出完成的任务活动。它不是快照候选视图的一部分，只用于全局终止判断。

## 3. 快照槽与控制字

快照槽结构：

```text
SnapshotSlot {
  ViewState        // ACTIVE | WAITING | TERMINATED
  JFI
  K_pool
  CandidateCount
  Candidates[]
}
```

候选数量满足：

```text
CandidateCount <= K_pool
K_eff = CandidateCount
```

全局控制字：

```text
AtomicControlWord = [Version | Lock | Leader_ID | Slot]
```

`Version` 和 `Slot` 用于读到同一版完整快照；它们不参与任务领取 CAS。

## 4. 请求节点流程

当节点存在空闲执行槽且本地没有可领取任务时，进入请求流程：

```text
read control word
read SnapshotSlot[Slot]
read control word again
if control words mismatch:
    retry snapshot read

if ViewState == TERMINATED:
    finish globally

if ViewState == WAITING:
    backoff or try OSP refresh

if ViewState == ACTIVE:
    probe Candidates[0:CandidateCount]
```

候选是否仍可让渡不由快照判断，而由后续实时 `QueueState_i` CAS 判断。

## 5. CAS 领取流程

一次获取活动开始前：

```text
atomic_add(InFlight_self, 1)
wait until InFlight_self is remotely visible
```

对候选供给节点：

```text
state = candidate.queue_state_snapshot or RDMA_READ(QueueState_provider)
head, tail = unpack(state)
W_rem = tail - head

if W_rem < B_min:
    try next candidate

C_transfer = min(W_rem, max(B_min, min(ceil(W_rem/2), B_max)))
new_tail = tail - C_transfer

CAS QueueState_provider:
    expected = [head | tail]
    swap     = [head | new_tail]
```

CAS 成功：

```text
install microbatch [new_tail, tail)
execute task and produce output
atomic_sub(InFlight_self, 1)
```

CAS 失败：

```text
read returned current QueueState
if current range still transferable and retry budget remains:
    retry within same InFlight activity
else:
    try next candidate or give up
```

若本次获取最终失败且没有获得任务：

```text
atomic_sub(InFlight_self, 1)
```

## 6. OSP 快照发布流程

请求节点在无有效快照、候选耗尽或需要刷新时尝试发布快照：

```text
Entry CAS:
  [V,0,0,S] -> [V,1,MyID,S]
```

获得发布权后：

```text
for each node i:
    read QueueState_i

for each node i:
    read InFlight_i

compute transferable W_i = Tail_i - Head_i
build candidates where W_i >= B_min
compute JFI
set ViewState:
    ACTIVE if candidates exist
    WAITING if no candidates but queues or InFlight remain
    TERMINATED if all queues empty and all InFlight_i == 0

write complete shadow SnapshotSlot
publish by Exit CAS
```

发布：

```text
Exit CAS:
  [V,1,MyID,S] -> [V+1,0,0,S']
```

发布 `TERMINATED` 前应重复确认一轮队列和 `InFlight`，避免读取到领取活动之间的瞬时空状态。

## 7. 本地执行与状态单调性

本地执行线程推进 `Head_i` 时也通过同一原子访问域更新 `QueueState_i`。远端请求节点缩减 `Tail_i` 时使用 RDMA CAS。实现需保证本地领取和远端让渡进入同一原子访问域；可采用 loopback RDMA atomic，或依赖平台提供 CPU-RNIC 原子一致性。

`QueueState_i` 对可让渡范围只做单调收缩。CAS 成功领取的区间不会重新进入可让渡范围；任务完成只递减 `InFlight_i`，不修改供给节点已收缩的 `Tail_i`。

## 8. 故障边界

核心实现假设任务领取和执行期间节点不失效。若请求节点 CAS 成功后故障，需要额外任务恢复和输出去重机制；若发布节点获得锁后失效，需要 epoch 或 fencing 防止旧发布者污染 shadow slot。这些属于容错扩展，不进入核心实现流程。

