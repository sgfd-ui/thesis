# 机制2：边界保留驱动的双侧负载识别与初始负载规划代码流程说明

## 1. 模块职责

代码层将机制2实现为当前轮负载识别与初始计划生成模块。它接收机制1输出的历史计划种子、`K_reuse`、`K_check`、`Omega_residual` 和资源上下文，输出完整初始负载计划。

```text
LoadRecognitionInput {
  reuse_mode
  K_reuse
  K_check
  Omega_residual
  plan_seed
  validation_summary
  resource_context
}

InitialLoadPlan {
  H_R
  H_S
  state_combination_map
  work_estimate
  route_plan
  split_parameters
  initial_task_assignment
}
```

## 2. 识别范围选择

入口根据 `reuse_mode` 决定工作范围：

```text
FULL_REUSE:
  skip residual candidate discovery
  instantiate plan from plan_seed and current resource_context

PARTIAL_REUSE:
  reuse K_reuse
  scan Omega_residual
  prioritize K_check in verification

REBUILD:
  set Omega_residual = full current input
  run full candidate discovery and verification
```

`K_check` 是键集合，`Omega_residual` 是输入扫描范围，二者不能混用。残余范围必须重新发现新候选，不能只统计 `K_check`。

## 3. 本地边界候选保留

每个节点在本地残余分片上扫描连接键，分别处理 $R$ 和 $S$：

```text
for key in residual_shards_X:
    update bounded candidate summary
    keep stable leading candidates
    keep controlled boundary candidates
```

候选结构不需要在代码说明中绑定某一种流式算法，但必须满足：

```text
local_export_X <= alpha * k
exported_candidates = stable_candidates + boundary_candidates
```

稳定候选用于保留明显高频键，边界候选用于降低接近阈值或替换边界的漏报风险。

## 4. 候选集合合并

每侧候选集合按以下来源合并：

```text
C_X =
  K_reuse_X
  union K_check_X
  union stable_summary_candidates_X
  union residual_candidates_X
```

共同统计集合为：

```text
C = C_R union C_S
```

一个键只要在任一侧进入候选集合，就必须查询两侧频率，避免只根据单侧候选直接导出状态组合。

## 5. 双侧统计与边界回查

默认统计路径为候选受限的二次计数扫描：

```text
for key in C:
    count R-side contribution
    count S-side contribution
```

稳定分片贡献按机制1提供的精确表、索引或 sketch 区间查询；残余分片贡献来自当前扫描。若频率为区间：

```text
if lower >= hotspot_threshold:
    mark side-hot
elif upper < hotspot_threshold:
    mark side-cold
else:
    add to boundary_lookup_set
```

边界回查以集合为单位批量执行：

```text
batch_count(stable_shards, boundary_lookup_set)
```

若边界集合过大、稳定分片回查成本过高或未枚举键上界仍可能跨越热点边界，返回 `REBUILD`。

## 6. 状态组合与工作量估计

双侧热点集合确定后，代码生成：

```text
if k in H_R and k in H_S: z(k) = HH
if k in H_R and k not in H_S: z(k) = HC
if k not in H_R and k in H_S: z(k) = CH
else: z(k) = CC
```

`z(k)` 只由两侧热点成员关系决定。工作量高的键可以进入特殊规划对象，但不能改变 `H_R/H_S` 或 `z(k)`。

代码同时计算：

```text
W(k)       // intrinsic join work
Cost(k,a) // strategy-dependent cost
```

`W(k)` 包含两侧频率、匹配规模、输出规模和输入本身处理量；移动、复制、拆分和节点资源占用放入 `Cost(k,a)`。

## 7. 初始计划生成

计划合成流程：

```text
build planning view:
  historical plan seed
  current recognition result
  high-work boundary candidates
  default rule for ordinary keys

enumerate candidate strategies:
  hash partition
  light-side replication
  key-internal split
  classified routing

for special key k:
  evaluate z(k), W(k), Cost(k,a), resource_context
  choose strategy and parameters

emit:
  route_plan
  split_parameters
  initial_task_assignment
```

普通键不需要全部显式枚举，默认进入常规哈希任务。特殊规划键需要输出显式路由、热点任务拆分参数和初始任务归属。

## 8. 输出给机制3

机制2输出初始任务计划及其边界：

```text
TaskRange {
  task_id
  key_or_partition_scope
  route_rule
  split_parameter
  assigned_node
  progress_boundary
}
```

这些任务是执行前初始任务，不是运行时微任务。机制3只接收可描述的任务边界和执行进度，并在运行时从真实剩余工作中按需划分微批次。

