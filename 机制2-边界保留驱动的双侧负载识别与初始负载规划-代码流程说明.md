# 机制2：边界保留驱动的双侧负载识别与初始负载规划代码流程说明

## 1. 模块职责

代码层将机制2实现为当前轮负载识别与初始计划生成模块。它接收机制1输出的历史计划种子、`K_reuse`、`K_check`、`Omega_residual`、`StableStateRef`、验证摘要和资源上下文，输出完整初始负载计划。

```text
LoadRecognitionInput {
  reuse_mode
  K_reuse
  K_check
  Omega_residual
  stable_state_ref
  plan_seed
  validation_summary
  resource_context
}

InitialLoadPlan {
  H_R
  H_S
  state_combination_map
  work_estimate
  strategy_costs
  route_plan
  split_parameters
  initial_task_assignment
  task_boundaries
}
```

## 2. 识别范围选择

入口根据 `reuse_mode` 决定工作范围：

```text
FULL_REUSE:
  assert Omega_residual is empty
  assert K_check is empty
  skip residual candidate discovery
  instantiate plan from committed historical state and current resource_context

PARTIAL_REUSE:
  use K_reuse only as strategy-template seed
  scan Omega_residual for residual candidates
  confirm K_check
  query stable contribution through stable_state_ref

REBUILD:
  set Omega_residual = full current input
  clear K_reuse as planning shortcut
  run full candidate discovery and verification
```

`K_check` 是显式键集合，`Omega_residual` 是输入扫描范围。残余范围必须重新发现新候选，不能只统计 `K_check`。

## 3. 本地候选导出接口

每个节点在本地残余分片上分别处理 $R$ 和 $S$：

```text
for key in residual_shards_X:
    update bounded candidate summary
    retain principal candidates
    retain boundary candidates
```

候选导出接口为：

```text
CandidateExport {
  relation_id
  node_id
  principal_candidates
  boundary_candidates
  omitted_upper_bound
}
```

要求：

```text
len(principal_candidates + boundary_candidates) <= alpha * k
```

其中 `omitted_upper_bound` 表示本节点本关系上未枚举键的最大可能频率。主候选用于保留明显高频键，边界候选用于保留接近阈值或替换边界的键。

## 4. 候选集合合并与充分性检查

协调端汇聚所有候选导出，并计算每侧未枚举键上界：

```text
B_omit_X = sum(export.omitted_upper_bound for export in exports_X)
```

共同候选集合为：

```text
C =
  K_reuse
  union K_check
  union stable_summary_candidates_R
  union stable_summary_candidates_S
  union residual_candidates_R
  union residual_candidates_S
```

只要一个键进入任意一侧来源，就必须查询两侧统计。候选充分性检查为：

```text
if hotspot_rule == threshold:
    sufficient_X = (B_omit_X < theta_X)

if hotspot_rule == topk:
    hth_lower_bound_X = hth_largest_lower_bound(C, side=X)
    sufficient_X = (B_omit_X < hth_lower_bound_X)
```

如果 `sufficient_X` 为假，模块不能声明当前候选集合已经覆盖全局热点边界，需要执行：

```text
expand candidate capacity
or read additional stable summaries
or batch lookup omitted-risk keys
or return REBUILD_REQUIRED
```

## 5. 双侧统计与边界回查

默认统计路径分为残余贡献和稳定贡献：

```text
residual_count = count_candidates(Omega_residual, C)
stable_contribution = query_stable_state(stable_state_ref, C)
```

稳定贡献查询顺序：

```text
ExactMap -> CountIndex -> Sketch interval -> batch stable lookup
```

合成当前规划频率：

```text
if stable_contribution is exact:
    f_plan_X[k] = f_stable_X[k] + f_residual_X[k]
else:
    L_plan_X[k] = L_stable_X[k] + f_residual_X[k]
    U_plan_X[k] = U_stable_X[k] + f_residual_X[k]
```

边界判断必须在生成 `H_R/H_S` 之前完成：

```text
if lower >= hotspot_threshold:
    mark side-hot
elif upper < hotspot_threshold:
    mark side-cold
else:
    add to boundary_lookup_set
```

批量回查：

```text
if boundary_lookup_set is not empty:
    exact_counts = batch_count(stable_shards, boundary_lookup_set)
    update f_plan intervals to exact values where possible
```

若边界集合过大、稳定分片回查成本过高，或未枚举键上界仍可能跨越热点边界，返回 `REBUILD_REQUIRED`。

## 6. 状态组合、工作量和策略代价

边界确认完成后生成两侧热点集合：

```text
for key in C:
    H_R[key] = is_hot_on_R(key)
    H_S[key] = is_hot_on_S(key)
```

状态组合由热点成员关系唯一决定：

```text
if k in H_R and k in H_S: z(k) = HH
if k in H_R and k not in H_S: z(k) = HC
if k not in H_R and k in H_S: z(k) = CH
else: z(k) = CC
```

工作量与策略代价分开计算：

```text
W(k) = beta_in * (f_R(k) + f_S(k))
     + beta_match * f_R(k) * f_S(k)
     + beta_out * output_estimate(k)

Cost(k, a) = move_cost(k, a)
           + replication_cost(k, a)
           + split_cost(k, a)
           + resource_cost(k, a)
```

`z(k)` 不能被 `W(k)` 或 `Cost(k,a)` 反向修改。高工作量边界键可以进入特殊规划对象集合，但不改变其 `HH/HC/CH/CC`。

## 7. 初始计划生成

计划合成流程：

```text
build planning view:
  historical plan seed
  current recognition result
  high-work boundary candidates
  default rule for ordinary keys

for k in K_reuse under PARTIAL_REUSE:
  use historical template as preferred candidate
  recompute z(k), W(k), Cost(k,a) after merging residual contribution
  reject template if outside valid scope

enumerate candidate strategies:
  hash partition
  light-side replication
  key-internal split
  classified routing

for special key k:
  evaluate z(k), W(k), Cost(k,a), resource_context
  choose strategy and parameters

emit:
  route_plan
  split_parameters
  initial_task_assignment
  task_boundaries
```

普通键不需要全部显式枚举，默认进入常规哈希任务。特殊规划键需要输出显式路由、热点任务拆分参数和初始任务归属。

## 8. 输出给机制3

机制2输出初始任务计划及其边界：

```text
TaskRange {
  task_id
  key_or_partition_scope
  route_rule
  split_parameter
  assigned_node
  progress_boundary
}
```

这些任务是执行前初始任务，不是运行时微任务。机制3只接收可描述的任务边界和执行进度，并在运行时从真实剩余工作中按需划分微批次。
