# 机制1改进B：历史统计状态与复用判定

## 1. 问题定位

基于历史验证的增量初始负载规划不能只保存上一轮热点名单。若残余范围中出现新候选，系统还需要知道该键在稳定分片中的贡献；若历史状态只保存 top-k 热点，新候选在稳定分片中的频率可能无法获得，当前轮总频率就无法可靠合成。

本改进关注历史控制状态应保存什么、如何判定复用范围，以及什么情况下必须回退。目标是让历史状态既能支持跨轮控制信息复用，又不会被误解为复用上一轮完整任务计划或连接结果。

## 2. 成立前提

历史统计状态以不可变分片为基本单位维护。每个分片状态写作：

$$
State(p)=\langle Meta_p,\ CandidateSummary_p,\ ExactMap_p,\ Sketch_p\rangle.
$$

`Meta_p` 保存关系标识、分片标识、版本、行数、快照语义和统计生成时间。`CandidateSummary_p` 是可合并的重频候选摘要，容量可设为 $m=\alpha h$，用于导出稳定候选和边界候选，并提供未保留键的频率上界。`ExactMap_p` 保存摘要中保留键的精确频率，服务历史稳定键和边界键快速确认。`Sketch_p` 用于查询残余范围中新出现候选在稳定分片中的近似贡献。

## 3. 核心流程

当前轮首先通过结构签名检索历史状态。结构签名包括输入关系、连接键、连接类型和规范化过滤谓词；动态上下文包括时间窗口、分片版本向量、节点数、并行度、内存状态和拓扑。结构签名决定能否检索历史，动态上下文决定复用范围和参数是否需要重算。

检索成功后，系统比较分片版本向量。版本未变化的分片贡献可以进入稳定统计合成；版本变化、新增、删除或无法确认的分片进入残余范围。历史策略模板只描述某类键倾向采用的路由策略、热点拆分方式和参数计算规则，不包含当前轮实际拆分数量、任务边界、节点归属或 offset。

验证输出三个对象：可复用历史策略模板及其有效范围、需要重新确认的显式键集合 $K_{check}$、需要重新执行候选发现的残余输入范围 $\Omega_{residual}$。其中 $K_{check}$ 是键集合，$\Omega_{residual}$ 是数据范围，二者不能混用。

## 4. 与第5/6章接口

`FULL_REUSE` 表示结构签名一致、分片版本完全一致、历史统计完整且历史边界键没有未解决不确定性。此时第 6 章可以跳过当前轮候选发现，只根据当前资源上下文重新实例化路由、拆分参数和初始任务归属。

`PARTIAL_REUSE` 表示存在稳定分片和变化分片。第 5 章提供稳定分片统计、历史策略模板、$K_{check}$ 和 $\Omega_{residual}$；第 6 章在残余范围上做双侧负载识别，并把残余统计与稳定贡献合成当前轮规划频率。

`REBUILD` 表示历史状态不能提供可靠计划种子。触发条件包括结构签名实质变化、分片没有版本或快照信息、稳定摘要缺失或版本不兼容、候选容量不足以排除未枚举键跨越热点边界、边界键过多、精确回查需要读取大部分稳定数据，或残余范围已经接近完整输入。

## 5. 正确性边界

历史状态只影响控制面和初始负载规划，不改变连接谓词，也不缓存连接结果。历史统计状态即使过期，后果也应限制在热点分类不准、初始路由不理想或长尾加重，而不是输出语义错误。这个结论依赖两个前提：未确认范围必须进入当前轮识别或默认路径，最终执行计划必须满足记录对覆盖、任务互斥和唯一输出。

不能把“继续复用历史计划”当成历史状态再次得到验证。状态刷新必须来自当前轮真实计数、当前轮识别结果、实际执行时间、输出规模、长尾和再平衡反馈。长期未经过当前轮识别确认的状态应触发置信度衰减或强制补充识别。

## 6. 开销与退化路径

历史统计状态带来的额外开销包括分片元数据维护、候选摘要存储、精确表存储、sketch 存储和复用前验证。收益来自减少重复候选发现、双侧统计和计划构造。若历史状态验证成本高于完整识别收益，或者边界候选需要大范围精确回查，应直接进入 `REBUILD`。

对于高基数连接键，不建议默认保存全键精确频率索引。更合理的实现是“候选摘要 + 候选精确频率 + sketch + 边界键按需回查”。这既避免状态爆炸，也能为残余新候选提供稳定部分贡献估计。

## 7. 是否建议并入正文

建议把本改进中的分片状态结构、复用判定和退化条件吸收到第 5 章。正文不需要展开具体 sketch 参数，但必须明确：历史状态不能只保存热点名单，稳定分片必须支持查询候选键贡献，否则不能可靠合成当前轮频率。

