# 机制2改进A：残余范围双侧候选识别

## 1. 问题定位

边界保留驱动的双侧负载识别与初始负载规划接收第 5 章输出的历史计划种子、待确认键集合和残余输入范围。若直接把残余范围理解成“未确认键集合”，第 6 章只能重新检查已知键，无法发现残余分片中新出现的热点。因此，本改进明确：残余范围是数据范围，候选识别必须在残余分片上重新执行。

目标是在不扫描完整当前输入的情况下，形成足够可靠的当前轮共同候选集合，并为后续双侧统计、热点状态组合和初始计划合成提供输入。

## 2. 成立前提

第 5 章需要提供四类输入：$K_{reuse}$、$K_{check}$、$\Omega_{residual}$ 和稳定分片统计状态。其中，$K_{reuse}$ 是可复用历史策略模板的稳定键集合，$K_{check}$ 是已经暴露但仍需确认的显式键集合，$\Omega_{residual}$ 是需要重新扫描或检查的残余分片范围。

残余分片扫描必须读取残余范围中的连接键列，而不是只统计 $K_{check}$。否则，残余范围中新出现的热点无法进入候选集合。

## 3. 核心流程

每个节点只扫描自己持有的残余分片，并在扫描过程中维护边界保留候选结构。该结构保留两类键：稳定候选和边界候选。稳定候选是在残余扫描中频率明显领先或达到保留条件的键；边界候选是接近热点阈值、接近替换边界或增长较快的键。扫描结束后，每个节点每个关系导出的候选总数不超过 $\alpha h$。

候选集合不能只来自残余扫描。对关系 $X\in\{R,S\}$，当前轮候选来源应为：

$$
C_X=K_{reuse,X}\cup K_{check,X}\cup C_X^{stable}\cup C_X^{residual}.
$$

其中，$C_X^{stable}$ 来自稳定分片历史摘要中的热点和边界候选，$C_X^{residual}$ 来自残余分片当前扫描。系统随后构造共同候选集合：

$$
C=C_R\cup C_S.
$$

即使一个键只从一侧进入候选，也必须查询另一侧频率，因为 `HH/HC/CH/CC` 两侧热点状态组合依赖左右关系的共同统计。

## 4. 与第5/6章接口

在 `FULL_REUSE` 下，$\Omega_{residual}=\emptyset$ 且 $K_{check}$ 为空，第 6 章可跳过残余候选发现，只根据历史稳定部分和当前资源上下文实例化计划。

在 `PARTIAL_REUSE` 下，第 6 章只在 $\Omega_{residual}$ 上执行候选发现，同时把 $K_{reuse}$、$K_{check}$ 与稳定摘要候选纳入共同候选集合。这样，历史稳定键不会因为残余扫描中不出现而丢失，新候选也不会因为历史中不存在而被忽略。

在 `REBUILD` 下，$\Omega_{residual}$ 覆盖完整当前输入，候选发现退化为完整当前轮双侧负载识别。

## 5. 识别边界

候选保留不能保证绝对无漏报。为了避免“稳定部分一般、残余部分也一般，但相加后成为热点”的键被漏掉，候选摘要需要提供未保留键的最大可能频率上界。设稳定摘要和残余摘要对未保留键分别给出 $B_X^{stable}$ 与 $B_X^{residual}$，则任意未进入候选集合的键在当前轮的频率上界为：

$$
B_X^{omit}=B_X^{stable}+B_X^{residual}.
$$

若 $B_X^{omit}$ 小于当前候选第 $h$ 大频率的下界 $T_X$，则未枚举键不可能进入 top-$h$ 热点集合。若该条件不成立，说明候选容量不足或边界不确定性过高，系统应增大过采样、扩展边界候选或进入 `REBUILD`。

## 6. 正确性边界

候选遗漏只影响负载规划质量，不直接改变连接结果。漏掉的热点键仍可沿默认哈希路径执行，因此连接输出仍由基础执行层的覆盖和互斥约束保证。不过，候选遗漏可能导致初始负载计划失准，增加执行长尾。因此，本机制的目标是提高当前轮负载语义可靠性，而不是证明所有热点都被绝对召回。

## 7. 开销与退化路径

本机制的主要开销来自残余分片扫描、候选导出、跨节点候选汇聚和双侧统计确认。候选通信和合并状态受 $2N\alpha h$ 控制，但输入访问成本取决于 $\Omega_{residual}$ 覆盖的残余分片规模。

当残余比例过大、候选上界无法排除未枚举热点、候选集合过大或稳定贡献回查代价接近完整扫描时，应退化为 `REBUILD`。这使增量识别不会在不适合的输入上强行增加控制面开销。

## 8. 是否建议并入正文

建议并入第 6.1、6.3、6.4 和 6.8。正文需要明确 `K_check` 与 $\Omega_{residual}$ 的差异，并把候选集合从“残余候选”扩展为历史稳定键、待确认键、稳定摘要候选和残余候选的并集。

