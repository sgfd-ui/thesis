# 机制2：边界保留驱动的双侧负载识别与初始负载规划

## 1. 机制定位

本机制负责在当前轮形成可靠的两侧热点状态组合、内在连接工作量估计和完整初始负载计划。它接收第 5 章输出的历史稳定部分、待确认键集合和残余输入范围，并补齐当前轮变化部分的候选发现、双侧统计、边界回查和计划合成。

机制2由两个内部组成构成：

- 残余范围双侧候选识别：在残余输入范围内发现新候选，并与历史稳定候选合并。
- 边界回查与初始负载计划合成：对频率区间跨越阈值的键执行精确确认，并将负载语义转化为初始计划。

## 2. 候选集合

第 5 章输出的 `K_check` 是显式键集合，`Omega_residual` 是需要重新扫描或检查的输入范围，二者不能混用。对关系 $X\in\{R,S\}$，当前轮候选来源为：

$$
C_X=K_{reuse,X}\cup K_{check,X}\cup C_X^{stable}\cup C_X^{residual}.
$$

其中，`K_reuse` 来自历史稳定键，`K_check` 来自待确认键，$C_X^{stable}$ 来自稳定分片摘要，$C_X^{residual}$ 来自残余分片当前扫描。共同候选集合为：

$$
C=C_R\cup C_S.
$$

即使一个键只从一侧进入候选，也必须获得两侧频率，才能导出 `HH/HC/CH/CC`。

## 3. 双侧统计与边界回查

对候选键 $k$，系统获得规划频率或频率区间：

$$
L_X^{plan}(k)\le f_X^{plan}(k)\le U_X^{plan}(k).
$$

若 $L_X^{plan}(k)\ge \theta_X$，则该键进入 $H_X$；若 $U_X^{plan}(k)<\theta_X$，则该键排除出 $H_X$；若 $L_X^{plan}(k)<\theta_X\le U_X^{plan}(k)$，则进入边界回查集合。使用 top-k 判定时，只对上界可能越过候选第 $h$ 大频率下界的键继续确认。

精确回查应以候选集合为单位批量执行，避免逐键扫描稳定分片。若边界集合过大或回查成本接近完整识别，应退化为 `REBUILD`。

## 4. 负载语义与计划合成

确认频率后，系统分别导出 $H_R$ 和 $H_S$，并形成两侧热点状态组合：

$$
z(k)\in\{HH,HC,CH,CC\}.
$$

状态组合只描述两侧热点成员关系，不能因为 $f_R(k)f_S(k)$ 或 $W(k)$ 较高而反向修改 $H_R/H_S$。内在连接工作量 $W(k)$ 只表示键本身产生的输入处理、匹配和输出规模；策略相关代价单独写作 $Cost(k,a)$。

初始计划合成包含四步：构造当前轮规划视图，生成候选执行策略，依据 $z(k),W(k),Cost(k,a),Ctx$ 实例化路由和拆分参数，最后输出特殊规划键显式任务、普通键默认任务和历史/当前识别结果的合并计划。

## 5. 正确性与边界

候选遗漏或边界误判只影响负载规划质量，不直接改变连接结果。漏掉的热点键仍可沿默认哈希路径执行。连接正确性依赖最终计划满足记录覆盖、任务互斥、默认路径完备和唯一输出约束。

本机制不承诺完整恢复所有全局 top-k 热点。它通过边界保留、双侧统计和边界回查提高候选集合内的负载语义可靠性；当候选上界无法排除未枚举热点或回查成本过高时，应退化为 `REBUILD`。

