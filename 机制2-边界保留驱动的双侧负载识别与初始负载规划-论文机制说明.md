# 机制2：边界保留驱动的双侧负载识别与初始负载规划

## 1. 机制定位

边界保留驱动的双侧负载识别与初始负载规划负责把第 5 章输出的历史计划种子、待确认键、残余输入范围和资源上下文转化为当前轮完整初始负载计划。它不是每轮固定执行完整全局统计，也不是单纯的候选验证步骤，而是在 `FULL_REUSE/PARTIAL_REUSE/REBUILD` 三种模式下选择不同识别范围，形成当前轮可用于规划的两侧热点集合、两侧热点状态组合、内在工作量估计和初始任务边界。

机制2的核心职责包括：确定当前轮识别范围，本地保留主候选和边界候选，跨节点汇聚候选集合，查询稳定分片贡献与残余分片贡献，执行边界确认，生成 $H_R/H_S$ 与 `HH/HC/CH/CC`，并基于 $W(k)$、$Cost(k,a)$ 和资源上下文合成初始负载计划。

## 2. 阶段接口

第 5 章传入的对象需要区分键集合和输入范围：

- `K_reuse`：已验证稳定的显式键集合，只能作为历史策略模板和参数规则种子。
- `K_check`：已显式暴露但状态不确定、需要当前轮确认的键集合。
- `Omega_residual`：需要重新执行候选发现的残余输入范围，可能包含尚未暴露的新候选。
- `StableStateRef`：可查询稳定分片贡献的历史状态引用。
- `PlanSeed`：历史策略模板及其有效范围。
- `ResourceContext`：当前轮节点数、并行度、内存、拓扑和资源状态。

三种模式下的接口语义为：

- `FULL_REUSE`：`Omega_residual=empty` 且 `K_check` 为空，不执行当前轮候选发现；使用已验证完整历史状态并根据当前资源上下文重算参数。
- `PARTIAL_REUSE`：只默认扫描 `Omega_residual`；稳定分片贡献通过 `StableStateRef` 查询；`K_reuse` 是策略模板种子，不能无条件沿用最终历史策略；`K_check` 进入当前轮确认路径。
- `REBUILD`：`Omega_residual` 覆盖当前完整输入，执行完整当前轮双侧负载识别和初始计划生成。

## 3. 候选保留、汇聚与充分性判定

每个节点在每个关系上维护容量为 $m=\alpha k$ 的候选结构，并导出两类候选：

- 主候选：本地观察中明显领先或达到保留条件的键。
- 边界候选：接近热点阈值、接近替换边界或具有增长风险的键。

每节点每关系导出候选不超过 $\alpha k$，因此跨节点汇聚前候选条目数上界为 $2N\alpha k$。候选结构还需要给出未枚举键的本地频率上界 $b_{X,j}^{omit}$，汇聚后形成

$$
B_X^{omit}=\sum_j b_{X,j}^{omit}.
$$

共同候选集合由历史键、待确认键、稳定摘要候选和残余候选共同组成：

$$
C=K_{reuse}\cup K_{check}\cup C_R^{stable}\cup C_S^{stable}\cup C_R^{residual}\cup C_S^{residual}.
$$

只要键进入任意一侧候选来源，就必须获得它在 $R$ 和 $S$ 两侧的统计。候选充分性必须显式检查：若采用 top-$h$，只有 $B_X^{omit}$ 小于候选第 $h$ 大频率下界时，才能排除未枚举键进入 top-k；若采用阈值规则，只有 $B_X^{omit}<\theta_X$ 时，才能排除未枚举键越过阈值。否则需要扩大候选、读取更多摘要、执行批量回查或进入 `REBUILD`。

## 4. 双侧统计与边界确认

`PARTIAL_REUSE` 的统计路径分为残余贡献和稳定贡献。残余范围 `Omega_residual` 必须重新扫描以发现新候选，必要时再对共同候选集合执行候选受限二次计数。稳定分片默认不重新扫描，而是通过 `StableStateRef` 查询贡献：优先使用精确表或计数索引，其次使用带误差界的摘要区间；只有边界键需要时，才批量访问稳定分片。

若稳定贡献可精确获得，则当前规划频率为

$$
f_X^{plan}(k)=f_X^{stable}(k)+f_X^{residual}(k).
$$

若稳定贡献只能给出区间，则使用

$$
L_X^{plan}(k)=L_X^{stable}(k)+f_X^{residual}(k),\qquad
U_X^{plan}(k)=U_X^{stable}(k)+f_X^{residual}(k).
$$

边界确认发生在生成热点集合之前。对关系 $X$，若 $L_X^{plan}(k)\ge\theta_X$，则 $k$ 可进入 $H_X$；若 $U_X^{plan}(k)<\theta_X$，则可排除；若 $L_X^{plan}(k)<\theta_X\le U_X^{plan}(k)$，则进入边界回查集合。top-k 判定同样需要先处理可能越过第 $k$ 大频率边界的候选。边界回查应批量执行；若回查集合过大或成本接近完整重建，则回退到 `REBUILD`。

## 5. 两侧负载语义与工作量估计

边界确认完成后，系统才生成当前轮用于规划的 $H_R$ 和 $H_S$。两侧热点状态组合定义为：

$$
z(k)\in\{HH,HC,CH,CC\}.
$$

`HH/HC/CH/CC` 只表示 $k$ 在 $H_R/H_S$ 中的成员关系。不能因为 $f_R(k)f_S(k)$ 或 $W(k)$ 较高而反向修改 $H_R/H_S$，也不能把高工作量 `CC` 键提升为 `HH`。高工作量边界键可以进入特殊规划对象集合，但其状态组合定义不变。

内在连接工作量只描述键本身带来的输入处理、匹配和输出规模，可抽象为

$$
W(k)=\beta_{in}(f_R(k)+f_S(k))+\beta_{match}f_R(k)f_S(k)+\beta_{out}o(k).
$$

策略相关代价单独记为 $Cost(k,a)$，用于描述候选策略 $a$ 的数据移动、复制、拆分和资源占用。规划依据为

$$
z(k),\ W(k),\ Cost(k,a),\ Ctx.
$$

## 6. 当前轮初始负载计划合成

计划合成分为四步。

第一，构造当前轮规划视图。该视图包含历史计划种子、当前轮识别结果、高工作量边界候选和普通键默认规则。`PARTIAL_REUSE` 中，`K_reuse` 只提供策略模板和参数规则，仍需合并残余贡献后重新检查状态组合、工作量和策略代价。

第二，生成候选执行策略。系统调用已有倾斜感知连接策略，包括分类路由、轻侧复制、键内拆分和常规哈希，不重新提出底层连接算子。

第三，评估策略并实例化参数。系统根据 $z(k)$、$W(k)$、$Cost(k,a)$ 和资源上下文确定路由策略、拆分数量、任务边界和初始节点归属。

第四，形成完整初始负载计划。输出包含特殊规划键的显式任务、普通键默认任务，以及历史稳定部分和当前识别部分的合并结果。

## 7. 正确性、边界与第 7 章接口

机制2的识别准确性影响负载规划质量，不直接决定连接结果正确性。若候选遗漏或边界误判导致某个热点沿普通路径执行，可能造成负载不均衡或执行长尾，但只要普通默认路径完整，连接谓词、任务覆盖和输出多重集合不应改变。

连接正确性依赖最终初始计划满足记录覆盖、任务互斥、默认路径完备和唯一输出约束。机制2输出的初始任务必须带有可定位工作范围、路由规则、拆分参数、初始归属和进度边界。这些任务是执行前初始任务，不是运行时微任务；第 7 章只接收这些边界，并在执行过程中从真实剩余工作中按需划分微批次。
