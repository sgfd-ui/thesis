# 机制2：边界保留驱动的双侧负载识别与初始负载规划

## 1. 机制定位

边界保留驱动的双侧负载识别与初始负载规划负责在当前轮形成可靠的两侧热点状态组合、内在连接工作量估计和完整初始负载计划。它接收第 5 章输出的历史稳定部分、待确认键集合、残余识别范围和资源上下文，并补齐当前输入中变化、不确定或缺失的负载语义。

本机制不是单纯的候选验证。它包含当前轮识别范围确定、本地边界候选保留、跨节点候选汇聚、双侧统计确认、边界回查、工作量估计和初始计划合成。最终输出供基础连接执行层使用的初始路由、热点任务拆分参数和初始任务归属。

## 2. 阶段接口

第 5 章传入的对象需要区分两类范围：

- `K_check`：已经显式发现但状态不确定、需要当前轮确认的键集合。
- `Omega_residual`：需要重新执行候选发现的残余输入范围，可能包含尚未暴露的新热点。

三种复用模式下的处理方式为：

- `FULL_REUSE`：`Omega_residual=\varnothing`，不执行完整当前轮候选发现，只根据历史计划种子和当前资源上下文完成参数实例化。
- `PARTIAL_REUSE`：复用 `K_reuse`，在 `Omega_residual` 上执行当前轮双侧负载识别，并重点确认 `K_check`。
- `REBUILD`：`Omega_residual` 覆盖当前轮完整输入，执行完整双侧负载识别。

当前章输出的是当前轮用于初始规划的 $H_R/H_S$、两侧热点状态组合、内在连接工作量 $W(k)$、候选策略代价 $Cost(k,a)$ 和完整初始负载计划。

## 3. 候选保留与双侧统计

对每个关系 $X\in\{R,S\}$，当前轮候选来源为：

$$
C_X=K_{reuse,X}\cup K_{check,X}\cup C_X^{stable}\cup C_X^{residual}.
$$

其中，`K_reuse` 来自历史稳定键，`K_check` 来自待确认键，$C_X^{stable}$ 来自稳定分片摘要，$C_X^{residual}$ 来自残余分片当前扫描。共同验证集合为：

$$
C=C_R\cup C_S.
$$

即使一个键只从一侧进入候选，也必须获得其在 $R$ 和 $S$ 两侧的统计，才能导出 `HH/HC/CH/CC`。候选受限的二次计数扫描是默认统计路径：系统只对共同候选集合维护精确计数或区间估计，但输入访问范围仍由残余分片和必要的稳定贡献回查决定。

双侧统计确认只提高候选集合内的负载语义可靠性，不承诺完整找回未进入候选集合的所有键。若候选摘要给出的未枚举键上界仍可能跨越热点边界，则需要扩大候选、执行精确回查或回退到 `REBUILD`。

## 4. 两侧负载语义与内在工作量

系统分别根据当前统计导出两侧热点集合 $H_R$ 和 $H_S$，再形成两侧热点状态组合：

$$
z(k)\in\{HH,HC,CH,CC\}.
$$

`HH/HC/CH/CC` 只表示 $k$ 在 $H_R/H_S$ 中的成员关系。不能因为 $f_R(k)f_S(k)$ 或 $W(k)$ 较高而反向修改 $H_R/H_S$，也不能把高工作量 `CC` 键提升为 `HH`。高工作量边界键可以进入特殊规划对象集合，但其热点状态组合定义不变。

内在连接工作量 $W(k)$ 描述键本身带来的输入处理、匹配规模、结果输出和局部处理量。策略相关代价单独记为 $Cost(k,a)$，用于描述候选策略 $a$ 的数据移动、复制、拆分、节点资源占用和具体执行代价。规划决策依据为：

$$
z(k),\ W(k),\ Cost(k,a),\ Ctx.
$$

## 5. 边界回查

对候选键 $k$，系统可能得到规划频率区间：

$$
L_X^{plan}(k)\le f_X^{plan}(k)\le U_X^{plan}(k).
$$

若 $L_X^{plan}(k)\ge\theta_X$，则该键在关系 $X$ 侧可判为热点；若 $U_X^{plan}(k)<\theta_X$，则可排除；若

$$
L_X^{plan}(k)<\theta_X\le U_X^{plan}(k),
$$

则该键进入边界回查集合。采用 top-k 判定时，只有频率上界可能超过候选第 $h$ 大频率下界的键才需要继续确认。

边界回查应以候选集合为单位批量执行，避免逐键扫描稳定分片。若边界键过多或回查成本接近完整重建，系统应退化为 `REBUILD`。

## 6. 当前轮初始负载计划合成

计划合成分为四步。

第一，构造当前轮规划视图。该视图包含历史计划种子、当前轮识别结果、高工作量边界候选和普通键默认规则。它只显式枚举需要特殊规划的键，其余键进入默认哈希路径。

第二，生成候选执行策略。系统调用已有倾斜感知连接策略，包括分类路由、轻侧复制、键内拆分和常规哈希。本机制不重新提出底层连接算子。

第三，评估策略并实例化参数。系统根据 $z(k),W(k),Cost(k,a),Ctx$ 确定路由策略、热点任务拆分数量、任务边界和初始节点归属。

第四，形成完整初始负载计划。输出包含特殊规划键的显式任务、普通键的默认任务，以及历史稳定部分和当前识别部分的合并结果。

## 7. 正确性与识别边界

候选遗漏或边界误判只影响负载规划质量，不直接改变连接结果。漏掉的热点键仍可沿默认哈希路径执行；这可能造成负载不均衡或长尾，但不应造成结果缺失。

连接正确性依赖最终计划满足记录覆盖、任务互斥、默认路径完备和唯一输出约束。双侧负载识别负责提高初始负载计划质量，基础连接执行层和任务合成约束负责保持等值连接输出语义。

