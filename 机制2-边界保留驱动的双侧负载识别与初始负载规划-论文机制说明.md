# 机制2：边界保留驱动的双侧负载识别

## 1. 机制定位

边界保留驱动的双侧负载识别是当前轮负载事实识别算法。它接收机制1发出的 `RecognitionRequest`，在指定范围内保留边界候选、补全两侧频率、确认热点集合与状态组合、估计键级内在连接工作量，并返回 `LoadProfile_t`。

机制2以 `LoadProfile_t` 作为稳定输出接口。机制1在计划合成阶段根据 `LoadProfile_t`、`Cost(k,a)` 和资源上下文确定最终路由方式、拆分参数、任务边界和初始归属；这些规划决策不属于机制2。

## 2. 边界候选与覆盖条件

候选保留的目标不是保留 top-$h$ 排名本身，而是避免遗漏可能越过热点阈值或进入显式规划范围的边界键。对每个关系侧 $X\in\{R,S\}$，候选集合写为

$$
C_X=K_{reuse,X}\cup K_{check,X}\cup C_X^{residual}\cup B_X .
$$

其中，$C_X^{residual}$ 来自残余范围中的当前轮候选，$B_X$ 是依据摘要误差上界保留的边界候选。双侧确认使用 $C=C_R\cup C_S$。机制2不仅要证明未候选键不会成为热点，还要证明其工作量上界低于机制1在 `RecognitionRequest.Budget` 中给出的显式规划工作量界限 $\tau_W$。$\tau_W$ 只表示某个键是否必须在 `LoadProfile_t` 中显式物化的工作量覆盖界限，不是执行策略切换阈值，也不表示该键必然进入 $K_{explicit}$。

覆盖条件不通过逐个检查所有未枚举键实现，而由候选构建过程返回尾部覆盖证书。令

$$
\overline U_X^{tail}=\sup_{k\notin C}\left(U_X^{stable}(k)+U_X^{residual}(k)\right),\qquad X\in\{R,S\},
$$

并用内在工作量组成式的单调上界得到

$$
\overline U_W^{tail}=F\left(\overline U_R^{tail},\overline U_S^{tail},b_R,b_S,b_{\mathrm{out}}\right).
$$

这里要求 $F$ 对 $f_R$ 和 $f_S$ 单调不减，且 $c_{\mathrm{in}}$、$c_{\mathrm{probe}}$、$c_{\mathrm{out}}$ 等成本系数非负。$\overline U_R^{tail}$ 与 $\overline U_S^{tail}$ 可能来自不同的未候选键，因此代入 $F$ 得到的上界可能偏松，但不会低估任一未候选键；界过松只会触发候选扩大或完整识别，不影响正确性。

候选覆盖证书必须满足

$$
\overline U_R^{tail}<\theta_R,\qquad
\overline U_S^{tail}<\theta_S,\qquad
\overline U_W^{tail}<\tau_W .
$$

无法证明该证书时，机制2必须扩大候选、执行边界回查，或返回识别不充分，不能静默把未枚举键归入冷键或低工作量键。`Cert_R/Cert_S` 是双侧尾部覆盖证书的两个关系侧分量，记录摘要来源、误差界、尾部频率上界、尾部工作量上界和候选容量是否充分，不要求扫描或物化完整键域。

## 3. 双侧频率确认

机制2把单侧候选合并为双侧候选集合，并补全每个候选键在两侧关系中的频率证据。稳定贡献来自已验证历史统计、可查询索引或带误差界的摘要；残余贡献来自当前轮统计。对候选键 $k$，当前轮频率区间为

$$
L_X(k)=L_X^{stable}(k)+L_X^{residual}(k),\qquad
U_X(k)=U_X^{stable}(k)+U_X^{residual}(k).
$$

关系侧热点判定采用统一规则：$L_X(k)\ge\theta_X$ 时判为热点，$U_X(k)<\theta_X$ 时判为非热点，$L_X(k)<\theta_X\le U_X(k)$ 时判为待确认边界键。只在一侧出现的键仍属于连接键域；只有另一侧完整统计或可查询索引确认该键不存在时，另一侧频率才为 0。若另一侧摘要未报告该键，或局部节点尚未观察到该键，频率仍可能未知，需要继续查询、保留区间或返回识别不充分。

## 4. 状态组合与键级工作量

双侧频率确认后，机制2根据 $H_R$ 和 $H_S$ 生成状态组合 $z(k)\in\{HH,HC,CH,CC\}$。状态组合只由两侧热点集合成员关系决定，不能由 $W(k)$ 或策略代价反向修改。

逻辑匹配规模为 $J(k)=f_R(k)f_S(k)$。键级内在连接工作量可写为

$$
W(k)=
c_{\mathrm{in}}\bigl(f_R(k)b_R+f_S(k)b_S\bigr)
+c_{\mathrm{probe}}G\bigl(f_R(k),f_S(k)\bigr)
+c_{\mathrm{out}}J(k)b_{\mathrm{out}} .
$$

其中，$b_R$、$b_S$ 和 $b_{\mathrm{out}}$ 表示输入和输出记录宽度，$G(\cdot)$ 表示本地构建、探测或枚举匹配对的处理项。$W(k)$ 描述键自身诱导的内在工作，不包含复制、远端访问、具体放置和运行时接管等策略相关代价。若频率或系数存在不确定性，机制2可以返回 $W(k)$ 的区间 $[L_W(k),U_W(k)]$ 或置信信息。

## 5. 识别算法与输出接口

`BuildBoundaryCandidates` 会在 `req.Budget` 允许范围内逐步扩大候选保留规模，并同步更新双侧尾部覆盖证书；只有预算耗尽后证书仍不成立，算法才返回候选覆盖不充分。

```text
算法2：边界保留驱动的双侧负载识别
输入：RecognitionRequest req
输出：LoadProfile_t 或识别不充分

1. (C_R,C_S,Cert_R,Cert_S) ← BuildBoundaryCandidates(req)
2. C ← C_R ∪ C_S
3. if not ValidateCoverageCertificate(Cert_R,Cert_S,req.Budget) then
4.     return Insufficient("candidate coverage")
5. end if
6. I ← CompleteTwoSideIntervals(C, req)
7. B ← FindBoundaryKeys(I, req.Budget)
8. while B ≠ ∅ and WithinLookupBudget(B, req) do
9.     I ← RefineBoundaryIntervals(I, B, req)
10.    B ← FindBoundaryKeys(I, req.Budget)
11. end while
12. if HasUnresolvedHotBoundary(I) then
13.    return Insufficient("hot boundary")
14. end if
15. (H_R,H_S,z) ← BuildHotStates(I)
16. W_profile ← EstimateIntrinsicWork(I, req.Budget)
17. if WorkConfidenceInsufficient(W_profile, req.Budget)
18.    and not AllowConservativeInterval(req.Budget) then
19.    return Insufficient("work confidence")
20. end if
21. return BuildLoadProfile(H_R,H_S,z,W_profile,RecognitionSummary)
```

输出接口为

$$
LoadProfile_t=\langle H_R,H_S,z(k),\mathcal W(k),RecognitionSummary\rangle .
$$

其中，$\mathcal W(k)=\widehat W(k)$ 表示点估计满足精度要求，$\mathcal W(k)=[L_W(k),U_W(k)]$ 表示采用保守区间规划。`RecognitionSummary` 记录候选覆盖证书、尾部频率上界、尾部工作量上界、摘要来源、误差界、候选容量是否充分、频率区间、边界确认、工作量置信和回退原因。`LoadProfile_t` 不要求返回完整键域上的逐键结构：逻辑上，$H_R/H_S$ 是当前轮完整热点集合；物化上，$z(k)$、频率区间和 $\mathcal W(k)$ 只需要对候选集合 $C$ 中显式返回的键给出。未候选键由覆盖证书证明为 `CC` 且工作量上界低于 $\tau_W$。机制1仍从 $C$ 中进一步选择 $K_{explicit}$，二者不能等同。

## 6. 正确性、开销与识别边界

热点状态正确性来自候选覆盖证书和双侧区间判定。若 `Cert_R/Cert_S` 证明 $\overline U_R^{tail}<\theta_R$ 且 $\overline U_S^{tail}<\theta_S$，未候选键不会进入 $H_R/H_S$；若所有候选键的频率区间均位于热点阈值同一侧，则返回的 $H_R$、$H_S$ 和 $z(k)$ 与当前轮阈值热点语义一致。若边界键无法在预算内确认，机制2必须返回识别不充分。

工作量估计有效性与热点状态正确性分开。$W(k)$ 或 $[L_W(k),U_W(k)]$ 是由当前轮频率区间、记录宽度和成本系数诱导的键级内在工作量估计，不等同于真实执行时间。覆盖证书中的 $\overline U_W^{tail}<\tau_W$ 保证未候选 `CC` 键不会超过机制1要求显式返回的工作量界限。若候选键的工作量区间过宽，且超出 `RecognitionRequest.Budget` 给出的规划精度要求，机制2在允许保守区间规划时返回区间和标记；在不允许保守规划时返回识别不充分。

令 $n_\Delta$ 为残余输入记录数，$c=|C|$ 为双侧候选数，$b$ 为需要精确回查的边界键数。机制2的同步开销主要来自残余范围统计、跨节点候选合并、覆盖证书构造、$c$ 个候选键的双侧稳定贡献查询，以及 $b$ 个边界键的精确回查。该开销随残余输入、候选数、边界键数和证书构造成本增长，而不是随完整键域逐键枚举增长；当这些量接近完整输入规模，或稳定统计不可查时，机制2应退化为更完整的双侧统计确认。
