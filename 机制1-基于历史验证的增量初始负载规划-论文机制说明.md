# 机制1：基于历史验证的增量规划

## 1. 问题与核心思想

基于历史验证的增量规划面向重复或相似连接查询中的执行前控制开销。相邻查询可能共享稳定分片、相似热点状态和相似策略选择，但输入范围、资源上下文和边界候选仍可能变化。因此，该机制不能直接复用上一轮物理任务，也不能每轮都从零重建计划。

机制1的核心对象是当前轮完整初始负载计划。它验证已提交历史规划状态，区分稳定策略覆盖部分、待更新部分和需要重建的部分；当需要当前轮负载事实时，通过 `RecognitionRequest` 获取 `LoadProfile_t`；随后增量更新受影响规划决策，并合成包含路由方式、拆分参数、任务边界和初始归属的完整初始负载计划。

## 2. 可验证历史规划状态

历史状态保存的是控制面证据和策略模板，而不是连接结果、上一轮物理任务或固定节点归属。一个已提交历史版本可抽象为

$
HV^{(v)}=\langle VersionMeta^{(v)},ShardState^{(v)},QueryControl^{(v)}\rangle .
$

`VersionMeta` 用于确认查询结构、源快照和分片版本；`ShardState` 保存可查询的分片级候选摘要、精确计数或带误差界的统计；`QueryControl` 保存热点证据、状态组合、键级工作量估计、策略模板、参数规则、执行反馈和置信度。查询 $q_t$ 读取的是某个已提交历史版本 $HV^{(v_t)}$，正在构建或发布失败的版本不参与当前轮验证。

机制1只在输入分片具有可验证版本、稳定统计贡献可查询、历史策略仅作为模板且存在完整回退路径时使用历史状态。否则，历史状态被视为低置信或不可用。

## 3. 计划更新模式

机制1将历史验证结果转化为三种计划更新模式。

`FULL_REUSE` 表示当前输入和统计语义被历史状态完整覆盖，且不存在未解决边界键。机制1不发出识别请求，而是使用被当前输入分片、查询语义和历史版本完整覆盖并验证通过的负载画像，在当前资源上下文下重新实例化稳定策略覆盖部分。

`PARTIAL_REUSE` 表示部分分片和策略模板可复用，但仍存在变化分片、未知范围或待确认键。机制1保留稳定策略覆盖部分，并把残余输入范围和待确认关系侧键组织为 `RecognitionRequest`。返回 `LoadProfile_t` 后，机制1只更新受影响规划决策。

`REBUILD` 表示历史状态不可用、统计语义不兼容、稳定贡献不可查或部分复用不划算。机制1发出覆盖完整输入的识别请求，并从当前轮输入重建完整初始负载计划。

关系侧证据集合满足

$
K_{reuse,X}\cap K_{check,X}=\emptyset,\qquad X\in\{R,S\}.
$

其中，$K_{reuse,X}$ 表示关系 $X$ 侧可复用统计证据，$K_{check,X}$ 表示仍需当前轮确认的关系侧键，$\Omega_{residual}$ 表示需要重新识别的输入范围。

## 4. 接口与增量规划算法

识别请求按语义分组为

$
RecognitionRequest=\langle Scope,Evidence,Budget,StableStateRef,HistoryValidationSummary\rangle .
$

历史策略模板保留在机制1内部，不交给双侧负载识别过程解释。当前轮负载描述由 `LoadProfile_t` 返回：

$
LoadProfile_t=\langle H_R,H_S,z(k),W(k),RecognitionSummary\rangle .
$

`LoadProfile_t` 提供当前轮热点集合、状态组合、键级内在连接工作量、边界确认和识别置信信息。稳定贡献和残余贡献如何合成当前轮频率事实由机制2完成；机制1只消费返回的负载事实和回退信号。

```text
算法1：基于历史验证的增量规划
输入：查询 q_t，当前输入 D_R^t,D_S^t，当前资源上下文 Ctx_t
输出：完整初始负载计划 P_t

1. HV ← LatestCommittedHistory(q_t)
2. S ← ValidateHistory(HV, q_t, D_R^t, D_S^t, Ctx_t)
3. mode ← SelectUpdateMode(S)
4. if mode = FULL_REUSE then
5.     Phi_t ← ReuseLoadProfile(S)
6. else
7.     req ← BuildRecognitionRequest(mode, S, D_R^t, D_S^t)
8.     Phi_t ← RecognizeDualSideLoad(req)
9.     while IsInsufficient(Phi_t) and mode != REBUILD do
10.        (mode, req) ← UpgradeRecognitionScope(mode, req)
11.        Phi_t ← RecognizeDualSideLoad(req)
12.    end while
13.    if IsInsufficient(Phi_t) then
14.        return ReportRecognitionFailure()
15.    end if
16. end if
17. retry ← true
18. while retry do
19.     retry ← false
20.     if mode = REBUILD then Tpl ← ∅ else Tpl ← ValidatedTemplates(S)
21.     K_explicit ← SelectExplicitPlanKeys(Phi_t,Ctx_t)
22.     (L_pred^(0),B_remain^(0),Q_special) ← InitPlanningState(Ctx_t)
23.     r_total ← 0
24.     for (j,k) in Enumerate(SortDesc(K_explicit,W(k))) do
25.         A^(j)(k) ← FeasibleStrategies(k,Phi_t,Tpl,L_pred^(j),B_remain^(j))
26.         while A^(j)(k)=∅ and r_total < R_max do
27.             (Q_special,L_pred^(j),B_remain^(j)) ← RepairOrReplan(k,Q_special,L_pred^(j),B_remain^(j),Ctx_t)
28.             A^(j)(k) ← FeasibleStrategies(k,Phi_t,Tpl,L_pred^(j),B_remain^(j))
29.             r_total ← r_total + 1
30.         end while
31.         if A^(j)(k)=∅ and mode != REBUILD then
32.             mode ← REBUILD
33.             Phi_t ← RecognizeDualSideLoad(BuildRecognitionRequest(REBUILD,S,D_R^t,D_S^t))
34.             if IsInsufficient(Phi_t) then return ReportRecognitionFailure()
35.             retry ← true
36.             break
37.         end if
38.         if A^(j)(k)=∅ then return ReportPlanningFailure(k)
39.         a_k^* ← SelectMinCost(A^(j)(k),Cost,L_pred^(j),B_remain^(j))
40.         Q_k ← GenerateTasks(k,a_k^*,Phi_t)
41.         (Q_k,L_pred^(j+1),B_remain^(j+1)) ← PlaceAndUpdate(Q_k,L_pred^(j),B_remain^(j),Ctx_t)
42.         Q_special ← Q_special ∪ Q_k
43.     end for
44. end while
45. DefaultRouteRule ← BuildDefaultHashRule(K_explicit,Ctx_t)
46. return AssemblePlan(Q_special,DefaultRouteRule,Ctx_t)
```

其中，`Phi_t` 表示当前轮负载描述，即 `LoadProfile_t` 或 `FULL_REUSE` 下经验证可复用的历史负载画像。`UpgradeRecognitionScope` 只能单调扩大识别范围或把模式提升为 `REBUILD`，因此不会在不同请求之间循环。进入 `REBUILD` 后，机制1清空历史策略模板，重新获取覆盖完整输入的当前轮负载画像，并从空的初始计划状态重新合成计划。算法通过 `SelectExplicitPlanKeys(Phi_t,Ctx_t)` 只物化需要非默认规划的键；逻辑补集 `K_default=K_all\setminus K_explicit` 由默认哈希规则覆盖，不要求系统枚举完整键域。`RepairOrReplan` 是有界保守修复过程，整个规划过程共享 $R_{\max}$ 次修复预算；每次修复采用预定义的资源节约型策略切换、复制或拆分预算收紧，或任务重放置。若尚未进入 `REBUILD`，规划失败触发完整识别与完整重建；若 `REBUILD` 下仍失败，则报告启发式规划失败，并由独立容量检查区分规划失败与真实容量不足。

## 5. 策略选择与任务放置

完整初始负载计划由机制1合成。对进入显式规划视图的键，机制1按 $W(k)$ 从大到小处理，并基于当前预测负载和剩余资源构造可行策略集合。显式规划键由机制1根据状态组合、$W(k)$、策略切换条件和资源上下文从 `LoadProfile_t` 中选出，而不是等同于负载画像包含的全部键。设 $L_{pred}^{(j)}$ 表示处理第 $j$ 个显式规划键之前已经放置任务形成的节点预测负载，$\mathbf B_{remain}^{(j)}=\langle B_{\mathrm{mem}}^{(j)},B_{\mathrm{rep}}^{(j)},B_{\mathrm{net}}^{(j)}\rangle$ 表示剩余内存、复制和通信预算向量，则策略选择写为

$
a_k^*=\arg\min_{a\in\mathcal A^{(j)}(k)} Cost(k,a\mid L_{pred}^{(j)},\mathbf B_{remain}^{(j)}).
$

每选择一个策略，机制1立即生成相应任务、完成初始放置，并更新为 $L_{pred}^{(j+1)}$ 与 $\mathbf B_{remain}^{(j+1)}$；算法中的 `B_remain^(j)` 即表示该多维资源状态。可行策略不仅要满足状态组合和执行语义，还必须在当前资源状态下至少存在一种合法初始放置。历史模板不影响策略可行性，只影响检查顺序、参数初值或代价接近时的稳定性偏好。负载证据不足时，机制1扩大识别范围或进入 `REBUILD`；若负载证据充分但当前启发式状态下没有可行策略，机制1先尝试有界保守修复或重新放置已规划任务。若修复预算耗尽且当前尚未处于 `REBUILD`，机制1转入完整识别与完整重建；若在 `REBUILD` 下仍失败，则报告启发式规划失败。只有独立容量条件显示当前输入和资源无法支持任何已定义执行路径时，系统才报告容量边界。默认哈希路径是普通键或轻度倾斜键的语义正确默认路径，不作为所有高工作量键的容量后备方案。

## 6. 异步历史维护与正确性边界

查询 $q_t$ 返回后，机制1异步维护历史状态。$v_t$ 表示查询 $q_t$ 实际读取的历史版本号，而不是查询轮次。后台任务可能生成候选历史版本 $HV^{(v')}$，其中 $v'>v_t$。候选版本完整写入并通过发布条件后，系统才更新最新已提交版本指针，使其对后续查询可见。

正确性来自任务责任域而不是历史判断本身。对键 $k$，逻辑匹配责任域为 $\mathcal D_k=R_k\times S_k$。机制1生成的计划必须把 $\mathcal D_k$ 划分为互斥且覆盖完整的最终任务责任域：

$
\mathcal D_k=\biguplus_j \mathcal T_{k,j},\qquad
\mathcal T_{k,i}\cap \mathcal T_{k,j}=\emptyset,\quad i\neq j .
$

哈希路由、轻侧复制、键内拆分和局部连接均按构造产生责任域的不交覆盖。显式规划键集合与默认键集合互斥，且二者并集覆盖当前轮完整连接键域，因此显式任务和默认哈希规则不会重复覆盖同一逻辑匹配责任域。对轻侧复制，若 $R_k$ 为重侧，则机制1按记录实例区间构造 $R_k=\biguplus_j R_k^{(j)}$，并令 $\mathcal T_{k,j}=R_k^{(j)}\times S_k$；$S_k$ 可以被复制到多个任务作为匹配输入，但每个重侧记录实例只属于一个 $R_k^{(j)}$，因此每个匹配对只在一个任务中输出。输入记录可以复制读取，但每个逻辑匹配对的输出责任只能属于一个最终任务。历史状态若判断失准，可能选择较差但合法的策略，不会删除或重复分配责任域；不确定证据、统计缺失或候选不充分时，机制1回退到更完整的识别和重建路径。

令 $m$ 为当前输入分片数，$h=|K_{explicit}|$ 为显式规划键数量，$A$ 为每个键最多考虑的候选策略数量，$N$ 为执行节点数，$Q$ 为生成的逻辑任务数量，$Q_\rho$ 为单次修复重新评估或重新放置的任务数。若任务放置使用最小负载堆或等价节点选择结构，机制1本地计划合成开销可写为 $O(m+h\log h+hA+Q\log N+R_{\max}(A+Q_\rho\log N))$。双侧负载识别成本由机制2单独分析。
