# 机制1：基于历史验证的增量初始负载规划

## 1. 机制定位

基于历史验证的增量初始负载规划面向重复或相似连接查询中的执行前控制开销。已有规划-执行协同方法通常需要在每轮查询开始时重新完成热点识别、状态分类、路由与拆分参数生成。若相邻轮次的输入结构、稳定分片和热点模式保持相似，这些控制决策存在可复用空间。

本机制不缓存连接结果，不直接复用上一轮物理任务编号、数据区间、offset 或节点放置结果。它复用的是经过验证的历史控制信息，包括稳定分片统计、候选摘要、两侧热点状态组合、历史策略模板、参数计算规则和执行反馈。当前轮只在历史信息不可确认、输入发生变化或边界候选不稳定时进入补充识别或完整重建。

因此，本机制的核心作用是为第 6 章提供历史计划种子和残余识别范围，而不是独立生成完整最终初始计划。完整初始负载计划由第 6 章在完成当前轮双侧负载识别后合成。

## 2. 版本化分片与增量统计

对每个关系 $X\in\{R,S\}$，当前轮输入表示为版本化不可变分片集合 $D_X^t$。系统根据结构签名、分片 ID、版本号、快照号和查询过滤语义，将输入划分为稳定分片和残余分片：

$$
D_X^t=U_X^t\cup \Delta_X^t,\qquad U_X^t\cap \Delta_X^t=\emptyset.
$$

其中，$U_X^t$ 表示分片 ID 与版本均未变化、统计可复用的稳定范围；$\Delta_X^t$ 表示新增、替换、删除后重入或无法确认稳定的残余范围。更新被建模为旧版本退出当前快照、新版本作为残余分片进入，避免维护复杂的负频率增量。

每个稳定分片保存控制面统计状态：

$$
State(p)=\langle Meta_p,\ CandidateSummary_p,\ ExactMap_p,\ Sketch_p\rangle.
$$

`Meta_p` 记录分片版本、行数、统计版本和查询语义；`CandidateSummary_p` 保存可合并的重频候选与未保留键频率上界；`ExactMap_p` 保存摘要中重要候选键的精确计数；`Sketch_p` 支持对残余范围中新出现候选键查询其稳定分片贡献。

对候选键 $k$，当前规划频率由稳定贡献和残余贡献合成：

$$
f_X^{plan}(k)=f_X^{stable}(k)+f_X^{residual}(k).
$$

其中 $f_X^{stable}(k)$ 必须来自未变化分片的精确统计、可查询索引或带误差界的摘要。不能因为历史状态未保存某个键，就把该键在稳定分片中的贡献视为 0。

历史状态本身采用不可变版本发布。论文层面可以把一个版本抽象为 `HV_t=<VersionMeta_t,ShardState_t,QueryControl_t>`：版本元数据记录结构签名、源快照和分片版本向量，分片状态保存可查询统计，查询级控制状态保存热点证据、策略模板、参数规则、反馈和置信度。正在构建的版本对查询不可见，只有完整写入并原子发布后的 committed version 才能作为下一轮验证输入。

## 3. 历史控制状态与复用边界

历史状态保存的是可验证的控制信息，而不是上一轮完整任务计划。可保存的信息包括：

- 两侧热点画像、两侧热点状态组合和历史工作量估计。
- 路由策略类型、热点拆分方式、参数计算规则和放置偏好。
- 分片级候选摘要、候选键精确计数、sketch 和统计版本。
- 执行反馈，包括输出规模、长尾程度、再平衡次数、实际完成时间和置信度。

上一轮物理任务、数据区间、offset、具体任务编号、固定节点编号、拆分数量和任务放置可以作为执行反馈保存，但不能直接作为当前轮任务计划复用。当前轮仍需根据输入规模、资源上下文和第 6 章识别结果重新实例化参数。

查询签名分为两层。结构签名用于检索历史状态，包含关系标识、连接键、连接类型和规范化过滤模板。动态上下文用于决定复用范围和参数是否重算，包括具体时间窗口、分片版本向量、节点数、并行度、内存和拓扑信息。

## 4. 复用范围判定

历史验证输出三类对象：

- `K_reuse`：经验证可复用历史策略模板的稳定键域。
- `K_check`：已暴露但状态靠近边界、需要当前轮确认的显式键集合。
- `Omega_residual`：需要重新执行候选发现和统计确认的残余输入范围。

对应三种复用状态：

- `FULL_REUSE`：结构签名一致、分片版本完全稳定、历史统计完整，且不存在未解决边界候选。此时 `Omega_residual=\varnothing`，第 6 章可跳过残余候选发现，只执行必要的参数实例化和计划合成。
- `PARTIAL_REUSE`：部分历史键或分片稳定，但仍存在变化分片、边界键、新候选风险或资源变化。系统复用 `K_reuse`，并把 `K_check` 与 `Omega_residual` 交给第 6 章补充识别。
- `REBUILD`：不存在匹配历史状态、查询签名发生实质变化、分片稳定性无法确认、摘要缺失或回查成本接近完整重建。系统执行完整当前轮双侧负载识别。

轻量验证不能证明当前轮不存在所有新热点。它只能在给定验证预算和误差范围内判断新热点风险是否足以改变当前初始计划；证据不足时进入 `PARTIAL_REUSE` 或 `REBUILD`。

## 5. 与第 6 章的接口

本机制向第 6 章输出历史计划种子、`K_reuse`、`K_check`、`Omega_residual`、验证摘要和当前资源上下文。这里的计划种子是历史策略模板及其有效范围，不是已经实例化的任务计划。

`K_reuse` 与 `K_check` 只划分历史相关的显式键域，并满足 `K_reuse ∩ K_check = ∅`。它们不等于当前轮完整特殊规划对象集合；尚未显式暴露的新候选需要由第 6 章在 `Omega_residual` 覆盖的输入范围中发现。第 6 章随后合并历史复用键、待确认键、稳定摘要候选和残余候选，估计内在连接工作量和策略代价，并生成当前轮完整初始负载计划。普通键不要求在第 5 章显式枚举，仍保留默认哈希路径。

## 6. 正确性、后台刷新与适用边界

历史信息只影响初始路由、热点拆分参数和初始任务归属，不改变连接谓词，也不复用连接结果。正确性边界分为三层：首先，版本比较必须把当前输入划分为互斥且完整的稳定分片与残余分片；其次，`K_reuse` 与 `K_check` 作为历史相关显式键域必须互斥，而 `Omega_residual` 负责发现尚未显式暴露的新候选；最后，最终路由和任务划分仍必须满足记录覆盖、任务互斥、默认路径完备和唯一输出约束。因此，历史误判可能造成热点分类不准、初始计划退化或执行长尾加重，但不会改变标准等值连接的输出多重集合。

历史状态刷新不能由“继续复用历史计划”自我确认，必须来自当前轮真实计数、当前轮识别结果、实际执行时间、输出规模、长尾和再平衡反馈。该刷新发生在连接完成后的后台维护任务中，用于生成后续查询可验证读取的控制状态，不属于当前查询的关键路径，也不计入当前查询的 `end-to-end` 或 `modeled_end_to_end_time`。后台刷新不重新扫描完整原始输入；新状态由稳定分片旧状态、当前轮残余扫描已经产生的统计，以及执行反馈合并、压缩、持久化后发布。

后台刷新必须以版本化方式发布。更新任务先构建临时历史状态，待分片摘要、策略模板、执行反馈和置信度均写入完成后，再原子发布为 committed history version。下一轮查询只能读取已经提交且版本完整的历史状态；若后台刷新失败、超时、尚未完成或与下一轮并发，下一轮应读取上一版 committed state，或把该历史状态视为缺失、低置信度并回退到 `PARTIAL_REUSE` 或 `REBUILD`。随机查询、热点快速漂移、写入更新频繁、无法确认分片版本或边界回查成本过高时，本机制应回退到 `REBUILD`。
