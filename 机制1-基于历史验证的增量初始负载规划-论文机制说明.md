# 机制1：基于历史验证的增量初始负载规划

## 1. 机制定位

本机制服务于重复或相似连接查询中的执行前控制路径。它不是复用上一轮连接结果，也不是直接套用上一轮完整任务计划，而是复用经过验证的历史控制信息，为当前轮提供计划种子，并将变化、不确定或缺失的部分交给第 6 章继续识别。

机制1由两个内部组成构成：

- 版本化分片增量统计：判断哪些输入分片未变化，哪些残余范围需要重新扫描。
- 历史统计状态与复用判定：定义历史中保存什么，以及如何进入 `FULL_REUSE/PARTIAL_REUSE/REBUILD`。

## 2. 输入与状态

当前轮输入按关系 $X\in\{R,S\}$ 表示为版本化不可变分片集合 $D_X^t$。通过结构签名、分片 ID 和版本向量比较，系统将其划分为稳定分片 $U_X^t$ 和残余分片 $\Delta_X^t$，满足：

$$
D_X^t=U_X^t\cup \Delta_X^t,\qquad U_X^t\cap \Delta_X^t=\emptyset.
$$

每个分片保存控制面统计状态：

$$
State(p)=\langle Meta_p,\ CandidateSummary_p,\ ExactMap_p,\ Sketch_p\rangle.
$$

其中，`Meta` 描述分片版本和统计有效性，`CandidateSummary` 保存可合并候选摘要及未保留键上界，`ExactMap` 保存重要候选键精确频率，`Sketch` 支持残余新候选查询其稳定分片贡献。

## 3. 复用判定

结构签名决定能否检索历史状态，动态上下文决定复用范围和当前轮参数是否需要重算。系统输出：

- `K_reuse`：可复用历史策略模板的稳定键域。
- `K_check`：已暴露但仍需当前轮确认的显式键集合。
- `Omega_residual`：需要重新候选发现和统计确认的残余输入范围。
- 历史计划种子：策略模板及其有效范围，不包含当前轮任务编号、offset、节点归属或连接结果。

复用状态含义如下：

- `FULL_REUSE`：结构签名匹配、分片版本未变化、历史统计完整，且没有未解决边界键。
- `PARTIAL_REUSE`：部分分片或键域稳定，残余分片、边界键和新候选风险进入第 6 章。
- `REBUILD`：历史状态无法提供可靠计划种子，当前轮执行完整双侧负载识别。

## 4. 与第 6 章接口

机制1不生成完整最终初始计划。它向第 6 章传递历史计划种子、`K_reuse`、`K_check`、`Omega_residual`、验证摘要和资源上下文。第 6 章负责在残余范围内完成双侧负载识别，并将当前轮识别结果与历史稳定部分合并，生成完整初始负载计划。

对候选键 $k$，当前轮规划频率由稳定贡献和残余贡献合成：

$$
f_X^{plan}(k)=f_X^{stable}(k)+f_X^{residual}(k).
$$

其中 $f_X^{stable}(k)$ 必须来自未变化分片的精确统计、可查询索引或带误差界的摘要。不能因为历史状态没有保存某个键，就把它在稳定分片中的贡献视为 0。

## 5. 正确性与边界

历史状态只影响初始负载规划质量，不改变连接谓词，不复用连接输出。历史误判可能导致热点分类不准、初始路由不理想或长尾加重，但只要最终路由和任务划分满足记录覆盖、任务互斥、默认路径完备和唯一输出约束，连接结果仍与标准等值连接等价。

该机制只在历史控制信息稳定且验证成本低于完整识别收益时启用。随机查询、热点快速漂移、分片版本无法确认、边界回查成本过高或残余范围接近完整输入时，应退化到 `REBUILD`。

