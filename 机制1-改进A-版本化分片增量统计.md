# 机制1改进A：版本化分片增量统计

## 1. 问题定位

基于历史验证的增量初始负载规划希望减少重复或相似查询中的热点识别和统计开销。关键问题不是“能否少统计一些键”，而是“能否少扫描一部分可证明没有变化的数据范围”。如果当前轮只知道查询相似，却不知道哪些输入数据没有变化，那么历史频率不能安全复用。

本改进将残余范围限定为版本化输入分片。系统复用未变化分片上的历史统计，只重新扫描新增、替换或无法确认稳定的分片。这样，当前轮频率由稳定分片贡献和残余分片贡献合成，而不是简单复用上一轮全局频率。

## 2. 成立前提

对每个关系 $X\in\{R,S\}$，当前轮输入表示为一组不可变版本化分片：

$$
D_X^t=\{p_1,p_2,\ldots,p_m\}.
$$

每个分片至少包含 `relation_id`、`partition_id`、`version`、`row_count`、`query_signature` 和统计状态指针。通过比较当前轮和历史轮的分片清单，系统将输入划分为：

$$
D_X^t=U_X^t\cup \Delta_X^t,\qquad U_X^t\cap \Delta_X^t=\emptyset.
$$

其中，$U_X^t$ 表示分片标识和版本均未变化、可安全复用统计的稳定分片；$\Delta_X^t$ 表示新增、替换、删除后新快照引入或无法确认稳定的残余分片。更新不作为原地修改处理，而是旧版本分片退出当前快照，新版本分片作为残余分片进入当前轮。

## 3. 核心流程

当前轮先验证查询结构签名。签名至少包含输入关系、连接键、规范化过滤谓词、统计版本和快照语义。投影字段变化通常不影响键频率，可以不进入签名；过滤谓词变化会改变参与连接的数据，原则上必须进入签名。签名不一致时，系统直接进入 `REBUILD`，除非能够证明谓词变化只影响明确的分片范围。

签名匹配后，系统比较分片清单。版本未变化的分片进入 $U_X^t$，新增或版本改变的分片进入 $\Delta_X^t$，旧分片消失时从当前统计合成中移除，元数据缺失或无法验证的分片进入残余范围或触发 `REBUILD`。这里的 $\Omega_{residual}$ 对应 $\Delta_R^t$ 与 $\Delta_S^t$ 代表的输入范围，而不是一组键。

随后，各节点只扫描本地持有的残余分片，在扫描中完成残余候选发现、候选精确计数和残余频率摘要生成，得到 $C_X^{residual}$ 与 $f_X^{residual}(k)$。残余扫描仍需要遍历残余范围中的连接键，不能只统计旧候选键，否则无法发现新热点。

## 4. 与第5/6章接口

第 5 章输出的 `FULL_REUSE/PARTIAL_REUSE/REBUILD` 可由分片变化状态辅助决定。`FULL_REUSE` 要求查询签名一致、当前分片清单和版本完全一致、历史统计状态完整，并且历史边界键没有未解决的不确定性。`PARTIAL_REUSE` 要求存在可验证稳定分片，同时存在新增或替换分片，且稳定分片统计可用于合成当前轮频率。`REBUILD` 则用于签名变化、分片版本不可确认、统计状态缺失、残余范围过大或边界回查成本过高的情况。

第 6 章接收的 $\Omega_{residual}$ 应解释为残余输入范围。双侧负载识别只在该范围上执行候选发现和残余统计，然后将残余贡献与稳定分片贡献合成当前轮规划频率。形式上，对候选键 $k$：

$$
f_X^{plan}(k)=f_X^{stable}(k)+f_X^{residual}(k).
$$

其中 $f_X^{stable}(k)$ 必须来自未变化分片的精确历史统计、可查询索引或带误差界的摘要。不能因为历史状态没有保存某个键，就把它在稳定分片中的贡献当成 0。

## 5. 正确性边界

版本化分片增量统计只影响热点状态判断和初始负载规划质量，不改变等值连接谓词。即使频率估计存在误差，只要最终计划仍保留默认完整执行路径，并满足记录覆盖、任务互斥和唯一输出约束，连接结果仍应与标准等值连接等价。

真正会破坏统计合成的是数据范围边界错误。如果稳定分片和残余分片重叠，频率会重复计数；如果有当前输入分片既不在稳定集合也不在残余集合，频率会漏计。因此，本机制的正确性前提是版本化分片清单能够互斥且完整地覆盖当前输入。

## 6. 开销与退化路径

设当前输入总量为 $D$，残余分片比例为 $\rho$。完整重建需要扫描约 $D$ 的输入；增量统计主要扫描 $\rho D$，再加上元数据比较、候选合并、稳定贡献查询和边界回查成本。只有当：

$$
C_{partial}<C_{rebuild}
$$

时才应进入 `PARTIAL_REUSE`。

实践中可用三个指标触发退化：残余分片比例过高、候选集合规模过大、稳定分片精确回查预计需要读取大部分稳定数据。一旦出现这些情况，系统应放弃增量统计，回到完整双侧负载识别路径。

## 7. 是否建议并入正文

建议作为第 5 章与第 6 章之间的增强解释并入，但不应写成任意输入下的通用增量统计。正文中可以用“若输入能够由时间窗口、版本化分片或快照元数据划分为稳定部分与变化部分，则系统可复用稳定部分统计，仅对变化部分执行候选发现与双侧统计；否则退化为当前轮完整识别”来收束。

