# 第三机制：热点感知预分布机制

## 1. 机制定位

热点感知预分布面向重复或相似连接查询中的稳定热点模式。本文前两个阶段分别解决当前轮输入中的热点语义识别和执行期剩余长尾再平衡，但在周期性批处理、相邻时间窗口查询或参数相近的 MapReduce/Spark 连接任务中，热点键集合、连接语义和路由决策往往具有跨轮稳定性。如果每轮都从头执行完整热点识别、全局候选验证和路由构造，系统会在稳定工作负载上重复支付控制面开销。

该机制的目标不是缓存连接结果，也不是维护长期物理数据布局，而是把上一轮已经验证过的热点语义转化为当前轮的初始控制决策。它只在轻量验证确认历史状态仍然可靠时复用控制信息；当查询签名不匹配、热点分布漂移或新热点风险过高时，系统回退到完整热点识别路径。

在论文叙事中，第三机制应写作 **热点感知预分布机制**。`Hot-key Reuse Plan Cache` 是当前代码实现手段，用于保存和验证跨轮控制信息，不作为论文机制名称。

## 2. 目标问题

给定第 $t$ 轮和第 $t+1$ 轮重复或相似的双表等值连接，热点感知预分布要回答的问题是：当热点模式在两个查询轮次之间保持稳定时，系统能否复用上一轮已经验证的热点分类和路由计划，为当前轮生成可靠的初始任务划分，从而减少重复热点识别和路由构造开销。

这个问题区别于普通数据预分区。普通预分区关注数据如何提前放置或减少 shuffle；热点感知预分布关注的是连接算子内部的控制状态如何跨轮复用。它也区别于执行期再平衡。预分布发生在当前轮 join 执行之前，用于生成初始计划；QueueState/OSP 发生在执行过程中，用于处理静态计划后仍然出现的剩余长尾。

## 3. 设计原则

热点感知预分布遵循三个原则。第一，它只复用控制信息，不复用连接结果。历史状态可以包含热点集合、HH/HC/CH/CC 连接语义、频率摘要、路由规则和初始任务放置建议，但最终输出仍由 $R.k=S.k$ 决定。第二，它必须经过轻量验证。历史热点不能直接被视为当前热点，验证必须检查查询签名、左右侧热点稳定性、连接语义一致性和新热点风险。第三，历史计划只能作为当前轮计划生成的输入。上一轮的具体数据区间、offset 或任务编号不能直接照搬，当前轮实际任务区间仍根据当前输入重新生成。

形式化地，第 $t$ 轮结束后保存的预分布状态可写为

$$
PreDist_t=\langle Sig^t,H_R^t,H_S^t,Q^t,F^t,Plan^t,Place^t,Version^t\rangle.
$$

其中，$Sig^t$ 表示查询签名，包括输入关系、连接键、过滤条件、时间窗口粒度和数据版本等信息。$H_R^t$ 与 $H_S^t$ 是左右关系中的热点集合，$Q^t$ 是由二者导出的 HH/HC/CH/CC 连接语义，$F^t$ 是热点频率摘要，$Plan^t$ 表示面向热点键的路由规则和分裂参数，$Place^t$ 表示初始任务放置偏好。只有当前轮查询签名与历史状态匹配时，系统才进入轻量验证；否则直接进入 `MISS`。

执行环境变化也会限制复用范围。若执行节点数、并行度、RDMA 注册内存规模或拓扑信息发生变化，系统只允许复用热点语义和路由规则，不直接复用历史初始任务放置建议。必要时，系统应进入 `PARTIAL_HIT` 或 `MISS`。

## 4. 跨轮预分布流程

第 $t$ 轮连接按完整路径执行。系统先识别左右关系中的热点集合，导出 HH/HC/CH/CC 连接语义，构造热点相关路由计划，并在执行期使用 RDMA 运行时再平衡处理剩余长尾。执行结束后，系统将经过完整路径验证的控制信息写入预分布状态，作为下一轮连接的候选历史计划。

第 $t+1$ 轮连接开始时，系统先比较查询签名。如果签名不匹配，历史计划不能服务当前查询，系统直接回退完整热点识别。若签名匹配，系统读取当前输入的轻量摘要，并判断上一轮热点模式是否仍然适用。轻量摘要可以来自输入元数据、抽样、早期扫描窗口、低成本 sketch，或只针对历史热点键的计数探测。它不要求精确恢复当前轮完整热点集合，而是用于判断历史计划是否仍具有足够置信度。

如果历史热点在左右关系中仍然稳定，连接语义边界没有明显漂移，且非历史键残余负载和新热点候选风险都较低，系统使用历史计划生成初始任务划分。如果只有部分热点稳定，系统只复用高置信度热点，并对未确认部分执行局部补充识别或完整热点识别。如果验证失败，系统丢弃历史状态并执行完整热点识别。无论预分布是否命中，后续执行期仍保留 QueueState/OSP，用于处理静态规划后仍可能出现的尾部任务。

## 5. 轻量验证与状态转移

轻量验证的核心是判断上一轮控制状态是否仍能服务当前轮连接。验证必须保留左右关系的方向性：系统分别验证 $H_R^t$ 在 $R^{t+1}$ 中的稳定性，以及 $H_S^t$ 在 $S^{t+1}$ 中的稳定性，再检查二者导出的 HH/HC/CH/CC 连接语义是否保持一致。只验证 $H_R^t\cup H_S^t$ 的并集是不充分的，因为同一个键可能从左侧热点变为右侧热点，从而改变后续路由策略。

系统使用归一化频率而不是原始频率比较漂移：

$$
p_R^t(k)=\frac{f_R^t(k)}{|R^t|},\qquad
p_S^t(k)=\frac{f_S^t(k)}{|S^t|}.
$$

轻量摘要得到的是当前轮频率的估计值。对 $R$ 侧历史热点，漂移可写为

$$
Drift_R(k)=\frac{|\hat{p}_R^{t+1}(k)-p_R^t(k)|}{\max(p_R^t(k),\epsilon)}.
$$

$S$ 侧同理。当估计误差或候选不确定性无法支撑状态判断时，系统不能进入 `FULL_HIT`。频率稳定仍不足以保证预分布安全，因为连接代价还取决于两侧频率的组合效应。因此，系统还需要检查估计连接工作量是否稳定，例如比较 $f_R(k)f_S(k)$ 或其归一化形式的相对变化。

状态转移分为三类：

| 状态 | 条件 | 执行动作 |
| --- | --- | --- |
| `FULL_HIT` | 历史热点在左右关系中稳定、连接语义稳定，且没有高置信度新热点候选或过大的非历史键残余负载 | 复用热点集合、连接语义和路由规则，生成初始任务划分 |
| `PARTIAL_HIT` | 部分历史热点稳定，但边界键、未确认部分或可能的新热点候选仍存在不确定性 | 只复用已确认稳定的热点，对未确认部分执行局部补充识别或完整热点识别 |
| `MISS` | 查询签名、输入规模、热点保留率、归一化频率漂移或连接语义一致性不满足条件 | 执行完整 FastLock-Verify 与常规路由构造 |

在 `PARTIAL_HIT` 下，系统必须先将连接键划分为已验证稳定热点集合和未确认集合，两部分必须互斥。稳定热点使用历史路由规则，未确认集合进入局部补充识别或完整热点识别，最终再合并为当前轮完整路由计划。未被验证的键不得直接套用历史热点路由。

## 6. 正确性与开销边界

热点感知预分布的正确性来自四个约束。第一，它不改变连接谓词，也不缓存连接输出；最终输出仍由 $R.k=S.k$ 决定。第二，它只允许复用经过验证的控制信息；当轻量验证不能确认热点模式稳定时，系统必须回退完整热点识别。第三，预分布只改变任务如何划分和路由，不改变每个 key 的完备性与互斥性约束。对于任意满足 $r.k=s.k$ 的记录对，路由计划必须保证该记录对至少在一个执行位置相遇；若执行策略可能产生重复相遇，实现还必须避免重复输出。第四，历史状态需要刷新和过期。对于长期未经过完整热点识别刷新的状态，系统设置版本过期或置信度衰减机制；超过阈值后强制进入完整热点识别，避免慢速漂移累积造成误复用。

该机制的收益依赖于稳定热点模式。对于重复或相似连接查询，如果左右侧热点集合、连接语义和估计连接工作量跨轮保持稳定，预分布可以减少每轮从零开始构造控制计划的成本。其主要开销来自轻量验证，包括输入规模检查、侧向热点保留率检查、归一化频率漂移估计、连接工作量漂移估计、新热点风险检查和必要的抽样验证。只有当验证开销低于完整热点识别与路由构造成本，且热点模式具有足够稳定性时，该机制才应启用。

因此，热点感知预分布是一个有明确适用边界的阶段。它适用于重复或相似连接查询中的稳定热点模式；在随机查询、热点快速漂移或写入更新频繁的场景中，系统应返回 `MISS`，继续使用完整热点识别和 RDMA 运行时再平衡路径。

## 7. 与其他机制的关系

热点感知预分布位于本文三阶段路径的第一阶段。它利用上一轮验证过的控制信息，为当前轮提供初始任务划分。FastLock-Verify 位于第二阶段，在历史信息不存在或验证失败时重新建立当前轮的全局热点语义。QueueState/OSP 位于第三阶段，处理预分布和热点识别之后仍然出现的执行期剩余长尾。

这三个阶段不是互相替代的关系。预分布减少稳定重复查询中的控制面开销；热点识别保证当前轮热点语义可靠；RDMA 运行时再平衡处理静态规划无法完全预测的尾部任务。
