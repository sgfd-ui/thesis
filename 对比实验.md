# 对比实验协议：大数据主线与正式实验设计（Pilot 前冻结版）

## 0. 总体口径

本实验协议用于重新运行和组织第 8 章实验。正式主线从“小规模功能验证”调整为“大数据规模下的连接键倾斜等值连接负载均衡”。开发 smoke 固定为 `0.1x=8万条`，只检查流程、字段完整性、正确性检查和成本互斥计费，不进入正文主图和核心结论。正式输入规模从 `1x` 到 `128x` 指数扩展，其中 `1x=80` 万条，是正式规模曲线的最小点，必须重新按正式协议运行，不能复用开发 smoke 结果。

在当前实验设备允许范围内，输入规模目标最高扩展至 `128x`。`128x` 是当前设备的目标旗舰配置和预计实验上限；各主方法能否在该规模下稳定完成，以 `128x capacity probe` 和最终 baseline qualification 结果为准。该配置不是方法本身的可扩展性上限。

正式图表必须同时标注 scale factor、实际输入记录数和输入字节数：

| scale factor | 实际输入记录数 | 角色 |
| --- | ---: | --- |
| `1x` | 80 万条 | 正式最小规模点，不是 smoke |
| `2x` | 160 万条 | 正式规模曲线 |
| `4x` | 320 万条 | 正式规模曲线 |
| `8x` | 640 万条 | 正式规模曲线 |
| `16x` | 1280 万条 | 大规模起点 |
| `32x` | 2560 万条 | 大规模曲线 |
| `64x` | 5120 万条 | 参数敏感性可用规模 |
| `128x` | 1.024 亿条 | 目标旗舰大规模配置，是否全方法稳定完成以 capacity probe 为准 |

输入字节数由 `n_R`、`n_S`、`record_size_R_bytes` 和 `record_size_S_bytes` 自动计算并写入 summary。正文不得只用抽象倍数支撑“大规模”结论。

本文方法由三项机制组成：

1. **基于历史验证的增量规划**：验证已提交历史状态，区分稳定策略覆盖部分、待更新部分和需要重建的部分；当需要当前轮负载事实时发出 `RecognitionRequest`，接收 `LoadProfile_t` 后增量合成完整初始负载计划。
2. **边界保留驱动的双侧负载识别**：在残余范围或完整当前输入上保留边界候选，补齐双侧统计，确认热点集合、状态组合、键级内在工作量和识别充分性，并返回当前轮负载事实；它不生成路由、拆分参数、任务边界或初始归属。
3. **快照驱动的剩余工作再平衡**：执行期只针对尚未领取且满足可接管条件的尾部剩余工作，通过按需快照选择候选供给节点，并在实时队列状态上接管尾部工作单元。
实验组织遵循 A 类系统论文的证据链：少量同问题域强 baseline 证明整体竞争力，同一执行底座上的受控版本解释机制收益。正式结果围绕四个研究问题展开：

1. **RQ1：随着输入从中小规模增长至大数据规模，本文方法的收益何时出现，并能否在大数据区间持续保持。** 输入规模从 `1x` 增长到 `128x` 时，比较 Ours 与同问题域 baseline 的模型估算完成时间、负载离散度和控制路径开销。其中 `1x-8x` 用于观察控制开销、性能交叉点和过渡区间，`16x-128x` 是大数据核心结论区间。
2. **RQ2：资源扩展与 workload 变化下是否保持有效。** Strong scaling 在固定 `128x` 输入下进行；weak scaling 以 `8x->128x` 输入与 `4->64 workers` 为目标配对，并允许生成器为保持每 worker 连接工作量稳定而微调实际输入规模；倾斜、controlled work concentration、record size 和双侧状态组合默认使用 `128x`。
3. **RQ3：三项机制为什么有效。** 分别用机制 1、机制 2 和机制 3 的直接替代方案验证历史复用、双侧识别和剩余工作再平衡的贡献。
4. **RQ4：受控合成实验之外是否仍然成立，模型假设变化后是否稳定。** E7 只验证公开 workload 外部有效性和成本模型敏感性；控制成本、低倾斜退化和机制参数敏感性分别回到 E1、E3 与 E4-E6。实验对应关系固定为 `RQ1->E1`、`RQ2->E2/E3`、`RQ3->E4/E5/E6/E8`、`RQ4->E7`。

每个实验小节必须写清：实验问题、唯一变化变量、固定条件、比较方法、指标、解释规则和无效 case 规则。

## 1. 文献参考与实验设计来源

大数据 Join 实验设计文献目录为：

`研究内容一/现有工作/10_大数据Join实验设计/`

该目录至少包含或索引以下近 5 年 B 会以上相关工作，并在 `experiment_design_notes.md` 中总结每篇的实验规模、数据分布、scale sweep、skew sweep、record size、baseline、指标、公开 benchmark 和可迁移设计点。

| 论文 | 级别/年份 | 对本文实验设计的启发 |
| --- | --- | --- |
| AM-Join / Scaling and Load-Balancing Equi-Joins | SIGMOD 2022 / TODS 2025 | 大规模 skewed equi-join、Zipf skew、record size、strong/weak scaling、热点分类 baseline |
| NOCAP | SIGMOD 2023 | correlation skew、memory budget、I/O cost 与 latency 同时报告 |
| To Partition or Not | SIGMOD 2021 | 将 join 策略选择放在真实系统因素中比较，强调 workload factors 而非单点胜负 |
| Distributed GPU Joins on Fast RDMA-capable Networks | SIGMOD 2023 | 大输入、快速网络、通信与计算重叠、端到端 runtime |
| FUDJ | ICDE 2024 | 分布式 join 算法集成和对比框架，强调可复现接口 |
| MultiGPU Large Relations Join | PVLDB 2025 | large relations、out-of-core/scale sweep、硬件并行度变化 |
| Efficient GPU Joins and Grouped Aggregations | SIGMOD 2025 | 算子级性能、数据规模与硬件变化下的实验组织 |
| Zero-sided RDMA Data Shuffling | SIGMOD 2024 | RDMA/shuffle 成本模型和数据移动对照，不作为 join skew 主 baseline |
| OneSize Self-Adaptive Dispatcher | DASFAA 2024 | skewed hash join dispatcher 参照，作为 B 会补充 baseline |
| Bala-Join | arXiv 2026 | 在线倾斜检测与通信-计算协同参照；正式资格未确认前只作为 pending |

## 2. 指标、成本模型与统计规则

### 2.1 主指标与派生指标

若当前阶段没有真实 RDMA 集群测量，主性能指标统一写作 `modeled_end_to_end_time`，即统一通信、计算和控制面成本模型下的模型估算完成时间。若代码仍输出 `end_to_end_time`，该字段在本协议中解释为 `modeled_end_to_end_time` 的别名，不直接声称是真实集群墙钟时间。

| 字段 | 含义 |
| --- | --- |
| `modeled_end_to_end_time` | 当前查询模型估算完成时间 |
| `max_worker_time` | `max_i query_worker_time_i`，用于解释最慢 worker 对查询完成时间的影响 |
| `load_cv` | `std_i(query_worker_time_i)/mean_i(query_worker_time_i)`，worker 间负载离散度 |
| `straggler_tail_time` | `max_i query_worker_time_i - median_i(query_worker_time_i)` |
| `worker_imbalance_ratio` | `max_i query_worker_time_i / mean_i query_worker_time_i` |
| `join_pair_count` | `sum_k f_R(k)f_S(k)`，表示逻辑匹配对数量 |
| `output_bytes` | `join_pair_count * output_record_size_bytes` |
| `output_size` | 兼容字段；inner equi-join 中固定满足 `output_size=join_pair_count` |
| `modeled_input_throughput` | `input_bytes / modeled_end_to_end_time`，仅为基于模型估算时间的可读性派生指标，不得写成真实吞吐或独立性能证据 |
| `normalized_time` | 同一方法相对 `normalization_base_scale=1x`、`normalization_base_setting=self` 的完成时间增长倍数，即 `T_A(s)/T_A(1x)` |
| `input_scale_efficiency` | 可选辅助字段，`scale_factor / completion_time_growth_factor`，只用于 E1 辅助表，不得写成 speedup |
| `control_cpu_time_total` | 控制面 CPU 总成本，用于资源解释，不作为正文主比例指标 |
| `nonoverlapped_control_cpu_time` | 未被执行关键路径覆盖的控制面 CPU 时间 |
| `control_rdma_time_total` | 控制面 RDMA READ/CAS/WRITE 总成本，用于资源解释 |
| `nonoverlapped_control_rdma_time` | 未被执行关键路径覆盖的控制面 RDMA 时间 |
| `control_path_time` | `nonoverlapped_control_cpu_time + nonoverlapped_control_rdma_time` |
| `control_path_overhead_ratio` | `control_path_time / modeled_end_to_end_time` |
| `control_path_time_per_million_rows` | `control_path_time / (actual_input_rows / 10^6)` |
| `snapshot_aggregate_resource_time_normalized` | `(snapshot_build_time_total + snapshot_read_time_total) / modeled_end_to_end_time`，解释聚合快照资源时间相对查询完成时间的归一化值；该值允许大于 1，不表示延迟占比 |
| `snapshot_path_overhead_ratio` | `nonoverlapped_snapshot_path_time / modeled_end_to_end_time`，解释快照进入关键路径的开销比例 |

输入与输出字节字段统一按下列公式生成：

```text
actual_input_rows = n_R + n_S
input_bytes = n_R * record_size_R_bytes + n_S * record_size_S_bytes
table_ratio = n_R : n_S
```

其中 `record_size_R_bytes` 和 `record_size_S_bytes` 分别对应两侧关系的逻辑记录宽度；两侧等宽实验可以继续记录 `record_size_bytes` 作为简写或兼容字段，但正式复算以两侧字段为准。E3C 改变逻辑记录宽度时，如果输出记录包含两侧 payload，则使用 `output_record_size_bytes=record_size_R_bytes+record_size_S_bytes-join_key_size_bytes`；如果实现只按固定输出宽度计费，则必须在 descriptor 中显式记录固定 `output_record_size_bytes`，并说明 E3C 主要改变输入移动、复制和缓冲字节压力。

辅助指标包括 `simulated_network_time`、`shuffle_bytes`、`remote_read_bytes`、`max_worker_shuffle_bytes`、`max_worker_remote_read_bytes`、`network_bytes_cv`、`max_worker_network_time`、`metadata_validation_time`、`residual_scan_time`、`candidate_count_path_time`、`candidate_count_resource_time_total`、`stable_lookup_time`、`boundary_lookup_path_time`、`boundary_lookup_resource_time_total`、`plan_synthesis_time`、`history_update_background_time`、`snapshot_build_time_total`、`snapshot_read_time_total`、`nonoverlapped_snapshot_path_time`、`cas_retry_count` 和 `rdma_operation_count`。旧字段 `rdma_operation_time` 仅作为兼容输入别名，不进入正式 summary 和正文主指标。

当前成本模型假设进入控制关键路径的 CPU 操作与控制 RDMA 操作按照依赖串行执行，因此 `control_path_time` 由二者相加得到；若后续实现支持控制 CPU 与控制 RDMA 重叠，则改按控制子 DAG 的关键路径计算。

上述 `max_worker_time`、`load_cv`、`straggler_tail_time` 和 `worker_imbalance_ratio` 必须来自同一个 query-level worker-time vector `query_worker_time_i`，不得混用 stage 级、线程级和任务级时间。`modeled_end_to_end_time` 是 stage DAG 关键路径加非重叠控制路径后的最终查询完成时间；`max_worker_time` 只用于解释长尾来源，不替代主性能指标。

`join_pair_count` 和 `output_bytes` 作为解释变量，不把 `join_pair_count/time` 作为唯一吞吐指标。`tail_ratio` 暂停作为正式主证据；尾部分析使用 `max_worker_time`、`load_cv`、`straggler_tail_time` 和机制 3 内部计数。

大数据实验必须报告资源边界。若仿真器具备内存模型，记录 `max_worker_memory`、`spill_bytes`、`spill_count` 和 `oom_or_capacity_violation`；若暂时没有完整内存模型，则至少记录 `estimated_max_worker_resident_task_bytes`、`estimated_max_worker_replication_bytes`、`estimated_max_worker_input_buffer_bytes` 和 `estimated_max_worker_output_buffer_bytes`。没有内存模型时，内存和 spill 字段统一写 `NA`，不得写 0；没有真实测量时，`model_relative_error=NA`。

热点 ground truth 统一采用相对频率阈值：`theta_R=tau_R*n_R`、`theta_S=tau_S*n_S`。本轮 `tau_R/tau_S` 标记为 `pending_pilot_freeze`，pilot 写死具体数值后才能升级为最终冻结版。`top-h` 只作为算法内部候选保留策略或补充报告，不与阈值法混合作为正式热点 ground truth。各 baseline 可以使用自身热点检测逻辑，但热点集合召回、精度和状态组合评估必须统一对齐该 ground truth。

### 2.2 成本模型与 stage DAG 关键路径

单个 stage 内 worker 时间按可重叠关系计费。若计算与 bulk network 可重叠，则使用关键路径上较大者；不能重叠时才相加。RDMA 控制操作按真实依赖加入关键路径。

```text
bulk_time_{s,i} =
  overlap_enabled ? max(compute_time_{s,i}, bulk_network_time_{s,i})
                  : compute_time_{s,i} + bulk_network_time_{s,i}

worker_stage_time_{s,i} =
  bulk_time_{s,i} + dependent_rdma_time_{s,i} + output_time_{s,i}

stage_time_s = max_i worker_stage_time_{s,i}
T_e2e = critical_path_over_stage_DAG + control_path_time
```

若 stage 之间存在流水重叠，`T_e2e` 按执行 DAG 的关键路径累计，而不是机械相加。`control_path_time` 只包含尚未计入 worker 关键路径的控制面 CPU 与 RDMA 非重叠成本。同一笔 RDMA READ/WRITE/CAS 不得同时进入多个成本分量。

三类成本互斥归属：

| 成本项 | 包含操作 |
| --- | --- |
| `simulated_network_time` | shuffle、broadcast、replication 等批量数据移动 |
| `rdma_operation_time` | 旧字段兼容别名，不进入正式 summary；若旧日志中包含 snapshot 读取、状态读取或 CAS，需要映射到 `control_rdma_time_total` 或 `nonoverlapped_control_rdma_time` |
| `control_cpu_time_total` | 元数据验证、候选合并、计划计算、快照构建等控制面 CPU 总成本 |
| `control_rdma_time_total` | 所有控制面 RDMA READ/WRITE/CAS 操作的聚合资源时间 |

正文主报告使用 `control_path_overhead_ratio`；`control_cpu_time_total` 和 `control_rdma_time_total` 只用于解释资源消耗。`nonoverlapped_control_rdma_time` 只包含进入查询关键路径且未在 stage 内计费的控制 RDMA 时间。同一 RDMA READ/CAS 不得同时计入 worker stage、`rdma_operation_time` 和控制路径时间。操作延迟归属顺序固定为：若该操作已经进入 worker stage DAG，则只计入 stage；否则若它进入查询控制关键路径，则计入 `nonoverlapped_control_cpu_time` 或 `nonoverlapped_control_rdma_time`；`nonoverlapped_snapshot_path_time` 只是 `control_path_time` 的子集分解，不能再次加到 `modeled_end_to_end_time`；旧字段 `rdma_operation_time` 等兼容字段不进入正式总时间。旧字段 `control_time`、`control_cpu_overhead_ratio` 和 `rdma_operation_time` 仅作为兼容输入字段，不作为正式 summary 展示名。

历史状态后台维护单独记为 `history_update_background_time`，包括状态构建、压缩、持久化、发布延迟、队列等待和状态大小。该成本不计入当前查询 `modeled_end_to_end_time`，但连续查询实验必须报告其资源成本和下一轮是否读取到最新 committed history version。

E0 只负责成本模型参数来源与最小验证；模型敏感性统一放到 E7，不在 E0 中混入参数 sweep。

### 2.3 统计单元、相对完成时间比与置信区间

正式结果不得只报告按 `Ours` 选择的最快 replicate。所有方法使用 paired seed：相同 `data_seed`、慢节点集合、slowdown 注入和任务代价扰动随机数。

| 实验类型 | 统计口径 |
| --- | --- |
| 成本模型实验 | 以 workload seed 为统计单元；若调度竞争、慢节点注入或 CAS 竞争含随机性，再增加独立 `run_seed` |
| 真实系统实验 | 每个 workload seed 可重复多次；先汇总同一 seed 内系统运行波动，再跨 workload seed 汇总 |

成本允许时至少使用 5 个 workload seed，最好 10 个。至少 5 个 workload seed 时，对 `log(T_baseline/T_ours)` 做 paired bootstrap 95% CI；不要对各算法分别做不配对置信区间后再比较。若只使用 3 个 seed，展示原始点和均值，不强调 95% CI。

#### 2.3.1 指标命名边界

为避免把不同含义的比值都写成“加速比”，全文固定使用下列术语：

| 比较对象 | 公式 | 字段/展示名 | 是否可称为 speedup/加速比 |
| --- | --- | --- | --- |
| 不同算法、同一 case | `relative_completion_time_ratio = T_baseline/T_ours` | 相对完成时间比 | 否 |
| 不同算法、同一 case | `completion_time_reduction = (T_baseline-T_ours)/T_baseline` | 完成时间降低比例，可选辅助解释 | 否 |
| 同一算法、不同输入规模 | `completion_time_growth_factor = T_A(s)/T_A(1x)` | `normalized_time`；完成时间增长倍数 | 否 |
| 同一算法、固定输入、不同 worker 数 | `strong_scaling_speedup = T_A(p0)/T_A(p)` | strong-scaling speedup；加速比 | 是 |
| 同一算法、输入与 worker 同比增长 | `weak_scaling_normalized_time = T_A(s_p,p)/T_A(s0,p0)` | weak-scaling normalized time | 否 |

因此，`T_baseline/T_ours` 只能解释为“baseline 相对 Ours 的完成时间比”。`T_baseline/T_ours>1` 表示 Ours 的模型估算完成时间更短；它不表示 Ours 获得了 strong-scaling speedup。若正文需要更自然的文字说明，可把相对完成时间比换算为完成时间降低比例，但同一段落中不得在 ratio 和 reduction 两种口径之间来回切换。

跨算法比较统一使用相对完成时间比：

```text
R_{B/O}(c) = T_baseline(c) / T_ours(c)
geomean_relative_completion_time_ratio
  = (prod_c T_baseline(c) / T_ours(c))^(1/|C|)
```

其中 `R_{B/O}>1` 表示 Ours 的完成时间更短，`R_{B/O}<1` 表示 baseline 的完成时间更短。正文、图例和 summary 统一写“相对完成时间比”或“几何平均相对完成时间比”。

几何平均必须基于双方共同正确完成的 case 集合：

```text
C_common(B,O) = {c | B 和 Ours 在 c 上均正确完成}
GM_{B/O} =
  (prod_{c in C_common(B,O)} T_B(c) / T_O(c))^(1/|C_common(B,O)|)
```

每个 baseline 必须同时报告 `completed_case_count`、`total_case_count`、`completion_rate`、`timeout_count` 和 `capacity_failure_count`。若某方法只完成较简单 case，则相对完成时间比只能在 `C_common(B,O)` 上解释；`timeout/OOM/capacity violation` 不进入数值几何平均，但必须作为容量边界结果保留。

不同 baseline 的 `C_common(B,O)` 可能不同，因此不能直接用各自配对几何平均给所有方法做统一排名。若需要全部主方法统一横向比较，必须另定义：

```text
C_all_main = {c | 所有主方法在 c 上均正确完成}
```

baseline 与 Ours 的配对结论使用 `C_common(B,O)`；全部方法统一横向比较使用 `C_all_main`。若 `C_all_main` 太小，则不发布统一排名，只报告配对结果、完成率和容量边界。

“加速比”只用于 strong scaling 中同一算法在不同资源配置下的自我比较：

```text
S_A(p) = T_A(p0) / T_A(p)
E_A(p) = S_A(p) / (p/p0)
```

其中输入数据、倾斜分布和 `join_pair_count` 保持不变。若 `4 workers` 因容量失败不能完成，则保留该容量结果，并以最小可完成配置作为 `p0`。

### 2.4 正确性验收

正式实验必须检查输出正确性。仅比较 `output_size` 不足以排除漏算和重复输出抵消，需要保存：

| 指标 | 含义 |
| --- | --- |
| `output_size` | 输出数量 |
| `pair_checksum_sum` | 顺序无关 `sum H(r.tuple_id,s.tuple_id)`；若大规模实验不物化全部输出 pair，则记为 `NA` |
| `pair_checksum_xor` | 顺序无关 `xor H'(r.tuple_id,s.tuple_id)`；若大规模实验不物化全部输出 pair，则记为 `NA` |
| `per_key_audit_path` | 逐键审计文件路径 |
| `per_key_audit_digest` | 逐键审计文件摘要 |
| `oracle_implementation` | 独立 oracle 路径名称 |
| `oracle_config_hash` | oracle 配置摘要 |
| `audit_sample_seed` | 抽样 pair 审计随机种子 |
| `audit_sample_size` | 抽样 pair 审计规模 |

输入记录必须有稳定唯一 `tuple_id`。`H` 和 `H'` 使用两组独立 hash，建议采用 128-bit 或两个独立 64-bit 摘要。该检查是高概率正确性审计，不是零碰撞证明。小规模和代表 case 执行完整 pair-level 校验；大规模 case 若不物化全部输出 pair，则完整任意 pair hash checksum 记为 `NA`，改用精确逐键输出计数、可组合 per-key digest、独立随机抽样 pair 审计和小规模完整 pair-level oracle。参考结果必须由独立 oracle 路径生成，不得复用待测算法相同的路由或任务生成代码计算期望摘要，避免同一 bug 同时污染结果和 oracle。逐键审计数据单独保存到 `correctness_audit/{case_id}/{setting}_per_key.parquet` 或压缩二进制文件，公共 summary 只保存审计路径与摘要。当前 inner equi-join 中固定 `output_size=join_pair_count`；正文主用 `join_pair_count` 和 `output_bytes`，`output_size` 仅保留为兼容字段。

## 3. 方法集合与 baseline contract

### 3.1 主文方法集合

主文不平铺大量不同抽象层次的 join 算法，而保留少量同问题域代表路线。所有方法均在统一通信、计算和控制面成本模型下比较；`oracle`、`diagnostic` 和 `pending` 结果只用于解释边界，不进入核心结论。

| `display_name` | `setting` | `implementation_origin` | `implementation_strategy` | `result_role` | `qualification_status` | `answers_question` | `counted_costs` |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Standard Dist-HJ | `dist_hash_join` | local unified implementation | local unified | `main` | ready | 无专门倾斜处理的基础执行锚点，检验复杂控制路径在低倾斜场景中的额外开销 | repartition、shuffle、local join、输出 |
| Full-SkewJoin-style | `full_skew_join` | `paper_derived` | fixed FSJ implementation | `main` | pending | 每轮重新识别热点并生成执行前静态倾斜计划，代表无历史复用和无运行时纠偏路线 | 热点统计、计划计算、复制、FSJ round-robin 热点分发、join 执行 |
| OneSize | `onesize` | official artifact preferred | `artifact_first` | `main` | pending | 只在当前轮根据倾斜特征选择 skew hash join dispatcher 是否已经足够 | dispatcher 统计、策略选择、数据移动、join 执行 |
| AMJoin-style | `amjoin_style` | `paper_derived` | paper-derived mechanism implementation | `main` | pending | 双侧热点分类、单侧复制和 HH 多阶段拆分是否已覆盖本文主要收益 | 双侧热点统计、多阶段拆分、复制、广播、join 执行 |
| Bala-Join-style | `bala_join_style` | `paper_derived` | paper-derived pending check | `pending` | pending | 近期在线倾斜检测与通信-计算协同路线的覆盖性参照 | 在线检测、通信调度、同步/协调、join 执行 |
| Ours | `ours` | full method | full implementation | `main` | ready | 历史增量规划、双侧识别和剩余工作再平衡的完整方法 | 三机制控制成本、数据移动、join 执行、运行时再平衡 |

`Standard Dist-HJ` 不得模糊写成 `Hash/Grace Join`。只有加入磁盘分区、递归分区和 spill I/O 模型后，才能使用 Grace-style Hash Join 口径。`Standard Dist-HJ` 必须进入主图，因为无倾斜或弱倾斜时普通分布式 Hash Join 可能完成时间更短，它用于检验 Ours 是否引入过高控制开销。

`Full-SkewJoin-style` 固定为 FSJ round-robin 热点分发逻辑，不允许按 case 切换 BSJ/HSJ/FSJ 后仍使用同一名称。`OneSize` 只有在完整接入当前执行器并统一计入 modeled time 时才命名为 `OneSize`；若只是参考官方逻辑重写 dispatcher，则命名为 `OneSize-style`。不能混用官方真实时间与本文 modeled time。`AMJoin-style` 按论文机制实现，不暗示完整复现作者 Spark 系统中的所有 outer join、Tree-Join 和 IB-Join 细节。`Bala-Join-style` 在没有正式会议版本和可确认官方实现前保持 `pending`，核心结论不得依赖它。

`qualification_status` 只允许取 `pending/ready/failed/diagnostic_only`。`result_role=main` 表示该方法计划进入主图，不表示复现资格已经完成。`Full-SkewJoin-style`、`AMJoin-style` 和 pilot 后确定名称的 `OneSize` 或 `OneSize-style` 必须通过 L3 baseline qualification 后才能将 `qualification_status` 改为 `ready`；L3 检查至少包含正确性、核心机制触发、成本完整性、`16x/64x` 规模、参数公平性和 summary 字段完整性。

`Topology-aware Parallel Join` 和 `AQE-style Join Re-selection` 不作为主 baseline 平铺比较，可在相关工作、边界讨论或附录诊断中解释。`Flow-Join-style` 放入 baseline qualification queue：若后续实现成本允许，可作为高性能网络 skew join 的 `paper_derived` 诊断 baseline；本版不强制进入主文图。

### 3.2 baseline 参数调优协议

所有方法必须使用统一调优协议。热点阈值、拆分数量、广播阈值、dispatcher 批次大小、Ours 的候选预算和微批次参数等，均只能在独立 tuning case 集合上选择；正式 test case 不得用于逐 case 选取最优参数。各方法使用相同数量的候选配置或相同调优预算，并在结果中记录 `algorithm_config_hash` 与关键参数。禁止 Ours 按正式 case 逐点调参，而 baseline 只使用固定默认值。

`baseline_registry.csv` 单独记录 `display_name`、`setting`、`answers_question`、`counted_costs`、`implementation_origin`、`implementation_strategy`、`result_role`、`qualification_status` 和关键参数，不在每条结果中重复长文本。

## 4. Workload descriptor 与 summary 输出

正式结果必须至少包含以下字段：

```text
experiment_id
case_id
scenario_id
setting
display_name
scale_factor
target_scale_factor
actual_scale_factor
actual_input_rows
input_bytes
bytes_R
bytes_S
n_R
n_S
record_size_bytes
record_size_R_bytes
record_size_S_bytes
join_key_size_bytes
output_record_size_bytes
table_ratio
table_ratio_R_over_S
intra_state_alpha_R
intra_state_alpha_S
global_zipf_alpha_R
global_zipf_alpha_S
tau_R
tau_S
state_mix
hh_work_share
hc_work_share
ch_work_share
cc_work_share
hot_work_share
top_1_percent_work_share
hot_keys_per_non_cc_state
hot_key_union_count
hh_key_count
hc_key_count
ch_key_count
hot_overlap_ratio
target_rank_correlation
actual_rank_correlation
worker_count
threads_per_worker
total_parallelism
shuffle_partitions
data_seed
run_seed
model_config_hash
workload_config_hash
algorithm_config_hash
join_pair_count
output_size
output_bytes
modeled_end_to_end_time
max_worker_time
load_cv
straggler_tail_time
worker_imbalance_ratio
modeled_input_throughput
normalized_time
control_cpu_time_total
nonoverlapped_control_cpu_time
control_rdma_time_total
nonoverlapped_control_rdma_time
control_path_time
control_path_overhead_ratio
control_path_time_per_million_rows
simulated_network_time
shuffle_bytes
remote_read_bytes
max_worker_shuffle_bytes
max_worker_remote_read_bytes
network_bytes_cv
max_worker_network_time
estimated_max_worker_resident_task_bytes
estimated_max_worker_replication_bytes
estimated_max_worker_input_buffer_bytes
estimated_max_worker_output_buffer_bytes
max_worker_memory
spill_bytes
spill_count
oom_or_capacity_violation
model_relative_error
run_status
failure_type
invalid_reason
correctness_passed
pair_checksum_sum
pair_checksum_xor
per_key_audit_path
per_key_audit_digest
oracle_implementation
oracle_config_hash
audit_sample_seed
audit_sample_size
correctness_check_mode
```

机制专用字段放入机制 summary，不强行塞入公共 summary：

```text
summary_common.csv
summary_m1.csv
summary_m2.csv
summary_m3.csv
baseline_registry.csv
summary_aggregate.csv
```

正式 summary 字段统一使用小写 snake_case。旧字段名 `InputThroughput`、`NormalizedTime`、`SelectionRegret`、`InvalidProbeRate`、`CASRetryPerSuccess` 和 `SnapshotCostRatio` 只作为兼容输入别名，不作为论文或正式 summary 展示名。

正式 summary 使用整数 byte 字段，例如 `record_size_R_bytes=256`，不得在正式字段中混用 `record_width`、`record_size` 和 `256B` 等写法。`record_size_bytes` 只作为两侧等宽实验的简写或兼容字段，正式复算以 `record_size_R_bytes/record_size_S_bytes` 为准。并行度字段拆分为 `threads_per_worker` 与 `total_parallelism=worker_count*threads_per_worker`；旧字段 `parallelism` 只作为兼容输入别名，必须在导入时映射到其中一个明确字段，不得作为正式展示名。`shuffle_partitions` 单独记录，不能与 worker 或线程并行度混写。E1 的状态类别内频率形状使用 `intra_state_alpha_R/intra_state_alpha_S`；E3A 的自然倾斜使用 `global_zipf_alpha_R/global_zipf_alpha_S`。旧字段 `alpha_R/alpha_S` 只作为兼容输入别名，不作为正式 summary 展示名。`summary_common.csv` 保存 run-level workload 与性能字段；E1 的 `hot_work_share_error`、`state_work_mix_error`、诊断字段 `join_pair_count_per_row_error` 和诊断字段 `output_bytes_per_row_error` 进入 run-level summary；E2 的 `target_scale_factor`、`actual_scale_factor`、`strong_scaling_speedup`、`parallel_efficiency` 和 `weak_scaling_normalized_time` 可进入 run-level 与 aggregate summary。聚合统计单独进入 `summary_aggregate.csv`，避免把单次运行字段和跨 case 汇总字段混在同一层级。`summary_aggregate.csv` 至少包含 `completed_case_count`、`total_case_count`、`completion_rate`、`timeout_count`、`capacity_failure_count`、`pairwise_common_case_count`、`all_method_common_case_count` 和 `geomean_relative_completion_time_ratio`。

字段归属原则是：输入是什么进入 workload descriptor；算法怎样运行进入对应机制配置或 summary。`candidate_boundary_width`、`oversampling_factor_alpha`、`sketch_error` 进入 `summary_m2.csv` 或由 `algorithm_config_hash` 指向的机制2配置；`residual_fallback_threshold` 进入 `summary_m1.csv`；`K_pool`、`B_min`、`B_max`、`R_retry` 进入 `summary_m3.csv`，不放入公共 workload descriptor。

缺失值规则：没有内存模型时内存和 spill 字段写 `NA`；没有真实测量时 `model_relative_error=NA`；没有成功接管时 `cas_retry_per_success=NA`；容量失败、OOM 和 timeout 不写成 0。

## 5. 正式实验矩阵：E0-E8

### 5.0 冻结配置与运行顺序

正式批量运行前必须冻结配置表。新增或替换取值必须生成新的实验批次编号，并更新 `model_config_hash`、`workload_config_hash` 或 `algorithm_config_hash`。

| 实验 | 冻结配置 |
| --- | --- |
| E0 成本模型来源与最小验证 | RDMA READ/WRITE/CAS latency、effective bandwidth、local compute throughput、bulk network overlap policy、控制面 RDMA 依赖规则；不做参数敏感性 |
| E1 输入规模扩展 | `scale_factor={1x,2x,4x,8x,16x,32x,64x,128x}`；`1x` 是正式配置；`128x` 是目标旗舰配置和预计实验上限，能否全方法稳定完成以 capacity probe 与 L3 qualification 为准；默认 `worker_count=16`、两侧等宽 `record_size_R_bytes=record_size_S_bytes=256`、`table_ratio=1:1`、`intra_state_alpha_R=intra_state_alpha_S=0.8`、`state_mix=mixed`、`CC=10%`、`HH/HC/CH` 各约 `30%` 总 `join_pair_count` |
| E2 资源扩展 | strong scaling: `worker_count={4,8,16,32,64}`，固定 `128x`、`morsel_size`、每 worker 内部线程数、算法实现、参数计算规则和 tuning 预算；允许算法按预先冻结公式随 `worker_count` 自动实例化拆分数、partition 数或任务数；Join-work weak scaling 以 `8x->4`, `16x->8`, `32x->16`, `64x->32`, `128x->64` workers 为目标配对，实际输入规模可为稳定 `join_pair_count/worker_count` 微调 |
| E3 workload 鲁棒性 | natural skew: `global_zipf_alpha_R=global_zipf_alpha_S=alpha`，`alpha={0.0,0.2,0.5,0.8,1.0,1.2}`；controlled work concentration: `top_1_percent_work_share={0.10,0.25,0.50,0.75}`；record size: `64B/256B/1KB`；state mix 使用含 `CC=10%` 的固定工作量占比 profile |
| E4 机制1 | `M2=相同完整实现`、`M3=disabled`；`Q0...Q5` 连续查询；每组 sweep 使用独立序列；默认 `residual_partition_ratio=0.25`、`new_hot_ratio=0.10`、`partition_version_change=0.10`、`history_age=1` |
| E5 机制2 | `reuse_mode=REBUILD`、`M3=disabled`；默认 `oversampling_factor_alpha=4`、`candidate_boundary_width=0.10`、`sketch_error=0.05`；sweep 集合在 pilot 前固定候选 |
| E6 机制3 | 默认 `B_min=1 morsel`、`B_max=64 morsels`、`N=worker_count`、`K_pool=min(N-1,max(1,ceil(log2 N)))`、`CandidateCount<=K_pool`、`R_retry=2`、`random_probe_budget=K_pool`、`periodic_polling_period=0.05*T_initial_estimated`；所有方法共享相同初始计划、任务粒度和 CAS 领取路径 |
| E7 外部有效性与模型稳健性 | 公开 workload 与成本模型敏感性；模型敏感性 one-factor-at-a-time，只改变 bulk network bandwidth、RDMA READ/WRITE/CAS latency、compute throughput；通信与计算重叠比例若需考察，只能作为附录候选实验单独冻结 |
| E8 RQ3 补充：整机制消融 | `Ours / no-M1 / no-M2 / no-M3`；每个消融使用触发对应机制的场景；`no-M2` 路径固定为独立 top-h、无边界候选、无 cross-side completion |

运行顺序固定为：`0.1x smoke -> 1x correctness -> 16x preliminary baseline qualification -> 128x capacity probe -> parameter pilot -> 参数和公式冻结 -> 64x final L3 qualification -> E1-E8 正式矩阵`。smoke 阶段只验字段、正确性、成本互斥计费和缺失值规则，不能进入正文主图；`16x preliminary baseline qualification` 不能替代包含 `16x/64x` 检查的最终 L3 qualification。

“近似稳定”的相对偏差阈值统一为 `5%`：

```text
|x - x_target| / x_target <= 0.05
```

超过阈值的 case 标记为 `invalid_workload_generation`，不得进入对应主结论；若保留，只能作为 stress 或诊断配置。

### E0. 成本模型参数来源与最小验证

| 项目 | 说明 |
| --- | --- |
| 实验问题 | 统一成本模型的参数来源和关键路径计费是否足以支撑大规模结果解释 |
| 内容 | 硬件/仿真参数、数据生成方式、baseline 资格、参数调优协议、正确性检查、成本模型最小验证 |
| 指标 | `model_relative_error`、参数来源表、关键路径 overlap policy、成本互斥检查 |

E0 不承担模型敏感性 sweep。强制验证项包括：在当前设备上测量本地扫描、Standard Dist-HJ 小规模执行和内存移动吞吐；RDMA READ/WRITE/CAS 延迟与有效带宽具有明确的公开测量或已有微基准来源，并在 E7 中做敏感性分析；组件成本互斥计费通过检查。推荐但不强制的验证项是选择一个小规模代表 case 比较真实端到端执行时间和模型估算时间，并报告 `model_relative_error=|T_modeled-T_measured|/T_measured`。若没有端到端对照，`model_relative_error=NA`，正文只能声称参数量级和组件行为得到验证，不能声称模型误差已经校准。没有真实内存模型时，不得由模型直接判定 `capacity_failure` 或 `OOM`，只能报告最大驻留字节估计和潜在容量风险。

### E1. RQ1：完整输入规模曲线与整体性能

| 项目 | 说明 |
| --- | --- |
| 实验问题 | 随输入规模从 `1x` 增长到 `128x`，Ours 相对同问题域 baseline 的模型估算完成时间、负载离散度和控制路径开销如何变化 |
| 变量 | `scale_factor={1x,2x,4x,8x,16x,32x,64x,128x}` |
| 固定条件 | `worker_count=16`、两侧等宽 `record_size_R_bytes=record_size_S_bytes=256`、`table_ratio=1:1`、`intra_state_alpha_R=intra_state_alpha_S=0.8`、`state_mix=mixed`、`CC=10%`、`HH/HC/CH` 各约 `30%` 总 `join_pair_count` |
| 方法 | Standard Dist-HJ、Full-SkewJoin-style、AMJoin-style、Ours，以及 pilot 后二选一确定名称的 OneSize 或 OneSize-style；Bala-Join-style 仅 qualification 通过后补充 |
| 主指标 | `modeled_end_to_end_time`、`relative_completion_time_ratio (T_baseline/T_ours)`、`load_cv`、`control_path_overhead_ratio` |
| 解释变量 | `join_pair_count`、`output_bytes`、网络字节数、`modeled_input_throughput` |

E1 采用“键域与输入规模同比扩大”的生成方式，使键频率分布形状和热点比例的生成口径可解释。在固定相对热点阈值 `theta_X=tau_X*n_X` 下，HH 键产生的 `join_pair_count` 会随输入规模自然增长，因此 E1 描述的是“输入规模及其自然产生的连接工作共同扩大”。`join_pair_count/actual_input_rows` 和 `output_bytes/actual_input_rows` 只作为解释和诊断字段，不作为 E1 workload invalid 条件。固定键域、频率随规模增长的配置属于超线性 `join_pair_count` stress test，只能作为 E3 补充 workload 或附录实验，不进入 E1 主规模结论。

E1 的生成顺序固定为：先按 `CC=10%`、`HH/HC/CH` 各约 `30%` 的目标工作量分配状态组合；再在每个状态类别内部按 `intra_state_alpha_R=intra_state_alpha_S=0.8` 的 Zipf-like 形状分配到具体 key；最后验收状态工作量占比和 `hot_work_share`，并记录 `join_pair_count/actual_input_rows` 与 `output_bytes/actual_input_rows` 作为诊断字段。`intra_state_alpha=0.8` 表示类别内部频率形状参数，不允许先生成完整 Zipf 再强行修改部分 key 来满足状态比例。正式 descriptor 记录 `intra_state_alpha_R/intra_state_alpha_S` 与生成器配置；若后续需要验证拟合误差，另增 `alpha_fit_error`，不把“实际 alpha 拟合”作为本轮强制验收项。

E1 增加规模稳定性验收：`hot_work_share_error` 使用相对误差并不超过 `5%`；`state_work_mix_error=max_{z in {HH,HC,CH,CC}} |share_z-target_share_z|` 使用绝对百分点误差并不超过 `0.02`。`join_pair_count_per_row_error` 和 `output_bytes_per_row_error` 仍写入 summary 作为解释规模增长时连接工作量和输出字节变化的诊断字段，但不用于 E1 主结论的 workload invalid 判定。热点阈值统一使用 `theta_X=tau_X*n_X` 口径，`tau_X` 不随 scale factor 改变。

### E2. RQ2：资源扩展

E2 只研究资源扩展，不混入 skew、record size 或 state mix。

| 子实验 | 变量 | 固定条件 | 指标 |
| --- | --- | --- | --- |
| E2A Strong scaling | `worker_count={4,8,16,32,64}` | 固定 `128x` 输入、固定 `morsel_size`、固定每 worker 内部线程数、算法实现、参数计算规则和 tuning 预算；允许算法依据 `worker_count` 按预先冻结公式自动实例化拆分数、partition 数或任务数；不同算法之间允许按自身机制生成不同任务图 | strong-scaling speedup、parallel efficiency、`modeled_end_to_end_time` |
| E2B Join-work Weak Scaling | 总连接工作量和 worker 同步增加 | `8x->4`, `16x->8`, `32x->16`, `64x->32`, `128x->64` 作为目标配对；生成器允许为满足工作量稳定微调实际输入规模，并记录 `target_scale_factor`、`actual_scale_factor=actual_input_rows/800000`、`actual_input_rows` 和 `worker_count`；图中横轴使用 worker 数，不得把调整后的输入仍标成精确 `8x/16x/...`；`join_pair_count/worker_count`、`output_bytes/worker_count` 和 hot-key work per worker 在 `±5%` 内稳定 | weak-scaling normalized time、`load_cv`、capacity result |

若 `4 workers` 在 `128x` 下容量失败，必须保留 `capacity_failure`，并以最小可完成配置作为 strong-scaling speedup 基准 `p0`。允许的自动实例化只能来自 pilot 前冻结的公式，例如 `split_count=fixed_rule(worker_count, workload_stats)`；禁止看到正式结果后为每个 `worker_count` 手工选择最快 `split_count`、partition 数、grid 参数或 dispatcher 参数。`threads_per_worker` 和手工配置的 `shuffle_partitions` 在 strong scaling 中保持固定；`total_parallelism=worker_count*threads_per_worker` 随 worker 数自然变化，仅作为派生字段记录，不作为独立变化变量。若需要单独研究线程数或 partition 数，只能设置 E2 附录诊断或独立资源参数实验，不进入 E7。

### E3. RQ2：倾斜与代表性负载形态

E3 承担 workload 鲁棒性，所有子实验默认使用 `128x`，每次只改变一个变量。

| 子实验 | 变量 | 固定条件 | 指标 |
| --- | --- | --- | --- |
| E3A Natural skew | `global_zipf_alpha={0.0,0.2,0.5,0.8,1.0,1.2}` | `global_zipf_alpha_R=global_zipf_alpha_S=global_zipf_alpha`，热点排序对齐，hot overlap 固定；`global_zipf_alpha={0.0,0.2,0.5}` 同时承担低倾斜退化分析 | `modeled_end_to_end_time`、`join_pair_count`、`load_cv`、`control_path_overhead_ratio`、相对普通执行路径的退化程度 |
| E3B Controlled work concentration | `top_1_percent_work_share={0.10,0.25,0.50,0.75}` | 固定 `n_R/n_S/join_pair_count/output_bytes/worker_count`、`state_work_mix`、热点键数量和热点 overlap 生成规则，误差 `<=1%`；不使用 Zipf `alpha` 作为横轴 | `modeled_end_to_end_time`、`straggler_tail_time`、`load_cv` |
| E3C Record size | `64B/256B/1KB` | `128x` 输入、默认 skew；record size 是逻辑字节量，仿真器不真实物化完整 payload，shuffle/replication/output 按逻辑字节计费 | 数据移动成本、控制路径开销、估算缓冲字节、复制字节和输出字节压力 |
| E3D State mix | `HH-dominant = CC 10%, HH 63%, HC 13.5%, CH 13.5%`；`HC_CH_dominant = CC 10%, HH 9%, HC 40.5%, CH 40.5%`；`mixed = CC 10%, HH/HC/CH 各 30%` | 以总 `join_pair_count` 工作量占比 profile 定义状态组合；热点键数量及 overlap 由冻结生成规则确定并记录，不声称可由工作占比唯一推导；若后续需要非对称验证，再新增 `HC-dominant/CH-dominant` | 双侧负载事实识别与增量规划对不同状态组合的适应性 |
| E3E Table-size ratio | `table_ratio={1:1,1:4,1:16}`，并记录 `table_ratio_R_over_S=n_R/n_S` | 补充 workload 因素；固定 `n_R+n_S`、两侧记录宽度、worker 数、`join_pair_count`、`hot_work_share`、`state_work_mix` 和热点 overlap 生成规则；验收 `join_pair_count_error_ratio<=0.01`、`hot_work_share_error<=0.01`，若无法满足则标记 `invalid_workload_generation`，不得在同一主图中改解释为 natural table-ratio workload | 大小表关系变化下的完整方法表现 |
| E3F Hot-key count | `hot_keys_per_non_cc_state={1,10,100}` | 补充 workload 因素；对应 `hot_key_union_count={3,30,300}`；记录 `hh_key_count/hc_key_count/ch_key_count/hot_key_union_count`；固定总 `join_pair_count`、`hot_work_share`、`state_work_mix` 和两侧热点 overlap 规则，误差 `<=1%` | 相同热点总工作由不同数量热点键承担时的表现 |
| E3G Two-side frequency correlation | `target_rank_correlation={1.0,0.5,0.0,-1.0}` | 补充 workload 因素；保持 R、S 两侧边际频率多重集合不变，只置换 S 侧频率与 key 的对应关系；同频键按固定 `key_id` 升序打破并列 | `modeled_end_to_end_time`、`load_cv`、`join_pair_count`、`hot_overlap_ratio`、`state_mix` |

E3B 必须使用独立 controlled work concentration profile，不得复用自然 Zipf profile。`top_1_percent_work_share` 定义为：按每个 distinct join key 的 `f_R(k)f_S(k)` 从高到低排序，取 `top_key_count=max(1,ceil(0.01*|K_distinct|))` 个键，其 `join_pair_count` 之和占总 `join_pair_count` 的比例。E3B 只改变固定总 `join_pair_count` 下的工作集中度，`state_work_mix`、热点键数量和 overlap 必须作为冻结生成规则或验收字段保持不变。E3C 的 `1KB` 只作为逻辑成本档；若后续实现真实物化 payload，则另开容量边界实验并允许 OOM/timeout 以正式边界结果保留。E3E-E3G 是补充 workload 实验或附录实验，逻辑归属仍属于 E3，不写入 E7，也不作为本轮 Pilot 前必须执行和冻结的正文主矩阵。E3G 在 `K_corr={k | f_R(k)>0 ∧ f_S(k)>0}` 上计算 Spearman rank correlation，记录 `actual_rank_correlation`，并要求 `|actual_rank_correlation-target_rank_correlation|<=0.05`。E3G 是自然相关性 workload：`join_pair_count`、`hot_overlap_ratio` 和 `state_mix` 可以随频率置换自然变化，必须作为解释变量报告，不得声称该子实验固定了总连接工作或热点状态组合。

### E4. RQ3-机制1：基于历史验证的增量规划

机制1只在连续查询实验中启用。E1–E3 是单次查询实验，没有自然历史状态，Ours 首轮必须进入 `REBUILD`，不得提前给 Ours 准备同一 workload 的历史状态再与冷启动 baseline 比较。E4 固定 `M2=相同完整实现`、`M3=disabled`，只改变历史验证、复用和回退路径。

| 方法 | 含义 |
| --- | --- |
| Full Rebuild | 每轮重新扫描和规划 |
| Blind Reuse | 不验证，直接沿用历史控制信息 |
| Ours-M1 | 验证后选择 `FULL_REUSE/PARTIAL_REUSE/REBUILD` |
| Oracle Mode | 事后在可行且正确的模式中选最优，仅作诊断 |

连续查询序列固定为 `Q0...Q5`，其中 `Q0` 是冷启动；每组 sweep 使用独立序列，并且每次只改变 `residual_partition_ratio`、新热点比例、分片版本变化率或历史年龄之一。默认配置为 `residual_partition_ratio=0.25`、`new_hot_ratio=0.10`、`partition_version_change=0.10`、`history_age=1`。除 `history_age` sweep 外，变化比例默认只在目标轮发生，前置轮用于构造 committed history。`history_age=a` 的 case 必须先生成可用历史状态，再经过 `a` 轮未使用或被更新的间隔，最后执行目标查询 `Q_target`；`history_age=0` 表示读取最新 committed history version，中间不存在未使用或更新间隔，不表示使用当前查询尚未产生的历史。若实现选择“只在 Q5 测量年龄”，则 `Q0...Q4` 只用于构造相应年龄的历史，不得把每一轮都混入同一个统计点。外部 baseline 每轮执行其原本识别和计划流程；Ours 根据验证结果选择 `FULL_REUSE/PARTIAL_REUSE/REBUILD`。

E4 必须在 descriptor 中写死 `partition_count`、`records_per_partition` 和 `partition_assignment_rule`。四组 sweep 固定为：`residual_partition_ratio={0,0.1,0.25,0.5,1.0}`、`new_hot_ratio={0,0.1,0.25,0.5}`、`partition_version_change={0,0.1,0.25,0.5}`、`history_age={0,1,3,5}`。每个 sweep 点生成一条独立的 `Q0...Q5` 六轮序列，不在同一条序列里轮流改变多个变量。`Omega_residual = 版本变化分片 ∪ 未知分片 ∪ 主动失效分片`。`residual_partition_ratio = |Omega_residual 中分片数| / partition_count`，`partition_version_change = 当前轮版本变化分片数 / partition_count`，`new_hot_ratio = 当前轮新出现热点键数 / 当前轮热点键总数`；这三个分母在本轮协议中固定，不允许运行后再改为上一轮热点键数或其他口径。sweep `residual_partition_ratio` 时固定 `partition_version_change=0`，目标 residual 只由未知分片和主动失效分片构造；sweep `partition_version_change` 时固定未知分片和主动失效分片比例为 0，因此 `residual_partition_ratio=partition_version_change` 并记录该派生关系；sweep `new_hot_ratio` 和 `history_age` 时使用默认 `residual_partition_ratio=0.25`、`partition_version_change=0.10`，剩余 `0.15` residual 由冻结的未知分片/主动失效分片规则补足。

机制1指标包括：`modeled_end_to_end_time`、`control_path_time`、`control_cpu_time_total`、扫描输入比例、`selection_regret`、`mode_selection_accuracy`、`rebuild_trigger_count`、`false_reuse_count`、`unnecessary_rebuild_count`、`history_hit_rate`、`history_state_read_bytes`、`history_state_lookup_time`、`cold_start_time`、`break_even_residual_ratio`。`selection_regret=(T_selected-T_oracle)/T_oracle`，其中 Oracle 只能从当前 case 中正确且可执行的模式中选择。`mode_selection_accuracy` 使用近似最优规则：若 selected mode 的完成时间 `<= (1+epsilon_mode)*T_oracle`，则判定选择正确，固定 `epsilon_mode=0.05`。序列级指标包括 `query_latency_sum`、`history_update_background_time`、`modeled_sequence_completion_time` 和 `sequence_total_resource_time`；旧字段 `background_history_update_time` 只作为兼容输入别名，不作为正式 summary 展示名。历史后台维护不机械加入当前查询延迟，但必须报告保守序列资源成本。

机制1参数敏感性归入 E4 或附录，只围绕历史验证与回退策略展开。默认候选项为 `residual_fallback_threshold={0.25,0.5,0.75}`；若正文篇幅不足，该 sweep 放入附录，不移入 E7。

`mode^*=argmin_{m in {FULL_REUSE,PARTIAL_REUSE,REBUILD}} T_m`，但只有该路径通过正确性与候选充分性检查时才参与比较。Oracle 不能选择当前证据下不可执行的 `FULL_REUSE`。

### E5. RQ3-机制2：边界保留驱动的双侧负载识别

机制2实验固定 `reuse_mode=REBUILD`、`M3=disabled`。所有方法使用相同当前输入、相同下游执行策略集合、相同 Cost 模型、相同计划合成器和相同执行器；只改变统计、候选保留、跨侧补全和边界回查路径。统一的是候选识别之后的规划和执行环境，不是候选路径本身。

| 方法 | 含义 |
| --- | --- |
| Exact Full Count | 全量精确统计；作为精确统计参考、识别质量上界和端到端全量统计成本观察点，不代表计划最优性参考 |
| Top-h Only | 相同头部容量，但不保留边界候选 |
| No Cross-Side Completion | 不执行共同候选补齐另一侧频率；每侧只使用本侧候选摘要和本侧已有统计，缺失侧按确定性冷键规则处理 |
| Ours-M2 | 边界保留、共同候选、双侧回查和候选充分性认证 |

默认参数固定为 `oversampling_factor_alpha=4`、`candidate_boundary_width=0.10`、`sketch_error=0.05`。其中 `candidate_boundary_width=beta` 表示边界键条件 `|f_X(k)-theta_X|/theta_X <= beta, X in {R,S}`；只要某个键在任一关系侧进入边界区域，该键就进入共同边界回查集合 `K_boundary_lookup`，以保持双侧统计语义。`sketch_error` 表示频率估计相对误差上界，若实现中只是 sketch 配置参数，则必须在 descriptor 中记录由该配置导出的理论误差界。参数 sweep 集合为 `oversampling_factor_alpha={2,4,8}`、`candidate_boundary_width={0.05,0.10,0.25}`、`sketch_error={0,0.05,0.15}`；进入正式批量运行前必须由 pilot 确认这些集合。

`No Cross-Side Completion` 的缺失侧规则必须写入 `algorithm_config_hash` 指向的配置，并同步记录到 `baseline_registry.csv`：关系 R/S 各自只使用本侧候选摘要和本侧已有统计；不会因为某键只出现在另一侧候选集中而主动查询本侧频率；若 `k in C_R` 但 `k notin C_S`，则 S 侧按 cold 处理，反之亦然；该路径不得额外触发 exact query、stable lookup 或 cross-side lookup。该规则只影响热点状态和初始计划质量评估，不改变底层 join 执行的逻辑范围覆盖与输出正确性。

机制2参数敏感性归入 E5 或附录，只解释候选预算、边界宽度和 sketch 误差对识别质量与控制成本的影响，不移入 E7。

指标包括分侧 `hotset_recall_R/S`、`hotset_precision_R/S`、`F1_HH`、`F1_HC`、`F1_CH`、`macro_f1_non_cc`、`candidate_retention_ratio=|C_merged|/|K_distinct|`、`candidate_reduction_ratio=1-|C_merged|/|K_distinct|`、`exact_counted_key_ratio=|C_exact_counted|/|K_distinct|`、`candidate_summary_update_count`、`exact_counter_update_count`、`candidate_count_path_time`、`candidate_merge_path_time`、`boundary_lookup_path_time`、`candidate_count_resource_time_total`、`candidate_merge_resource_time_total`、`boundary_lookup_resource_time_total`、`max_worker_candidate_counter_state_bytes`、`peak_cluster_candidate_counter_state_bytes`、`peak_cluster_exact_count_state_bytes`、`boundary_lookup_ratio=|K_boundary_lookup|/|C_merged|`、`stable_lookup_keys`、`candidate_export_bytes`、`rebuild_trigger_rate`、`false_certification_rate`、`initial_plan_relative_time=max_worker_time(method_plan)/max_worker_time(exact_count_plan)`、`modeled_end_to_end_time` 和 `load_cv`。其中 `C_merged` 是跨节点合并后的候选集合，`C_exact_counted` 是真正进入双侧精确计数或精确回查的键集合，`K_boundary_lookup` 是进入共同边界回查的键集合，`K_distinct` 是当前输入中的 distinct join key 集合；若 pilot 发现 `C_exact_counted` 与 `C_merged` 在所有实现路径中始终相等，正式图只展示 `candidate_retention_ratio`，`exact_counted_key_ratio` 保留为审计字段。`candidate_count_path_time`、`candidate_merge_path_time` 和 `boundary_lookup_path_time` 定义为对应阶段进入查询关键路径的时间；`candidate_count_resource_time_total`、`candidate_merge_resource_time_total` 和 `boundary_lookup_resource_time_total` 定义为对应阶段的聚合资源时间。旧字段 `candidate_count_time`、`candidate_merge_time` 和 `boundary_lookup_time` 只作为兼容输入别名，不作为正式 summary 展示名。`scanned_input_ratio=residual_scanned_rows/total_input_rows` 保留为审计字段；在 `reuse_mode=REBUILD` 下它通常接近 1，不作为 Fig9 或机制2的主指标。机制2的主要控制收益体现在减少需要维护精确计数状态的 distinct key、降低候选导出与合并规模、仅对边界集合回查，并向机制1返回足以支持规划的当前轮负载事实。`initial_plan_relative_time` 在 `M3=disabled` 下计算；Exact Full Count 提供精确统计参考，用于衡量热点识别误差、候选压缩收益和全量统计成本，不保证在有限候选执行策略集合和当前成本模型下产生全局最优计划。Fig9 优先展示 `hotset_recall/precision`、`macro_f1_non_cc`、`candidate_retention_ratio`、`candidate_count_path_time`、`max_worker_candidate_counter_state_bytes`、`candidate_export_bytes` 和 `boundary_lookup_path_time`，不把整体 `state_macro_f1` 或 `scanned_input_ratio` 作为主 panel。

E5 必须区分 join 正确性和识别质量：join 输出与 oracle 不一致时，该 method-case 无效；热点集合与 oracle 不一致时，保留为识别结果并进入 `hotset_recall/hotset_precision`；完整方法声明候选认证成功但热点集合错误时记录为 `false_certification`；触发精确重建后仍错误属于实现异常。

### E6. RQ3-机制3：快照驱动的剩余工作再平衡

机制3实验比较运行时再平衡策略，所有方法共享相同 `InitialLoadPlan`、`QueueState`、`Head/Tail`、`morsel_size`、`B_min/B_max`、慢节点注入、任务粒度和初始负载估计误差。

| 方法 | 含义 |
| --- | --- |
| No Rebalancing | 只执行初始计划 |
| Random Probing | 空闲节点随机探测供给节点 |
| Periodic Global Polling | 周期性获取全局状态后选择供给节点 |
| Ours Snapshot + CAS | 按需快照选候选，实时 `QueueState` CAS 领取 |
| Oracle Provider Selection | 使用真实剩余工作选择 provider，仅作诊断 |

Random Probing、Periodic Global Polling、Ours Snapshot + CAS 和 Oracle Provider Selection 在找到 provider 后，都必须走同一实时 `QueueState` CAS 领取路径；唯一差异是 provider 发现或选择方式。Oracle 只能使用真实剩余工作选择 provider，仍支付相同访问、迁移、执行和 CAS 成本，不允许绕开任务粒度、`B_min/B_max` 或输出唯一性约束。Random/Periodic/Ours 使用相同 tuning 预算。

默认参数固定为 `B_min=1 morsel`、`B_max=64 morsels`、`N=worker_count`、`K_pool=min(N-1,max(1,ceil(log2 worker_count)))`、`R_retry=2`、`random_probe_budget=K_pool`、`periodic_polling_period=0.05*T_initial_estimated`。E6 正文主实验统一使用 morsel 数量作为 `B_min/B_max` 计量单位；加权 estimated-work 切分只作为附录候选，不进入本轮 Pilot 前必须执行的正文矩阵。其中 `T_initial_estimated` 来自所有方法共享的初始计划估计，必须在执行前可获得，不得使用真实完成时间或事后统计；轮询周期仍需通过独立 tuning set 验证公平性。快照中 `CandidateCount <= K_pool`，候选不足时使用真实 `CandidateCount`，不要求快照槽填满。

E6 使用 one-factor-at-a-time 扰动矩阵：E6A 只改变 `slow_worker_ratio={0.125,0.25,0.5}`，固定 `slowdown_factor=2.0`、`initial_load_estimation_error=0`、`microtask_cost_cv=0`；E6B 只改变 `slowdown_factor={1.0,1.5,2.0,4.0}`，固定 `slow_worker_ratio=0.25`、`initial_load_estimation_error=0`、`microtask_cost_cv=0`；E6C 只改变 `initial_load_estimation_error={0,0.1,0.25,0.5}`，不注入慢节点且固定 `microtask_cost_cv=0`；E6D 只改变 `microtask_cost_cv={0,0.5,1.0,2.0}`，不注入慢节点且固定 `initial_load_estimation_error=0`。其中 `slow_worker_count=max(1,ceil(slow_worker_ratio*worker_count))`；`initial_load_estimation_error=epsilon` 表示对每个初始任务独立采样 `xi_task~Uniform[-epsilon,epsilon]`，并令 `estimated_work_task=true_work_task*(1+xi_task)`，采样使用固定 `run_seed`；`microtask_cost_cv` 表示微批次代价的变异系数，pilot 使用 Gamma 或 Lognormal 分布生成非负任务代价，最终冻结版必须写死具体分布和参数。

指标包括 `modeled_end_to_end_time`、`straggler_tail_time`、`idle_to_completion_time=query_completion_time-first_worker_idle_time`、`time_to_first_successful_takeover=first_successful_takeover_time-first_worker_idle_time`、`invalid_probe_rate=invalid_probes/total_probes`、`cas_retry_per_success=cas_retry_count/successful_takeover_count`、`snapshot_aggregate_resource_time_normalized=(snapshot_build_time_total+snapshot_read_time_total)/modeled_end_to_end_time`、`snapshot_path_overhead_ratio=nonoverlapped_snapshot_path_time/modeled_end_to_end_time`、`snapshot_age_at_probe`、`stale_candidate_rate=stale_candidate_probe_count/snapshot_candidate_probe_count`、`takeover_work_per_success=total_transferred_work/successful_takeover_count`、`provider_concentration=max_provider_successful_takeover_count/successful_takeover_count`、`transferred_microbatch_count`、`transferable_work_ratio=initial_transferable_tail_work/total_initial_work`、`transferred_morsel_ratio=transferred_morsel_count/total_transferable_morsel_count` 和 `termination_detection_delay=termination_publish_time-actual_last_task_finish_time`。正文主开销结论优先使用 `snapshot_path_overhead_ratio`；`nonoverlapped_snapshot_path_time` 是 `control_path_time` 的子集，只用于分解快照在控制关键路径中的贡献，不能再次加到 `modeled_end_to_end_time`。`snapshot_aggregate_resource_time_normalized` 只解释聚合快照资源时间，允许大于 1，不表示延迟占比。若后续需要描述快照成本在再平衡控制成本内部的占比，必须使用单独字段 `snapshot_share_of_rebalance_control`，不得与上述两个字段混用。

`first_worker_idle_time` 定义为第一个 worker 进入空闲状态，且此时系统内仍存在尚未完成、可继续执行或可接管的工作时的时间。普通 worker 正常提前结束且系统已无可接管任务时，不计为负载长尾起点。若从未出现上述有效空闲事件，`idle_to_completion_time` 记为 `NA`；若没有成功接管，`time_to_first_successful_takeover` 记为 `NA`，不得写 0。

机制3正确性与终止验收必须包含：`claimed_range_overlap_count=0`、`uncovered_range_count=0`、`duplicate_output_count=0`、`termination_detection_delay`、`successful_takeover_count`。`B_min` 和 `B_max` 使用同一计量单位：近似等代价模式下为 morsel 数量，加权任务下为估计工作量，并满足 `1 <= B_min <= B_max`。`cas_retry_per_success` 在 `successful_takeover_count=0` 时记为 `NA`。`invalid_probe_rate`、`stale_candidate_rate`、`takeover_work_per_success`、`transferred_morsel_ratio`、`provider_concentration` 和 `snapshot_share_of_rebalance_control` 的分母为 0 时均记为 `NA`，不得写 0；只有发生了对应事件且计数为 0 时才能写 0。`termination_detection_delay` 必须满足 `>=0`，若出现负值应标记为事件时间线或实现异常。`stolen_task_count` 只作为旧字段兼容名，正式 summary 使用 `transferred_microbatch_count`。

机制3参数敏感性归入 E6 或附录，默认候选项包括 `K_pool={1,min(N-1,ceil(log2 N)),N-1}`、`B_min/B_max` 和 retry budget。正文篇幅不足时只保留最能解释探测成本与再平衡收益关系的一项，其余放入附录，不移入 E7。

### E7. RQ4：外部有效性与模型稳健性

E7 只回答一个问题：受控合成实验得到的主要结论，是否能推广到公开 workload，并且是否依赖某一组特定成本模型参数。E7 不再承接低倾斜退化、机制内部参数敏感性或补充 workload 因素；这些内容分别归入 E3、E4、E5 和 E6。

**E7A 公开 workload。** 至少 4 个二表 case；本轮协议标记为 `public_workload_id_pending`，未写入具体 query ID 前不得称为最终冻结版。需要冻结公开数据集、表、连接键、查询、是否 native 或 profile-scaled。比较完整 Ours 与主要 baseline，报告 `modeled_end_to_end_time`、负载离散度和 `control_path_overhead_ratio`。若原始 benchmark 太小，可生成 profile-scaled case，但必须明确标注；输入规模不足时，公开 workload 只作为外部有效性证据，不替代 E1 大数据结论。

**E7B 成本模型敏感性。** 固定代表 workload 为两个 case：Case 1 为 `64x default mixed workload`，状态组合为 `CC 10%, HH 30%, HC 30%, CH 30%`，`intra_state_alpha_R=intra_state_alpha_S=0.8`，`table_ratio=1:1`，`record_size_R_bytes=record_size_S_bytes=256`；Case 2 为 `64x HH-dominant workload`，状态组合为 `CC 10%, HH 63%, HC 13.5%, CH 13.5%`，其余 alpha、table ratio 和 record size 与 Case 1 相同。方法集合固定为 `Ours`、`Standard Dist-HJ`、`Full-SkewJoin-style`、`AMJoin-style` 以及 pilot 后确定名称的 `OneSize` 或 `OneSize-style`。使用 one-factor-at-a-time，只在 bulk network bandwidth、RDMA READ/WRITE/CAS latency、compute throughput 三者中一次改变一项，扰动集合为 `{0.5x,1.0x,2.0x}`；通信与计算重叠比例若需要考察，只能作为附录候选实验单独冻结，不作为 E7B 正文开放项。报告 Ours 与 baseline 的相对完成时间比、算法排序是否改变、控制路径开销是否仍低于收益，以及结论失效的参数边界。

**E7C 控制成本汇总表。** 该表是可选汇总，不启动新 workload：历史验证与增量扫描成本来自 E4，候选识别与边界回查成本来自 E5，Snapshot、probe 与 CAS 成本来自 E6，完整系统控制路径开销来自 E1。正文中只能把它写成已有结果的汇总，不能写成新的实验矩阵。

### E8. RQ3 补充：三项机制整机制消融

E8 只保留整机制消融，不放内部组件消融和参数敏感性。内部组件消融回到 E4、E5、E6 对应机制小节。

| 版本 | 含义 | 触发场景 |
| --- | --- | --- |
| Ours | 完整三机制 | 机制触发 workload |
| no-M1 | 关闭历史验证与增量规划，每轮重建 | 连续查询 workload |
| no-M2 | 各侧独立保留 top-h；不保留边界候选；不执行 cross-side completion；其余计划合成和执行路径保持一致 | 双侧热点/边界风险 workload |
| no-M3 | 关闭运行时剩余工作再平衡 | 慢节点或负载估计误差 workload |

不能让所有消融共用一个默认 case。若某个消融与 Ours 接近，必须先检查该 case 是否触发对应机制。E8 只能证明每项机制在其目标场景中是否必要，不能直接证明三项机制之间存在协同收益；正文应写“三项机制分别覆盖不同阶段的负载均衡问题，每项机制在其对应触发场景中具有独立贡献”。若要证明协同收益，必须另增同时包含连续相似查询、双侧边界热点、节点变慢或任务代价扰动的复合 case。

## 6. Pilot 后冻结项

以下项目不阻止 `0.1x` smoke 和参数 pilot 启动，但在升级为最终冻结执行版前必须写死，并重新生成相应的 `model_config_hash`、`workload_config_hash` 或 `algorithm_config_hash`：

1. **热点阈值**：将 `tau_R/tau_S=pending_pilot_freeze` 替换为具体数值，并验证默认 workload 中 HH、HC、CH 均有足够样本，热点集合不会覆盖过多键，且 `1x-128x` 之间热点语义稳定。
2. **机制1分片配置**：写死默认 `partition_count`、`partition_assignment_rule`，以及机制实验中分片数量是否随 scale factor 变化；`records_per_partition` 可由输入规模和分片数计算。
3. **机制2参数集合**：确认 `oversampling_factor_alpha={2,4,8}`、`candidate_boundary_width={0.05,0.10,0.25}`、`sketch_error={0,0.05,0.15}` 是否覆盖有效区间；若 pilot 显示全部饱和或不可实现，必须生成新批次后再替换。
4. **机制3扰动分布**：写死 `initial_load_estimation_error` 的误差分布和 `microtask_cost_cv` 的非负任务代价生成分布；若选择 Gamma 或 Lognormal，需要记录分布参数到 descriptor。
5. **E3 补充 workload 规则**：写死 E3E controlled table-ratio 的生成器可行性规则；写死 E3F 的 `hot_keys_per_non_cc_state` 与 `hot_key_union_count` 映射；写死 E3G 的 Spearman 相关性计算域 `K_corr`、tie-break 规则和 `|actual-target|<=0.05` 验收。
6. **机制2 No Cross-Side 规则**：写死缺失侧按 cold 处理、不得额外查询缺失侧频率、不得触发 stable lookup/cross-side lookup 的配置，并记录到 `algorithm_config_hash` 与 `baseline_registry.csv`。
7. **机制3正文矩阵**：确认 E6 使用 morsel count mode，写死 E6A-E6D one-factor-at-a-time 固定条件；若后续加入加权 estimated-work 切分，只能作为附录候选实验。
8. **并行度字段语义**：确认 `threads_per_worker`、`total_parallelism=worker_count*threads_per_worker`、`shuffle_partitions` 和旧字段 `parallelism` 的导入映射规则。
9. **正确性检查模式**：确认 `correctness_check_mode=full_pair/per_key_digest_sampled` 的字段输出、oracle 生成路径、抽样大小和 `NA` 字段比较规则。
10. **机制参数敏感性归属**：确认 E4 的 `residual_fallback_threshold={0.25,0.5,0.75}`、E5 的 `oversampling_factor_alpha={2,4,8}`、E6 的 `K_pool={1,min(N-1,ceil(log2 N)),N-1}` 是否作为最终集合；这些参数敏感性属于对应机制小节或附录，不进入 E7。
11. **E7B 模型敏感性 case**：写死两个 64x case 的 workload descriptor 和方法集合：default mixed 与 HH-dominant，方法为 `Standard Dist-HJ`、`Full-SkewJoin-style`、`AMJoin-style`、pilot 后确定名称的 `OneSize/OneSize-style` 和 `Ours`。
12. **公开 workload**：写死 `benchmark_name`、`query_id`、`join_tables`、`join_key` 和 `native_or_profile_scaled`。
13. **OneSize 名称**：官方 artifact 完整接入并统一 modeled time 时使用 `OneSize`；仅按论文逻辑重写 dispatcher 时使用 `OneSize-style`。正式图中不得继续显示由二者拼接而成的占位名。
14. **端到端模型校准**：Pilot 阶段允许 `model_relative_error=NA`，但投稿前必须完成端到端校准。至少包含低倾斜代表 case、高倾斜代表 case、一个中等规模 case 和一个当前设备可承受的较大规模 case，并报告 `T_measured`、`T_modeled`、`model_relative_error` 和方法性能排序是否一致；否则只能声称组件参数量级合理，不能声称端到端模型得到验证。

## 7. 图 5-图 12 映射

| 图 | 对应实验 | panel 角色 |
| --- | --- | --- |
| Fig5 | E1 完整输入规模曲线 | `modeled_end_to_end_time`、`relative_completion_time_ratio (T_baseline/T_ours)`、`load_cv`、`control_path_overhead_ratio` |
| Fig6 | E2 strong/weak scaling | strong-scaling speedup、parallel efficiency、weak-scaling normalized time |
| Fig7 | E3 workload 鲁棒性 | natural skew、controlled work concentration、record size、state mix |
| Fig8 | E4 机制1 | 单轮与序列累计完成时间、扫描输入比例、`selection_regret`、后台维护 |
| Fig9 | E5 机制2 | 热点集合召回/精度、`macro_f1_non_cc`、候选保留、候选计数时间、候选计数状态大小、候选导出量、边界回查开销 |
| Fig10 | E6 机制3 | 扰动完成时间、straggler tail、idle-to-completion、`snapshot_path_overhead_ratio`、CAS/probe 指标 |
| Fig11 | E7 外部有效性与模型稳健性 | 公开 workload、成本模型敏感性、可选控制成本汇总表；不包含低倾斜退化或算法参数敏感性 |
| Fig12 | E8 RQ3 补充：整机制消融 | 在三个机制触发场景中展示 `T_ablation/T_ours` 和负载离散度，避免跨不同 workload 直接比较绝对时间 |

所有图使用独立输出目录，禁止覆盖旧图。主图展示 `Standard Dist-HJ`、`Full-SkewJoin-style`、`AMJoin-style`、`Ours`，以及 pilot 后二选一确定名称的 `OneSize` 或 `OneSize-style`；`Bala-Join-style` 仅在 qualification 通过后作为补充曲线。`Topology-aware`、`AQE-style` 和 `Flow-Join-style` 只用于边界讨论、附录或资格队列，不作为默认主图曲线。

## 8. 无效 case、失败分类与审计规则

失败分类分为三层：

1. **workload 级无效**：生成器不满足控制变量、正确性 oracle 失效、E1/E2 稳定性验收失败等；该 case 的所有方法不进入对应主结论。
2. **方法级失败**：单个方法输出错误、实现异常或资格未通过；其他方法结果可保留，但该方法在该 case 标记为 failed。
3. **容量结果**：`timeout/OOM/capacity violation` 不进入数值几何平均，但必须保留为正式边界结果，不得直接删除。

正确性检查分为两种模式，并由 `correctness_check_mode` 写入 descriptor。小规模和代表 case 使用 `full_pair`：检查完整输出 pair、`output_size`、`pair_checksum_sum/xor` 与逐键输出计数。大规模且不物化全部 pair 的 case 使用 `per_key_digest_sampled`：`pair_checksum_sum=NA`、`pair_checksum_xor=NA`，改用精确逐键输出计数、可组合 per-key digest、抽样 pair audit 和独立 oracle digest。无效判定只比较当前 `correctness_check_mode` 启用的检查项；被定义为 `NA` 的字段不得参与一致性比较，也不得因为为 `NA` 而判为失败。

以下情况必须标记或剔除：

1. `run_status != success` 且未作为容量边界单独报告。
2. 当前 `correctness_check_mode` 启用的正确性检查项与 oracle 不一致；未启用或记为 `NA` 的 checksum 字段不参与比较。
3. E1 的 `hot_work_share_error > 0.05`，或 `state_work_mix_error > 0.02`；`join_pair_count_per_row_error` 和 `output_bytes_per_row_error` 只作为诊断字段，不作为 E1 workload invalid 条件。
4. E2B weak scaling 的 `join_pair_count/worker_count`、`output_bytes/worker_count` 或 hot-key work per worker 偏差超过 `5%`。
5. E3B controlled work concentration 的 `join_pair_count_error_ratio > 0.01` 或 `output_bytes_error_ratio > 0.01`。
6. E5 机制2中 join 输出与 oracle 不一致。
7. E5 机制2热点集合与 oracle 不一致却被当作 invalid 删除，而不是保留为识别质量结果。
8. E6 机制3的内部字段缺失，却被用于 snapshot/CAS/probe 内部机制结论。
9. baseline 使用正式 test case 逐点选参，或 Ours 与 baseline 使用不同调优预算。
10. `model_config_hash`、`workload_config_hash` 或 `algorithm_config_hash` 变化后混合旧结果。

## 9. 解释边界

1. 不得声称所有 baseline 都必须被 Ours 全面击败。
2. 不得把 `Standard Dist-HJ` 写成弱 baseline，也不得将其误称为 Grace Join。
3. 不得把 `Bala-Join-style` 的 pending 结果写成核心主结论。
4. 不得把 `OneSize-style` 写成官方 artifact 结果。
5. 不得把后台历史更新写成无成本。
6. 不得把机制2热点集合准确性写成 join 输出正确性。
7. 不得把机制3的快照候选选择写成全局最优 provider 选择。
8. 不得把跨算法相对完成时间比写成 speedup；speedup 只用于 strong scaling 的同算法自比较。

正式实验设计（Pilot 前冻结版）可以按以下顺序启动：`0.1x smoke -> 1x correctness -> 16x preliminary baseline qualification -> 128x capacity probe -> parameter pilot -> 参数和公式冻结 -> 64x final L3 qualification -> E1-E8 正式矩阵`。公开 workload ID、pilot 后参数集合、OneSize 最终实现名和模型验证记录写死后，才能升级为最终冻结执行版。
## Flow-Join 7-method pilot freeze addendum

This addendum freezes the current main-method set used by the formal L3 pilot and subsequent formal matrix:

`dist_hash_join / full_skew_join / onesize / amjoin_style / bala_join_style / flowjoin_adapter / ours`

`flowjoin_adapter` is included as **Flow-Join-style**, a paper-derived ICDE 2016 adapter under the unified Spark/RDD execution and modeled-cost framework. Its implemented logic is: lightweight runtime histogram over early probe/partition input, global heavy-hitter consensus, selective broadcast for detected heavy hitters, and regular distributed hash join for non-skew keys. It must not use Ours history reuse, FastLock verification, QueueState/OSP, or RDMA stealing.

Before any full formal run, the pilot must pass:

`0.1x smoke -> 1x correctness -> 16x preliminary baseline qualification -> 128x capacity probe -> parameter_pilot_freeze`

The formal launch is blocked unless `formal_l3_pilot_audit.csv`, `matrix_md_alignment_audit.md`, `baseline_paper_trend_audit.csv`, `workload_generator_stability_audit.csv`, and `metric_formula_audit.md` show no P0 issue.

## Formal L3 statistics freeze addendum

The formal 7-method matrix uses paired node/seed raw points, arithmetic summaries for per-case tables, and common-case geometric means for cross-method relative comparisons. A method pair is compared only on the common-case set that both methods complete successfully; timeout/OOM/capacity-boundary rows remain visible as boundary evidence but are not silently dropped into the geometric mean.

The main paper conclusion must not use a best-ours replicate selection. Any best-ours CSV is diagnostic only and must be named as such. The main result tables and figures use paired seeds, common-case summaries, invalid/capacity audit files, and the frozen method set:

`dist_hash_join / full_skew_join / onesize / amjoin_style / bala_join_style / flowjoin_adapter / ours`

## 20260708 v4 seven-method figure addendum

This addendum records the current figure-generation and Chapter 8 writing target. It does not override the correctness, common-case, capacity-boundary, or modeled-time rules above.

For the current manuscript draft, Figures 5-7 and Figure 11 are generated with the full seven-method set:

`Standard Dist-HJ / Full-SkewJoin / AMJoin-style / OneSize-style / Bala-Join-style / Flow-Join-style / Ours`

The seven-method display is a first-pass main-figure draft. Later revisions may move `Bala-Join-style` or `Flow-Join-style` to supplement or boundary discussion, but the present result summary should preserve their values so that this decision can be made from the data rather than from figure layout pressure.

Mechanism figures use their own controlled method sets rather than the seven general baselines:

1. **E4 / Figure 8**: compare `Ours`, `M1 Full Rebuild`, `M1 Blind Reuse`, `M1 Oracle Mode`, `Ours w/o History Reuse`, and `Ours w/o Versioned Incremental Stats`. The figure should support mode selection, oracle gap, selection regret, and background history-maintenance discussion. It must describe mechanism 1 as cross-round historical planning, not merely range selection.
2. **E5 / Figure 9**: compare `Ours`, `Exact Full Count`, `Top-h Only`, `No Cross-Side Completion`, `Ours K_check Only`, `Ours Residual Candidate Only`, and `Ours w/o Boundary Lookup`. The figure should distinguish recognition precision/recall, certified boundary recall, candidate omission upper bound, false certification, and boundary lookup cost. It must not imply that mechanism 2 generates routing, split parameters, task boundaries, or initial ownership.
3. **E6 / Figure 10**: compare `Ours`, `Random Probing`, `Periodic Global Polling`, `Oracle Provider Selection`, `Ours w/o Bounded Probing`, and `Ours-No-Rebalance`. These variants share the same real-time queue-state ownership path; they differ in provider discovery and probing policy. The figure must not describe snapshot selection as globally optimal provider selection.
4. **E8 / Figure 12**: report strict mechanism ablations under their trigger scenarios. Use `T_ablation/T_ours` and load dispersion; do not compare absolute times across different trigger workloads.

E3 owns workload-shape factors, including skew, controlled work concentration, record size, state mix, table ratio, hot-key count, and two-sided rank correlation. E7 is reserved for profile-scaled workload transfer and cost-model sensitivity such as network-cost scaling. Do not move rank correlation into E7.

Field names in a result CSV may differ from the prose names in this document. The figure script may use semantic aliases, but the availability summary must record the mapping and missing fields. Missing metrics cannot be replaced by nearby fields unless the metric definition is identical.

Chapter 8 prose for this version must avoid data-pipeline terms such as raw file paths, result directory names, best-replicate selection, or internal validity labels. The正文 only states the formal experimental口径: modeled completion time, common correct-completion configurations, correctness/capacity boundaries, and whether enough seeds exist for confidence intervals.
