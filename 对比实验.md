# 对比实验执行协议

本文档用于指导正式实验重跑、结果汇总和论文第 8 章写作。实验目标不是罗列脚本，而是回答当前三项机制是否改善 RDMA shared-nothing 环境下连接键倾斜导致的分布式等值连接负载不均衡问题。

本文方法由三项机制组成：基于历史验证的增量初始负载规划、边界保留驱动的双侧负载识别与初始负载规划、快照驱动的剩余工作再平衡。实验协议围绕这三项机制组织，不再沿用旧的预分布、单一验证或 cache 命中口径作为正式叙事。

## 0. 全局实验前提

所有方法默认运行在同一套 RDMA transport cost model 下。部分 baseline 并非 RDMA 原生算法，但它们面向 distributed join、parallel join 或 skewed hash join 的数据分配与负载均衡问题；统一通信环境后，实验比较的是 join 分配、热点处理、执行期纠偏和跨轮控制信息复用逻辑，而不是比较 TCP 与 RDMA 协议差异。

若当前阶段没有真实 RDMA 集群测量，主性能指标统一写作 `modeled_end_to_end_time`。若代码仍输出 `end_to_end_time`，该字段在本文档中解释为经微基准校准的成本模型端到端时间别名，不直接声称是真实集群墙钟时间。

| 项目 | 默认设置 |
| --- | --- |
| seeds | `7,13,23`；正式结果建议每个 seed 至少重复 5 次并报告均值/标准差或中位数/95% CI |
| 默认总输入 | `n_R+n_S=800,000` |
| 默认表大小 | `n_R=400,000`, `n_S=400,000` |
| 默认表比例 | `1:1` |
| 默认记录宽度 | `64B` |
| 默认并行度 | `parallelism=16` |
| 默认合成负载 | Zipf / hot-key mixed synthetic workload |
| 通信模型 | 统一 RDMA transport cost model |
| 默认通信倍率 | `network_cost_scale=1.0`，主实验固定 |
| 默认拓扑 | `topology_profile=two_rack_rdma`，且 `same_rack_cost < cross_rack_cost` |

主指标包括 `modeled_end_to_end_time`、`max_worker_time`、`tail_ratio` 和 `load_cv`。辅助指标包括 `avg_worker_time`、`simulated_network_time`、`shuffle_bytes`、`remote_read_bytes`、`detection_time`、`metadata_validation_time`、`residual_scan_time`、`stable_lookup_time`、`boundary_lookup_time`、`plan_synthesis_time`、`control_time`、`stolen_task_count`、`rdma_operation_count`、`join_work`、`output_size` 和 per-key 正确性指标。

分布式完成时间按关键路径定义。对每个执行阶段，先计算 worker 内部时间：

```text
worker_stage_time_i =
    compute_time_i
  + simulated_network_time_i
  + rdma_operation_time_i
  + output_time_i
```

阶段时间由最慢 worker 决定：

```text
stage_time = max_i(worker_stage_time_i)
modeled_end_to_end_time = sum(stage_time) + control_time
```

单个 case 的加速比为：

```text
speedup(baseline, ours) = T_baseline / T_ours
```

跨 case 正式汇总使用几何平均，不使用算术平均作为主汇总：

```text
geomean_speedup =
  exp((1 / |C|) * sum_{c in C} log(T_baseline(c) / T_ours(c)))
```

其中 `C` 表示参与同一组比较的 case 集合，`T` 默认使用 `modeled_end_to_end_time`。控制开销建议单独报告 P50/P95，避免平均值掩盖尾部控制成本。

所有正式实验必须检查输出正确性。仅比较 `output_size` 不足以排除漏算和重复输出抵消的情况，因此需要保存 `output_checksum`、`per_key_output_count` 和 `per_key_output_checksum`。若不物化全部 join 结果，至少保存 `sum_k f_R(k)f_S(k)` 和 `per_key_join_work` 作为按 key 的正确性代理。任意方法若 per-key 输出不一致，该 case 标记为 invalid，不得进入性能结论。

## 1. 主方法集与 baseline qualification

### 1.1 主 baseline

| 实验名 | method_id | 来源/角色 | 资格与作用 |
| --- | --- | --- | --- |
| Hash-Join | `hash_join` | 基础执行锚点 | 不做热点识别、历史复用或运行时再平衡，用于回答低倾斜时本文控制机制是否引入过高开销，以及 `FULL_REUSE` 距基础执行成本有多近 |
| Full-SkewJoin | `full_skew_join` | 静态倾斜处理代表 | 执行前识别热点并用经典哈希、复制或广播方式处理倾斜，但不包含历史增量统计、残余候选补全、边界回查和运行时剩余工作再平衡 |
| AMJoin-style | `amjoin_style` | TODS 2025 / CCF-A 相关路线 | 代表双侧热点的多阶段处理路线，使用热点键预分配、双侧热点拆分和单侧热点复制思想 |
| RDMA-OneSize | `rdma_onesize` | DASFAA 2024 / CCF-B | shared-nothing skewed hash join dispatcher baseline；在 RDMA 成本模型下选择 GraHJ/PRPD/PnR 风格策略 |
| Topology-aware Parallel Join | `topology_aware_parallel_join` | Parallel join + topology-aware line | 拓扑感知分布式 join 对照，要求在非均匀拓扑下运行 |
| AQE-style Join Re-selection | `aqe_join_reselection` | PVLDB 2024 / CCF-A | runtime statistics driven join strategy re-selection baseline；根据运行时统计重选 broadcast/shuffle/skew-split 类策略 |
| Ours-Full | `ours` | 本文 | 基于历史验证的增量初始负载规划 + 边界保留驱动的双侧负载识别与初始负载规划 + 快照驱动的剩余工作再平衡 |

正式主方法集固定为：

```text
hash_join,
full_skew_join,
amjoin_style,
rdma_onesize,
topology_aware_parallel_join,
aqe_join_reselection,
ours
```

这组 baseline 形成清晰演进线：`Hash -> 静态倾斜处理 -> 多阶段热点处理 -> 历史增量规划 + 运行时纠偏`。`Hash-Join` 不是弱基线，而是基础执行成本锚点；`Full-SkewJoin` 与 `AMJoin-style` 不重复，前者代表静态热点处理，后者代表热点分类驱动的多阶段处理。

### 1.2 不进入主 baseline 的方法

以下方法不进入主表，只能作为诊断、附录或相关工作解释：

```text
zero_sided_rdma_style,
modularis_rdma_join,
nocap_style_partitioning,
pystachio_chunked_rdma,
bala_join_style,
pactree_style_placement,
reljoin_style_selector,
patch_based_shuffling
```

`Zero-sided RDMA` 和 `Modularis` 是 RDMA shuffle/query execution 线索，不是 join-key skew load balancing 主算法。`NOCAP` 属于 correlation-aware partitioning / storage-based join 路线，不是本文所需的 shared-nothing 分布式 join 负载均衡 baseline。`PystachIO`、`BalaJoin`、`PAC-Tree`、`RelJoin` 和 `Patch-Based Shuffling` 可用于相关工作或附录诊断，但不进入默认主表。

### 1.3 消融方法

| method_id | 去除内容 | 主要用途 |
| --- | --- | --- |
| `ours` | 无 | 完整三机制方法 |
| `ours_no_history_reuse` | 每轮都 `REBUILD`，不复用历史计划种子 | 验证机制1中历史状态复用的控制成本收益 |
| `ours_no_versioned_incremental_stats` | 不区分稳定分片与残余分片 | 验证版本化增量统计是否降低扫描和统计成本 |
| `ours_kcheck_only` | 只检查历史显式待确认键，不扫描残余范围候选 | 验证机制2中残余候选补全的必要性 |
| `ours_no_boundary_lookup` | 不对跨边界频率区间执行精确回查 | 验证边界回查对热点状态和计划质量的贡献 |
| `ours_no_rebalance` | 去掉快照驱动的剩余工作再平衡 | 验证机制3对执行期长尾的贡献 |
| `ours_no_bounded_probing` | 不限制快照候选探测范围 | 验证受限探测对控制面开销的作用 |

主文消融建议展示 `ours`、`ours_no_history_reuse`、`ours_kcheck_only` 或 `ours_no_boundary_lookup`、`ours_no_rebalance`。`ours_no_versioned_incremental_stats` 和 `ours_no_bounded_probing` 可放入参数敏感性或附录诊断。

## 2. Workload descriptor

每次运行必须保存完整 workload descriptor。至少包含：

```text
experiment_id
method_id
seed
n_R
n_S
table_ratio
record_size
key_domain
skew_param
hot_key_count
hot_overlap
quadrant_mix
residual_ratio
partition_version_change_rate
new_hotkey_ratio
boundary_lookup_ratio
history_state_capacity
parallelism
topology_profile
network_cost_scale
rdma_cost_model_version
workload_family
benchmark_name
query_name
page_warmup_policy
```

合成 workload 按三步生成。第一步，生成左右关系的 key 频率分布 $f_R(k)$ 和 $f_S(k)$。第二步，根据 `hot_overlap` 和 `quadrant_mix` 构造 HH/HC/CH/CC 两侧热点状态组合。第三步，根据 `record_size` 生成 payload，并根据 `table_ratio` 分配 $|R|$ 与 $|S|$。

公开 benchmark 至少加入 JCC-H 或 JOB 中的一个代表性两表等值连接，推荐包含 JCC-H 和 JOB 各一个。候选组合包括：

| benchmark | join case | 用途 |
| --- | --- | --- |
| TPC-H | `lineitem ⋈ orders` | 低到中等倾斜参考 |
| JCC-H | 可调相关性连接 | 受控连接相关性倾斜 |
| JOB | `cast_info ⋈ title` | 真实关系连接形态 |
| JOB | `cast_info ⋈ name` | 高基数连接键场景 |

公开 benchmark 的作用是验证合成受控结论是否在更真实的数据分布下仍然成立。若当前暂时只能运行合成数据，应在结果中标记为 synthetic-only pilot，不写成完整系统评估结论。

## 3. 实验矩阵

### E0. Cost model calibration

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 如何校准所有方法共用的计算、shuffle 和 RDMA 单位成本 |
| 校准对象 | local compute、join output、shuffle、RDMA READ、RDMA WRITE、RDMA atomic、fixed RDMA op |
| 输出文件 | `rdma_cost_model.json`, `cost_model_calibration.csv`, `notes.md` |
| 后续用途 | 所有正式 baseline 和 Ours 共用同一套成本模型 |

必需输出：

```text
compute_cost_per_tuple
output_cost_per_pair
shuffle_cost_per_byte
rdma_read_cost_per_byte
rdma_write_cost_per_byte
rdma_atomic_cost_per_op
rdma_fixed_op_cost
model_error_on_representative_cases
```

若能使用真实小规模 RDMA 集群，应至少选取若干代表性 case 同时报告真实测量和模型预测误差；否则主文表述必须收缩为“经微基准校准的 RDMA 成本模型”。

### E1. Skew strength

E1 继续保留为整体性能主实验，用于观察倾斜增强时各方法的负载分布和端到端趋势。

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 倾斜强度变化时，Ours 是否比 Hash、静态倾斜处理、多阶段热点处理和运行时策略重选更稳 |
| 方法矩阵 | 主方法集 |
| 主指标 | `modeled_end_to_end_time`, `max_worker_time`, `tail_ratio`, `load_cv`, `shuffle_bytes`, `rdma_operation_count` |

E1A 为自然倾斜扫描，唯一变化变量为 `skew_param`。E1B 为 controlled join-work skew sweep，固定或近似固定 `join_work`，只改变热点质量在 key 之间的集中程度。E1B 是证明负载均衡能力的关键证据：当 `join_work` 近似固定时，若 Ours 仍能降低 `tail_ratio`、`load_cv` 和 `max_worker_time`，收益才可归因于更好的工作分布和执行期纠偏。

### E2. Workload shape

E2 保留，用于验证不同表比例、记录宽度和热点结构下的稳健性。

| 子实验 | 唯一变化变量 | 建议取值 | 观察重点 |
| --- | --- | --- | --- |
| E2A Table ratio | `table_ratio` | `1:1`, `1:4`, `1:16`, `4:1`, `16:1` | 复制、广播和拆分策略的代价边界 |
| E2B Record size | `record_size` | `16B`, `64B`, `256B`, `1KB` | 复制和远端访问成本敏感性 |
| E2C Hotspot structure | `quadrant_mix` | `HH-dominant`, `HC/CH-dominant`, `CC-heavy` | 两侧热点状态组合对初始计划和运行时长尾的影响 |

### E3. Scale and parallelism

E3 保留，用于扩展性评估。

| 子实验 | 唯一变化变量 | 建议取值 | 观察重点 |
| --- | --- | --- | --- |
| E3A Input scale | `n_R+n_S` | `20w`, `40w`, `80w`, `160w`, `320w` | 输入规模增长下的端到端趋势 |
| E3B Strong scaling | `parallelism` | `4`, `8`, `16`, `32`, `64` | 固定输入下增加 worker 是否降低尾部 |
| E3C Weak scaling | `parallelism` 与输入同比增长 | 每 worker 输入量固定 | 负载均衡和控制开销是否可扩展 |

### E4. 残余候选补全、边界回查与计划质量

| 项目 | 内容 |
| --- | --- |
| 对应机制 | 机制2：边界保留驱动的双侧负载识别与初始负载规划 |
| 实验问题 | 残余范围候选补全和边界回查是否提高当前轮热点召回、状态组合准确率和初始计划质量 |
| 方法矩阵 | `kcheck_only`, `residual_candidate_only`, `residual_plus_stable_without_boundary_lookup`, `ours_full`, `full_rebuild_exact_oracle` |
| 说明 | `full_rebuild_exact_oracle` 只作为准确率上界，不作为性能 baseline |

必须构造三类 case：

| case | 目的 |
| --- | --- |
| 新热点 case | 热点只出现在残余分片，证明只检查 `K_check` 会漏报 |
| 跨部分累积 case | 某键在稳定部分和残余部分分别不突出，但合成后进入 top-h |
| 边界误差 case | 候选频率区间跨越热点阈值，验证精确回查必要性 |

变化变量：

```text
residual_ratio
new_hotkey_ratio
oversampling_factor_alpha
candidate_boundary_width
sketch_error
stable_candidate_capacity
boundary_lookup_ratio
```

识别层指标：

```text
precision@k
recall@k
HH_HC_CH_CC_classification_accuracy
candidate_count
boundary_lookup_count
lookup_time
```

计划与执行层指标：

```text
estimated_max_worker_load
actual_max_worker_time
load_cv
tail_ratio
rebalance_trigger_count
modeled_end_to_end_time
```

理论解释规则：E4 不能只报告召回率提高，还必须说明召回改善是否降低初始计划的最大节点负载、尾部比例和后续再平衡触发次数。识别误差不应改变 join 结果正确性；若出现 output/work mismatch，说明实现破坏了默认路径、任务覆盖或互斥约束，该 case 不得进入性能结论。

### E5. Ablation and mechanism analysis

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 三项机制及关键内部设计分别贡献什么 |
| 方法矩阵 | `ours`, `ours_no_history_reuse`, `ours_no_versioned_incremental_stats`, `ours_kcheck_only`, `ours_no_boundary_lookup`, `ours_no_rebalance`, `ours_no_bounded_probing` |
| 固定条件 | 默认 `80w` workload，并补充高倾斜 HH-dominant case |
| 主指标 | `modeled_end_to_end_time`, `control_time`, `max_worker_time`, `load_cv`, `rdma_operation_count` |

消融解释规则：

| method_id | 去除内容 | 预期影响 |
| --- | --- | --- |
| `ours_no_history_reuse` | 历史计划种子和跨轮信息复用 | 重复或相似查询中的控制开销上升 |
| `ours_no_versioned_incremental_stats` | 稳定/残余分片区分 | 残余比例较低时扫描和统计成本上升 |
| `ours_kcheck_only` | 残余范围候选发现 | 新热点和跨部分累积热点召回下降 |
| `ours_no_boundary_lookup` | 频率区间边界精确回查 | 边界键误判增加，计划质量下降 |
| `ours_no_rebalance` | 快照驱动剩余工作再平衡 | 执行期长尾、最大节点时间和负载离散度上升 |
| `ours_no_bounded_probing` | 候选探测上界 | RDMA 控制面操作数和远端探测成本上升 |

主文建议只展示三项机制对应的核心消融：`ours_no_history_reuse`、`ours_kcheck_only` 或 `ours_no_boundary_lookup`、`ours_no_rebalance`。其他消融可作为参数敏感性或附录。

### E6. Runtime perturbation and residual-tail rebalancing

| 项目 | 内容 |
| --- | --- |
| 对应机制 | 机制3：快照驱动的剩余工作再平衡 |
| 实验问题 | 初始计划之后出现运行时变化和剩余长尾时，快照驱动再平衡是否降低尾部 |
| 变量组 | `slow_worker_ratio`, `slowdown_factor`, `task_cost_estimation_error`, `hotspot_location_drift`, `microtask_cost_variance` |
| 方法矩阵 | 主方法集 + `ours_no_rebalance` |
| 主指标 | `modeled_end_to_end_time`, `max_worker_time`, `load_cv`, `stolen_task_count`, `stolen_work`, `cas_failure_count`, `snapshot_refresh_count`, `control_time`, `rdma_operation_count` |

运行时扰动子实验：

| 子实验 | 唯一变化变量 | 建议取值 | 固定条件 |
| --- | --- | --- | --- |
| Slow-worker ratio | `slow_worker_ratio` | `0%`, `6.25%`, `12.5%`, `25%` | `slowdown_factor=2x` |
| Slowdown factor | `slowdown_factor` | `1.5x`, `2x`, `4x` | `slow_worker_ratio=12.5%` |
| Cost estimation error | `task_cost_estimation_error` | `0%`, `10%`, `25%`, `50%` | 慢节点比例固定 |
| Hotspot location drift | `hotspot_location_drift` | `static`, `mild`, `severe` | 估计误差固定 |
| Microtask variance | `microtask_cost_variance` | `low`, `medium`, `high` | 中高倾斜输入 |

内部设计对比：

```text
no_rebalance
random_provider_stealing
periodic_global_snapshot
on_demand_snapshot_unbounded
ours_snapshot_bounded
```

参数敏感性：

```text
B_min
B_max
K_pool
R_retry
local_step
```

新增机制指标：

```text
snapshot_build_time
request_wait_time
failed_probe_count
average_transfer_size
provider_contention
empty_snapshot_count
```

理论解释规则：如果普通 work stealing 或周期性快照能降低部分长尾但引入过多控制开销，`ours_snapshot_bounded` 应体现出更低的探测失败、等待时间或 RDMA 操作数。若 `ours_no_rebalance` 与 `ours` 接近，说明该 case 没有制造足够执行期剩余长尾，需要提高慢节点比例、减速倍率或微任务代价方差。

### E7. 版本化增量统计与历史状态复用

| 项目 | 内容 |
| --- | --- |
| 对应机制 | 机制1：基于历史验证的增量初始负载规划 |
| 实验问题 | 重复或相似查询中，版本化分片增量统计和历史状态复用是否降低统计与规划成本，并在变化增大时安全退化 |
| 方法矩阵 | `full_rebuild`, `full_reuse`, `partial_reuse`, `reuse_without_validation` |
| 说明 | `reuse_without_validation` 只作为诊断，不进入正式性能结论 |

变化变量：

| 变量 | 建议取值 | 回答的问题 |
| --- | --- | --- |
| 残余分片比例 `rho` | `0%`, `5%`, `10%`, `25%`, `50%`, `75%`, `100%` | `PARTIAL_REUSE` 何时仍有收益 |
| 分片版本变化率 | `0`, `0.1`, `0.25`, `0.5`, `1.0` | 版本变化如何影响复用模式 |
| 新热点比例 | `0`, `0.05`, `0.1`, `0.25` | 新热点能否被残余扫描发现 |
| 边界回查比例 | 按边界键占候选比例扫描 | 回查成本何时触发重建 |
| 历史状态容量 | 不同 `alpha*h` 或摘要预算 | 状态空间与准确率的权衡 |

必看指标：

```text
metadata_validation_time
residual_scan_time
stable_lookup_time
boundary_lookup_time
plan_synthesis_time
control_time
scanned_partitions
scanned_bytes
history_state_size
full_reuse_ratio
partial_reuse_ratio
rebuild_ratio
modeled_end_to_end_time
```

公平性要求：

- `FULL_REUSE/PARTIAL_REUSE/REBUILD` 均必须执行完整 join，不能缓存连接结果或输入中间结果。
- `FULL_REUSE` 只能省去重复统计和规划，不能复用上一轮物理任务编号、数据区间或节点放置结果。
- `PARTIAL_REUSE` 必须扫描残余分片，不能只检查历史显式键。
- 随着 `rho`、分片版本变化率或新热点比例升高，`partial_reuse_ratio` 和 `rebuild_ratio` 应上升；不能强行复用过期历史状态。

理论解释规则：E7 的主结论是机制1降低重复或相似查询下的统计与规划成本，而不是证明任意随机 workload 加速。正式结果必须随 `rho` 画退化曲线；固定 case 的加速比只能作为默认配置结果。

### 可选诊断：AQE behavior diagnostics

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 运行时统计驱动的 join 策略重选择能解决哪些问题，不能解决哪些问题 |
| 方法矩阵 | `aqe_join_reselection`, `ours`，必要时加入 `rdma_onesize` |
| 固定条件 | 默认 `80w` workload，并补充 HH-dominant 高倾斜 case |
| 主指标 | `modeled_end_to_end_time`, `selected_join_strategy`, `strategy_reselection_count`, `reoptimized_stage_count`, `max_worker_time`, `load_cv`, `shuffle_bytes`, `output_size` |

AQE-style baseline 可以减少错误物理策略带来的数据移动或 stage 代价，但它不导出 key-level 两侧热点状态组合，也不执行剩余尾部任务原子接管。因此，该诊断只用于解释 AQE 行为边界，不作为新的主 claim。

## 4. 可选诊断：RDMA cost sensitivity

| 项目 | 内容 |
| --- | --- |
| 实验问题 | RDMA 成本模型参数变化是否影响方法边界 |
| 唯一变化变量 | `network_cost_scale` |
| 建议取值 | `0.25`, `0.5`, `1.0`, `2.0`, `4.0` |
| 固定条件 | 默认 `80w` workload，`record_size=64B`, `parallelism=16` |
| 方法矩阵 | 主方法集 |
| 主指标 | `modeled_end_to_end_time`, `simulated_network_time`, `remote_read_bytes`, `shuffle_bytes`, `rdma_operation_count` |

该实验不属于正式主矩阵。正式主实验默认所有方法都使用统一 RDMA 通信环境，`network_cost_scale=1.0` 固定不变。只有当需要解释某个 baseline 对复制、广播或远端操作特别敏感时，才把本实验作为附录或诊断实验补充。

## 5. 输出与验收

每个 `experiment_id` 必须生成：

| 文件 | 内容 |
| --- | --- |
| `runs.csv` | 每个 seed、每次重复、每个 method 的原始运行记录 |
| `descriptors.jsonl` | 每次运行的 workload descriptor |
| `summary.csv` | 聚合后的均值、标准差、中位数、95% CI、几何平均加速比和失败状态 |
| `notes.md` | 异常、失败、降级、解释规则 |

正式结果验收规则：

- 主表只接受属于主方法集的 method。
- E4 结论只来自机制2方法矩阵，不得用普通整体性能替代候选补全和边界回查证据。
- E5 消融只接受 E5 方法集。
- E6 运行时扰动结论只来自 E6，不得用纯静态 workload 证明执行期纠偏。
- E7 历史复用结论只来自 E7，不得用随机 workload 证明普适加速。
- `network_cost_scale` 在正式主实验中固定为 `1.0`；RDMA cost sensitivity 只作为可选诊断或附录结果。
- E0 必须先完成并生成 `rdma_cost_model_version`，否则主实验只能标记为 pilot。
- `Hash-Join` 必须进入整体性能、倾斜强度和扩展性实验；它用于成本锚点，不得只在低倾斜附录中出现。
- `Full-SkewJoin` 必须进入主 baseline 表；它用于静态倾斜处理参照。
- `Topology-aware` baseline 必须在 `topology_profile=two_rack_rdma` 且 `same_rack_cost < cross_rack_cost` 的非均匀拓扑下进入主表；均匀拓扑只作为诊断对照。
- `AQE-style` baseline 必须记录 `selected_join_strategy`、`strategy_reselection_count` 和 `reoptimized_stage_count`；不得把它写成 key-level skew load balancing 方法。
- 正确性检查必须包含 `output_size`、`output_checksum`、`per_key_output_count` 和 `per_key_output_checksum`。若任意方法 per-key 输出不一致，即使总 `output_size` 相同，也不得进入性能结论。
- `pilot`、`diagnostic`、`system_reference` 和 `pending` 不进入正式结论。
