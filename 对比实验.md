# 对比实验执行协议

本文档用于指导正式实验重跑、结果汇总和论文第 8 章写作。实验目标不是罗列脚本，而是回答当前三项机制是否改善 RDMA shared-nothing 环境下连接键倾斜导致的分布式等值连接负载不均衡问题。

本文方法由三项机制组成：基于历史验证的增量初始负载规划、边界保留驱动的双侧负载识别与初始负载规划、快照驱动的剩余工作再平衡。实验协议围绕这三项机制组织，不再沿用旧的预分布、单一验证或 cache 命中口径作为正式叙事。

## 0. 全局实验前提

所有方法默认运行在同一套 RDMA transport cost model 下。部分 baseline 并非 RDMA 原生算法，但它们面向 distributed join、parallel join 或 skewed hash join 的数据分配与负载均衡问题；统一通信环境后，实验比较的是 join 分配、热点处理、执行期纠偏和跨轮控制信息复用逻辑，而不是比较 TCP 与 RDMA 协议差异。

若当前阶段没有真实 RDMA 集群测量，主性能指标统一写作 `modeled_end_to_end_time`。若代码仍输出 `end_to_end_time`，该字段在本文档中解释为经微基准校准的成本模型端到端时间别名，不直接声称是真实集群墙钟时间。

| 项目 | 默认设置 |
| --- | --- |
| seeds | 默认 `7,13,23`；成本允许时正式结果至少使用 5 个 workload seed，最好 10 个 |
| 默认总输入 | `n_R+n_S=800,000` |
| 默认表大小 | `n_R=400,000`, `n_S=400,000` |
| 默认表比例 | `1:1` |
| 默认记录宽度 | `64B` |
| 默认并行度 | `parallelism=16` |
| 默认合成负载 | Zipf / hot-key mixed synthetic workload |
| 通信模型 | 统一 RDMA transport cost model |
| 默认通信倍率 | `network_cost_scale=1.0`，主实验固定 |
| 默认拓扑 | `topology_profile=two_rack_rdma`，且 `same_rack_cost < cross_rack_cost` |

主指标包括 `modeled_end_to_end_time`、`max_worker_time`、`tail_ratio` 和 `load_cv`。辅助指标包括 `avg_worker_time`、`simulated_network_time`、`shuffle_bytes`、`remote_read_bytes`、`detection_time`、`metadata_validation_time`、`residual_scan_time`、`stable_lookup_time`、`boundary_lookup_time`、`plan_synthesis_time`、`control_time`、`history_update_background_time`、`history_state_build_time`、`history_state_compress_time`、`history_state_persist_time`、`history_publish_latency`、`history_update_queue_wait`、`history_state_size`、`new_history_visible_ratio`、`stolen_task_count`、`rdma_operation_count`、`join_work`、`output_size` 和 per-key 正确性指标。

统计口径需要区分成本模型实验和真实系统实验。若实验只运行确定性成本模型，`seed` 主要控制数据分布和扰动注入，默认每个 workload seed 运行一次并跨 seed 汇总；只有调度竞争、慢节点注入或 CAS 竞争本身包含随机性时，才为同一 workload seed 增加独立运行 seed。若后续补充真实系统实验，则每个 workload seed 可重复多次，先汇总同一 seed 内的系统运行波动，再跨 workload seed 汇总。正式结果不能把 `3` 个数据 seed 与每个 seed 的多次重复简单视为完全独立样本；成本允许时至少使用 5 个 workload seed，最好使用 10 个。若仍使用 3 个 seed，只展示三个原始点和均值，不强调 95% CI；需要置信区间时建议以 workload seed 为统计单元，或使用分层 bootstrap。

分布式完成时间按关键路径定义。对每个执行阶段，先计算 worker 内部时间：

```text
worker_stage_time_i =
    compute_time_i
  + simulated_network_time_i
  + rdma_operation_time_i
  + output_time_i
```

阶段时间由最慢 worker 决定：

```text
stage_time = max_i(worker_stage_time_i)
modeled_end_to_end_time = sum(stage_time) + control_time
```

各时间分量按照操作类型互斥归属。`simulated_network_time` 只包含 shuffle、broadcast、replication 等批量数据移动；`rdma_operation_time` 只包含快照读取、状态读取、CAS 等控制面单边操作；`control_time` 只包含元数据验证、候选合并、计划计算、快照构建等尚未计入 worker 关键路径的控制面 CPU 时间和等待成本。同一笔 RDMA READ/WRITE/CAS 不能同时进入 `simulated_network_time` 与 `rdma_operation_time`，快照 CAS 也不能既完整计入 worker 时间又完整计入 `control_time`。若控制操作与执行阶段重叠，只计入关键路径上的非重叠部分。

历史状态维护单独区分三类时间：当前查询完成时间 `T_e2e`，后台历史更新耗时 `T_history_update^bg`，以及连续工作负载中因后台队列积压或新版本尚未可见造成的后续查询影响。连接完成后用于构建下一轮历史状态的合并、压缩、索引写入、持久化和 committed version 发布记为 `history_update_background_time`；该成本可与后续空闲时间或其他查询重叠，不计入当前查询的 `modeled_end_to_end_time`。但它不是无开销操作，实验需要单独报告状态构建、压缩、持久化、发布延迟、后台队列等待、状态大小和新版本可见比例。若下一轮查询到达时新版本尚未发布，只能读取上一版 committed state 或进入回退路径，不能使用半写入状态。

单个 case 的加速比为：

```text
speedup(baseline, ours) = T_baseline / T_ours
```

跨 case 正式汇总使用几何平均，不使用算术平均作为主汇总：

```text
geomean_speedup =
  exp((1 / |C|) * sum_{c in C} log(T_baseline(c) / T_ours(c)))
```

其中 `C` 表示参与同一组比较的 case 集合，`T` 默认使用 `modeled_end_to_end_time`。控制开销建议单独报告 P50/P95，避免平均值掩盖尾部控制成本；报告 P50/P95 时必须注明统计对象是跨运行、跨 case、跨 worker，还是跨快照事件。

所有正式实验必须检查输出正确性。仅比较 `output_size` 不足以排除漏算和重复输出抵消的情况，因此需要保存 `output_checksum`、`per_key_output_count` 和 `per_key_output_checksum`。若不物化全部 join 结果，输入记录必须具有稳定且唯一的 `tuple_id`，执行过程中至少维护顺序无关的流式 pair checksum：`pair_checksum_sum=sum H(r.tuple_id,s.tuple_id)`、`pair_checksum_xor=xor H'(r.tuple_id,s.tuple_id)`，并按 key 保存 `per_key_pair_checksum_sum` 和 `per_key_pair_checksum_xor`。`H` 和 `H'` 使用两组独立哈希，建议采用 128-bit 摘要或两个独立 64-bit 摘要；该检查是高概率正确性审计，不是数学上的零碰撞证明。`sum_k f_R(k)f_S(k)` 和 `per_key_join_work` 只能作为输出规模与工作覆盖的弱代理，不能单独证明输出内容正确。任意方法若 per-key 输出或 pair checksum 不一致，该 case 标记为 invalid，不得进入性能结论。

## 1. 主方法集与 baseline qualification

### 1.1 主 baseline

| 实验名 | method_id | 来源/角色 | 资格与作用 |
| --- | --- | --- | --- |
| Hash-Join | `hash_join` | 基础执行锚点 | 不做热点识别、历史复用或运行时再平衡，用于回答低倾斜时本文控制机制是否引入过高开销，以及 `FULL_REUSE` 距基础执行成本有多近 |
| Full-SkewJoin | `full_skew_join` | 静态倾斜处理代表 | 锁定为 Full-SkewJoin/FSJ 静态倾斜处理版本；执行前识别热点并用经典哈希、复制或广播方式处理倾斜，但不包含历史增量统计、残余候选补全、边界回查和运行时剩余工作再平衡 |
| AMJoin-style | `amjoin_style` | TODS 2025 / CCF-A 相关路线 | 代表双侧热点的多阶段处理路线，使用热点键预分配、双侧热点拆分和单侧热点复制思想 |
| RDMA-OneSize | `rdma_onesize` | DASFAA 2024 / CCF-B | shared-nothing skewed hash join dispatcher baseline；在 RDMA 成本模型下选择 GraHJ/PRPD/PnR 风格策略 |
| Topology-aware Parallel Join | `topology_aware_parallel_join` | Parallel join + topology-aware line | 拓扑感知分布式 join 对照，要求在非均匀拓扑下运行 |
| AQE-style Join Re-selection | `aqe_join_reselection` | PVLDB 2024 / CCF-A | runtime statistics driven join strategy re-selection baseline；根据运行时统计重选 broadcast/shuffle/skew-split 类策略 |
| Ours-Full | `ours` | 本文 | 基于历史验证的增量初始负载规划 + 边界保留驱动的双侧负载识别与初始负载规划 + 快照驱动的剩余工作再平衡 |

候选主方法集包含：

```text
hash_join,
full_skew_join,
amjoin_style,
rdma_onesize,
topology_aware_parallel_join,
aqe_join_reselection,
ours
```

完成 baseline qualification 后形成最终主方法集。若 `topology_aware_parallel_join` 在正式重跑前仍无法锁定具体论文、算法和任务分配规则，则从最终主方法集中移除，只保留为诊断或相关工作说明。合格后的 baseline 形成清晰演进线：`Hash -> 静态倾斜处理 -> 多阶段热点处理 -> 历史增量规划 + 运行时纠偏`。`Hash-Join` 不是弱基线，而是基础执行成本锚点；`Full-SkewJoin` 与 `AMJoin-style` 不重复，前者代表静态热点处理，后者代表热点分类驱动的多阶段处理。

### 1.2 Baseline implementation contract

所有主 baseline 必须记录实现边界，避免把不同论文系统的完整能力与本文成本模型中的适配版本混用。每个方法至少说明：输入统计来源、热点阈值或策略触发条件、是否知道精确频率、热点识别与计划生成成本是否计入、是否允许运行时调整、如何适配 RDMA 成本模型，以及原论文中未实现或未适配的能力。

| method_id | 输入统计与热点规则 | 计划/识别成本 | 运行时调整 | RDMA 成本模型适配 | 未实现能力与限制 |
| --- | --- | --- | --- | --- | --- |
| `hash_join` | 不使用热点统计；仅按哈希分区执行 | 无热点识别成本 | 不允许 | shuffle、exchange 和本地 join 成本按统一模型计入 | 只作为基础执行锚点，不处理倾斜 |
| `full_skew_join` | 采用 Full-SkewJoin/FSJ 静态倾斜处理版本；热点阈值与复制/广播规则固定记录 | 当前轮热点识别和静态计划成本必须计入 | 不允许 | 复制、广播、shuffle 按统一 RDMA 成本模型计入 | 只代表执行前静态倾斜处理；不含历史复用、残余候选补全和运行时再平衡 |
| `amjoin_style` | 明确是否实现 Tree-Join/多阶段执行，以及热点状态组合如何得到 | 热点识别、分类和多阶段任务生成成本必须计入 | 只按预设规则展开任务，不读取真实剩余状态再分配 | 多阶段数据移动和任务调度按统一模型计入 | 若只适配 binary inner equi-join，必须说明未覆盖原工作中的其他 join 类别 |
| `rdma_onesize` | 记录 dispatcher 可观察的统计和候选策略集合 | dispatcher 选择成本计入 | 按其策略模型执行，不启用本文快照再平衡 | RDMA read/write/atomic 成本按统一模型计入 | 作为 skewed hash join dispatcher 对照，不代表本文三机制 |
| `topology_aware_parallel_join` | 必须补充具体论文或算法名称、任务分配规则和拓扑输入 | 拓扑感知计划成本计入 | 按原算法边界执行 | `same_rack_cost` 与 `cross_rack_cost` 按统一拓扑模型计入 | 若具体方法尚未锁定，标记为 `pending qualification`，不得进入正式主表结论 |
| `aqe_join_reselection` | 使用运行时统计触发 stage 边界重选；可选策略集合限定为 broadcast/shuffle/skew-split 类策略 | 运行时统计收集和 reoptimization 成本计入 | 只修改未完成 stage 或后续策略 | 重选后的数据移动按统一模型计入 | 这是 AQE-style 适配，不暗示完整复现 Photon AQE，也不提供 key-level 双侧热点语义 |
| `ours` | 使用三项机制形成初始计划并执行快照驱动剩余工作再平衡 | 三项机制的验证、识别、规划和快照控制成本全部计入 | 允许快照候选选择与实时 CAS 领取 | RDMA 状态读取、快照发布和 CAS 按统一模型计入 | 不缓存 join 结果，不复用上一轮物理任务放置 |

`Topology-aware Parallel Join` 在具体论文或算法名称未锁定前只能作为待确认 baseline。若重跑时仍无法给出明确来源、任务分配规则和适配方式，应从正式主结论表中移除，仅保留为诊断或相关工作说明。

### 1.3 不进入主 baseline 的方法

以下方法不进入主表，只能作为诊断、附录或相关工作解释：

```text
zero_sided_rdma_style,
modularis_rdma_join,
nocap_style_partitioning,
pystachio_chunked_rdma,
bala_join_style,
pactree_style_placement,
reljoin_style_selector,
patch_based_shuffling
```

`Zero-sided RDMA` 和 `Modularis` 是 RDMA shuffle/query execution 线索，不是 join-key skew load balancing 主算法。`NOCAP` 属于 correlation-aware partitioning / storage-based join 路线，不是本文所需的 shared-nothing 分布式 join 负载均衡 baseline。`PystachIO`、`BalaJoin`、`PAC-Tree`、`RelJoin` 和 `Patch-Based Shuffling` 可用于相关工作或附录诊断，但不进入默认主表。

### 1.4 消融方法

| method_id | 去除内容 | 主要用途 |
| --- | --- | --- |
| `ours` | 无 | 完整三机制方法 |
| `ours_no_history_reuse` | 每轮都 `REBUILD`，不复用历史计划种子 | 验证机制1中历史状态复用的控制成本收益 |
| `ours_no_versioned_incremental_stats` | 不区分稳定分片与残余分片 | 验证版本化增量统计是否降低扫描和统计成本 |
| `ours_kcheck_only` | 只检查历史显式待确认键，不扫描残余范围候选 | 验证机制2中残余候选补全的必要性 |
| `ours_no_boundary_lookup` | 不对跨边界频率区间执行精确回查 | 验证边界回查对热点状态和计划质量的贡献 |
| `ours_no_rebalance` | 去掉快照驱动的剩余工作再平衡 | 验证机制3对执行期长尾的贡献 |
| `ours_no_bounded_probing` | 不限制快照候选探测范围 | 验证受限探测对控制面开销的作用 |

主文消融建议展示 `ours`、`ours_no_history_reuse`、`ours_kcheck_only` 或 `ours_no_boundary_lookup`、`ours_no_rebalance`。`ours_no_versioned_incremental_stats` 和 `ours_no_bounded_probing` 可放入参数敏感性或附录诊断。

## 2. Workload descriptor

每次运行必须保存完整 workload descriptor。至少包含：

```text
experiment_id
method_id
result_role
seed
run_seed
n_R
n_S
table_ratio
record_size
key_domain
skew_param
hot_key_count
hot_overlap
quadrant_mix
residual_ratio
partition_version_change_rate
new_hotkey_ratio
boundary_lookup_ratio
history_state_capacity
baseline_variant
oversampling_factor_alpha
candidate_boundary_width
sketch_error
stable_candidate_capacity
slow_worker_ratio
slowdown_factor
task_cost_estimation_error
initial_load_estimation_error
microtask_cost_variance
B_min
B_max
K_pool
R_retry
local_step
selected_mode
rebuild_reason
partition_count
changed_partition_count
parallelism
topology_profile
network_cost_scale
rdma_cost_model_version
workload_family
benchmark_name
query_name
page_warmup_policy
algorithm_config_hash
```

`result_role` 取值固定为 `main`、`ablation`、`oracle`、`diagnostic` 或 `pending`，用于自动过滤正式主结论。`oracle`、`diagnostic` 和 `pending` 不进入主表性能结论。`algorithm_config_hash` 必须覆盖算法开关、阈值、摘要容量、成本模型版本和 baseline 变体，保证同一结果可以追溯到唯一配置。`initial_load_estimation_error`、`microtask_cost_variance`、`candidate_boundary_width`、`sketch_error` 等分类变量在正式运行前必须映射为确定数值；例如 `mild/severe` 不能只作为自然语言标签进入 `runs.csv`。

合成 workload 按三步生成。第一步，生成左右关系的 key 频率分布 $f_R(k)$ 和 $f_S(k)$。第二步，根据 `hot_overlap` 和 `quadrant_mix` 构造 HH/HC/CH/CC 两侧热点状态组合。第三步，根据 `record_size` 生成 payload，并根据 `table_ratio` 分配 $|R|$ 与 $|S|$。

公开 benchmark 至少加入 JCC-H 或 JOB 中的一个代表性两表等值连接，推荐包含 JCC-H 和 JOB 各一个。候选组合包括：

| benchmark | join case | 用途 |
| --- | --- | --- |
| TPC-H | `lineitem ⋈ orders` | 低到中等倾斜参考 |
| JCC-H | 可调相关性连接 | 受控连接相关性倾斜 |
| JOB | `cast_info ⋈ title` | 真实关系连接形态 |
| JOB | `cast_info ⋈ name` | 高基数连接键场景 |

公开 benchmark 的作用是验证合成受控结论是否在更真实的数据分布下仍然成立。若当前暂时只能运行合成数据，应在结果中标记为 synthetic-only pilot，不写成完整系统评估结论。

## 3. 实验矩阵

### E0. Cost model calibration

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 如何校准所有方法共用的计算、shuffle 和 RDMA 单位成本 |
| 校准对象 | local compute、join output、shuffle、RDMA READ、RDMA WRITE、RDMA atomic、fixed RDMA op |
| 输出文件 | `rdma_cost_model.json`, `cost_model_calibration.csv`, `notes.md` |
| 后续用途 | 所有正式 baseline 和 Ours 共用同一套成本模型 |

必需输出：

```text
compute_cost_per_tuple
output_cost_per_pair
shuffle_cost_per_byte
rdma_read_cost_per_byte
rdma_write_cost_per_byte
rdma_atomic_cost_per_op
rdma_fixed_op_cost
model_error_on_representative_cases
```

若能使用真实小规模 RDMA 集群，应至少选取若干代表性 case 同时报告真实测量和模型预测误差；否则主文表述必须收缩为“经微基准校准的 RDMA 成本模型”。

### E1. Skew strength

E1 继续保留为整体性能主实验，用于观察倾斜增强时各方法的负载分布和端到端趋势。

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 倾斜强度变化时，Ours 是否比 Hash、静态倾斜处理、多阶段热点处理和运行时策略重选更稳 |
| 方法矩阵 | 主方法集 |
| 主指标 | `modeled_end_to_end_time`, `max_worker_time`, `tail_ratio`, `load_cv`, `shuffle_bytes`, `rdma_operation_count` |

E1A 为自然倾斜扫描，唯一变化变量为 `skew_param`。E1B 为 controlled join-work skew sweep，固定或近似固定 `join_work`，只改变热点质量在 key 之间的集中程度。E1B 必须满足 `|W-W_target|/W_target <= 1%`，否则该 case 不能用于证明“总工作量近似一致下的负载分布改善”。E1B 是证明负载均衡能力的关键证据：当 `join_work` 近似固定时，若 Ours 仍能降低 `tail_ratio`、`load_cv` 和 `max_worker_time`，收益才可归因于更好的工作分布和执行期纠偏。

### E2. Workload shape

E2 保留，用于验证不同表比例、记录宽度和热点结构下的稳健性。

| 子实验 | 唯一变化变量 | 建议取值 | 观察重点 |
| --- | --- | --- | --- |
| E2A Table ratio | `table_ratio` | `1:1`, `1:4`, `1:16`, `4:1`, `16:1` | 复制、广播和拆分策略的代价边界 |
| E2B Record size | `record_size` | `16B`, `64B`, `256B`, `1KB` | 复制和远端访问成本敏感性 |
| E2C Hotspot structure | `quadrant_mix` | `HH-dominant`, `HC/CH-dominant`, `CC-heavy` | 两侧热点状态组合对初始计划和运行时长尾的影响 |

### E3. Scale and parallelism

E3 保留，用于扩展性评估。

| 子实验 | 唯一变化变量 | 建议取值 | 观察重点 |
| --- | --- | --- | --- |
| E3A Input scale | `n_R+n_S` | `20w`, `40w`, `80w`, `160w`, `320w` | 输入规模增长下的端到端趋势 |
| E3B Strong scaling | `parallelism` | `4`, `8`, `16`, `32`, `64` | 固定输入下增加 worker 是否降低尾部 |
| E3C Weak scaling | `parallelism` 与输入同比增长 | 每 worker 输入量固定 | 负载均衡和控制开销是否可扩展 |

### E4. 残余候选补全、边界回查与计划质量

| 项目 | 内容 |
| --- | --- |
| 对应机制 | 机制2：边界保留驱动的双侧负载识别与初始负载规划 |
| 实验问题 | 残余范围候选补全和边界回查是否提高当前轮热点召回、状态组合准确率和初始计划质量 |
| 方法矩阵 | `ours_kcheck_only`, `ours_residual_candidate_only`, `ours_no_boundary_lookup`, `ours`, `oracle_full_rebuild_exact` |
| 说明 | `oracle_full_rebuild_exact` 的 `result_role=oracle`，只作为准确率上界，不作为性能 baseline |

必须构造三类 case：

| case | 目的 |
| --- | --- |
| 新热点 case | 热点只出现在残余分片，证明只检查 `K_check` 会漏报 |
| 跨部分累积 case | 某键在稳定部分和残余部分分别不突出，但合成后进入 top-h |
| 边界误差 case | 候选频率区间跨越热点阈值，验证精确回查必要性 |

变化变量：

```text
residual_ratio
new_hotkey_ratio
oversampling_factor_alpha
candidate_boundary_width
sketch_error
stable_candidate_capacity
boundary_lookup_ratio
```

识别层指标：

```text
precision@k
recall@k
HH_HC_CH_CC_classification_accuracy
candidate_count
boundary_lookup_count
lookup_time
```

计划与执行层指标：

```text
estimated_max_worker_load
actual_max_worker_time
load_cv
tail_ratio
rebalance_trigger_count
modeled_end_to_end_time
```

理论解释规则：E4 不能只报告召回率提高，还必须说明召回改善是否降低初始计划的最大节点负载、尾部比例和后续再平衡触发次数。识别误差不应改变 join 结果正确性；若出现 output/work mismatch，说明实现破坏了默认路径、任务覆盖或互斥约束，该 case 不得进入性能结论。

### E5. Ablation and mechanism analysis

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 三项机制及关键内部设计分别贡献什么 |
| 定位 | E5 是贡献汇总实验，不用一个统一默认 case 替代 E4/E6/E7 的机制专门实验 |
| 主指标 | `modeled_end_to_end_time`, `control_time`, `max_worker_time`, `load_cv`, `rdma_operation_count` |

E5 不再让所有消融共享一个默认 `80w` case。不同消融只有在对应机制被触发时才有解释意义；否则消融与 `ours` 接近只能说明该 case 没有覆盖对应机制，而不能说明机制无效。

| 子实验 | 对应消融 | 必须具备的触发条件 | 重点指标 |
| --- | --- | --- | --- |
| E5A 历史复用消融 | `ours_no_history_reuse`, `ours_no_versioned_incremental_stats` | 重复或相似查询序列，且存在稳定分片与残余分片 | `metadata_validation_time`, `residual_scan_time`, `scanned_bytes`, `control_time`, `history_update_background_time`, `modeled_end_to_end_time` |
| E5B 识别规划消融 | `ours_kcheck_only`, `ours_no_boundary_lookup` | 残余新热点、稳定与残余累积热点、跨阈值边界键 | `recall@k`, `classification_accuracy`, `estimated_max_worker_load`, `load_cv`, `rebalance_trigger_count` |
| E5C 运行时纠偏消融 | `ours_no_rebalance`, `ours_no_bounded_probing` | 慢节点、初始负载估计误差、微任务代价方差或执行长尾 | `max_worker_time`, `tail_ratio`, `snapshot_build_time`, `failed_probe_count`, `rdma_operation_count` |

主文建议展示一张贡献汇总表：机制1使用 E5A 或 E7 的结果，机制2使用 E5B 或 E4 的结果，机制3使用 E5C 或 E6 的结果。`ours_no_versioned_incremental_stats` 和 `ours_no_bounded_probing` 可放入参数敏感性或附录诊断；正式主结论至少覆盖 `ours_no_history_reuse`、`ours_kcheck_only` 或 `ours_no_boundary_lookup`、`ours_no_rebalance`。

### E6. Runtime perturbation and residual-tail rebalancing

| 项目 | 内容 |
| --- | --- |
| 对应机制 | 机制3：快照驱动的剩余工作再平衡 |
| 实验问题 | 初始计划之后出现运行时变化和剩余长尾时，快照驱动再平衡是否降低尾部 |
| 变量组 | `slow_worker_ratio`, `slowdown_factor`, `task_cost_estimation_error`, `initial_load_estimation_error`, `microtask_cost_variance` |
| 方法矩阵 | 主方法集 + `ours_no_rebalance` |
| 主指标 | `modeled_end_to_end_time`, `max_worker_time`, `load_cv`, `stolen_task_count`, `stolen_work`, `cas_failure_count`, `snapshot_refresh_count`, `control_time`, `rdma_operation_count` |

运行时扰动子实验：

| 子实验 | 唯一变化变量 | 建议取值 | 固定条件 |
| --- | --- | --- | --- |
| Slow-worker ratio | `slow_worker_ratio` | `0%`, `6.25%`, `12.5%`, `25%` | `slowdown_factor=2x` |
| Slowdown factor | `slowdown_factor` | `1.5x`, `2x`, `4x` | `slow_worker_ratio=12.5%` |
| Cost estimation error | `task_cost_estimation_error` | `0%`, `10%`, `25%`, `50%` | 慢节点比例固定 |
| Initial load estimation error | `initial_load_estimation_error` | `none`, `mild`, `severe` | 初始计划预测的节点负载排序与真实执行负载排序之间的偏差；不表示批处理执行期间键频率动态变化 |
| Microtask variance | `microtask_cost_variance` | `low`, `medium`, `high` | 中高倾斜输入 |

内部设计对比：

```text
ours_no_rebalance
random_provider_stealing
periodic_global_snapshot
on_demand_snapshot_unbounded
ours
```

参数敏感性：

```text
B_min
B_max
K_pool
R_retry
local_step
```

新增机制指标：

```text
snapshot_build_time
request_wait_time
failed_probe_count
average_transfer_size
provider_contention
empty_snapshot_count
```

理论解释规则：如果普通 work stealing 或周期性快照能降低部分长尾但引入过多控制开销，`ours` 作为 bounded on-demand snapshot 正式方法，应体现出更低的探测失败、等待时间或 RDMA 操作数。若 `ours_no_rebalance` 与 `ours` 接近，说明该 case 没有制造足够执行期剩余长尾，需要提高慢节点比例、减速倍率或微任务代价方差。

### E7. 版本化增量统计与历史状态复用

| 项目 | 内容 |
| --- | --- |
| 对应机制 | 机制1：基于历史验证的增量初始负载规划 |
| 实验问题 | 重复或相似查询中，版本化分片增量统计和历史状态复用是否降低统计与规划成本，并在变化增大时安全退化 |
| 方法矩阵 | `ours_validated_reuse`, `ours_no_history_reuse`, `reuse_without_validation` |
| 说明 | `FULL_REUSE/PARTIAL_REUSE/REBUILD` 是 `ours_validated_reuse` 验证后选择的 `selected_mode`，不是平级算法；`reuse_without_validation` 只作为错误设计诊断，不进入正式性能结论 |

变化变量：

| 变量 | 建议取值 | 回答的问题 |
| --- | --- | --- |
| 残余分片比例 `rho` | `0%`, `5%`, `10%`, `25%`, `50%`, `75%`, `100%` | `PARTIAL_REUSE` 何时仍有收益 |
| 分片版本变化率 | `0`, `0.1`, `0.25`, `0.5`, `1.0` | 版本变化如何影响复用模式 |
| 新热点比例 | `0`, `0.05`, `0.1`, `0.25` | 新热点能否被残余扫描发现 |
| 边界回查比例 | 按边界键占候选比例扫描 | 回查成本何时触发重建 |
| 历史状态容量 | 不同 `alpha*h` 或摘要预算 | 状态空间与准确率的权衡 |

必看指标：

```text
metadata_validation_time
residual_scan_time
stable_lookup_time
boundary_lookup_time
plan_synthesis_time
control_time
history_update_background_time
history_state_build_time
history_state_compress_time
history_state_persist_time
history_publish_latency
history_update_queue_wait
new_history_visible_ratio
scanned_partitions
scanned_bytes
history_state_size
selected_mode
full_reuse_ratio
partial_reuse_ratio
rebuild_ratio
modeled_end_to_end_time
```

公平性要求：

- `ours_validated_reuse` 必须根据查询签名、分片版本、历史摘要完整性和边界不确定性自动选择 `selected_mode=FULL_REUSE/PARTIAL_REUSE/REBUILD`。
- `ours_no_history_reuse` 每轮完整重新统计和规划，用作 forced rebuild 控制成本基线。
- `selected_mode=FULL_REUSE/PARTIAL_REUSE/REBUILD` 均必须执行完整 join，不能缓存连接结果或输入中间结果。
- 在满足 `FULL_REUSE` 条件的输入上，可以报告 `selected_mode=FULL_REUSE` 的结果；在满足 `PARTIAL_REUSE` 条件的输入上，可以报告 `selected_mode=PARTIAL_REUSE` 的结果，但不能把二者写成可任意强制选择的算法。
- `FULL_REUSE` 只能省去重复统计和规划，不能复用上一轮物理任务编号、数据区间或节点放置结果。
- `PARTIAL_REUSE` 必须扫描残余分片，不能只检查历史显式键。
- 连续查询序列中，下一轮只能读取已经发布的 committed history version；若上一轮后台历史更新尚未完成或发布失败，则按上一版历史状态、低置信度状态或 `REBUILD` 处理，不能使用半写入状态。
- `reuse_without_validation` 只用于展示错误设计风险，不进入正式性能结论。
- 随着 `rho`、分片版本变化率或新热点比例升高，`partial_reuse_ratio` 和 `rebuild_ratio` 应上升；不能强行复用过期历史状态。

理论解释规则：E7 的主结论是机制1降低重复或相似查询下的统计与规划成本，而不是证明任意随机 workload 加速。正式结果必须随 `rho` 画退化曲线；固定 case 的加速比只能作为默认配置结果。

### 可选诊断：AQE behavior diagnostics

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 运行时统计驱动的 join 策略重选择能解决哪些问题，不能解决哪些问题 |
| 方法矩阵 | `aqe_join_reselection`, `ours`，必要时加入 `rdma_onesize` |
| 固定条件 | 默认 `80w` workload，并补充 HH-dominant 高倾斜 case |
| 主指标 | `modeled_end_to_end_time`, `selected_join_strategy`, `strategy_reselection_count`, `reoptimized_stage_count`, `max_worker_time`, `load_cv`, `shuffle_bytes`, `output_size` |

AQE-style baseline 可以减少错误物理策略带来的数据移动或 stage 代价，但它不导出 key-level 两侧热点状态组合，也不执行剩余尾部任务原子接管。因此，该诊断只用于解释 AQE 行为边界，不作为新的主 claim。

## 4. 可选诊断：RDMA cost sensitivity

| 项目 | 内容 |
| --- | --- |
| 实验问题 | RDMA 成本模型参数变化是否影响方法边界 |
| 唯一变化变量 | `network_cost_scale` |
| 建议取值 | `0.25`, `0.5`, `1.0`, `2.0`, `4.0` |
| 固定条件 | 默认 `80w` workload，`record_size=64B`, `parallelism=16` |
| 方法矩阵 | 主方法集 |
| 主指标 | `modeled_end_to_end_time`, `simulated_network_time`, `remote_read_bytes`, `shuffle_bytes`, `rdma_operation_count` |

该实验不属于正式主矩阵。正式主实验默认所有方法都使用统一 RDMA 通信环境，`network_cost_scale=1.0` 固定不变。只有当需要解释某个 baseline 对复制、广播或远端操作特别敏感时，才把本实验作为附录或诊断实验补充。

## 5. 输出与验收

每个 `experiment_id` 必须生成：

| 文件 | 内容 |
| --- | --- |
| `runs.csv` | 每个 workload seed、运行 seed、每个 method 的原始运行记录；成本模型实验默认以 workload seed 为统计单元 |
| `descriptors.jsonl` | 每次运行的 workload descriptor |
| `summary.csv` | 聚合后的原始点、均值、标准差、中位数、几何平均加速比和失败状态；只有 seed 数量或分层 bootstrap 足够时才报告 95% CI |
| `notes.md` | 异常、失败、降级、解释规则 |

正式结果验收规则：

- 主表只接受完成 baseline qualification 且 `result_role=main` 的 method。
- E4 结论只来自机制2方法矩阵，不得用普通整体性能替代候选补全和边界回查证据。
- E5 消融必须按 E5A/E5B/E5C 的触发场景解释，不得用单一默认 case 汇总所有消融。
- E6 运行时扰动结论只来自 E6，不得用纯静态 workload 证明执行期纠偏。
- E7 历史复用结论只来自 E7，不得用随机 workload 证明普适加速。
- `network_cost_scale` 在正式主实验中固定为 `1.0`；RDMA cost sensitivity 只作为可选诊断或附录结果。
- E0 必须先完成并生成 `rdma_cost_model_version`，否则主实验只能标记为 pilot。
- `Hash-Join` 必须进入整体性能、倾斜强度和扩展性实验；它用于成本锚点，不得只在低倾斜附录中出现。
- `Full-SkewJoin` 必须以 FSJ 静态倾斜处理版本进入主 baseline 表；它用于静态倾斜处理参照。
- `Topology-aware` baseline 必须先补齐具体论文或算法名称、任务分配规则和 RDMA 成本模型适配方式；若仍为 `pending qualification`，不得进入正式主结论表。
- `AQE-style` baseline 必须记录 `selected_join_strategy`、`strategy_reselection_count` 和 `reoptimized_stage_count`；不得把它写成 key-level skew load balancing 方法。
- 正确性检查必须包含 `output_size`、`output_checksum`、`per_key_output_count`、`per_key_output_checksum`、`pair_checksum_sum`、`pair_checksum_xor`、`per_key_pair_checksum_sum` 和 `per_key_pair_checksum_xor`；pair checksum 必须基于稳定唯一 `tuple_id` 和两组独立顺序无关哈希聚合。若任意方法 per-key 输出或 pair checksum 不一致，即使总 `output_size` 相同，也不得进入性能结论。
- `oracle`、`diagnostic`、`pending`、`pilot` 和 `system_reference` 不进入正式结论。
