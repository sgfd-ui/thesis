# 对比实验执行协议

本文档是当前实验协议，用于指导正式实验运行、结果汇总和后续论文第 8 节写作。实验目标不是罗列运行脚本，而是回答本文三阶段方法是否真正改善 RDMA 环境下连接键倾斜的分布式等值连接性能。

本文所有方法默认运行在同一 RDMA 通信成本模型下。部分 baseline 并非 RDMA 原生算法，但它们明确面向分布式 join、parallel join 或 skewed hash join 的数据分配与负载均衡问题。统一通信环境后，实验比较的是 join 分配、热点处理、执行期纠偏和跨轮复用逻辑，而不是比较 TCP 与 RDMA 协议差异。因此，RDMA 通信环境是全局前提，不作为主实验中的独立问题反复验证。

## 0. 全局实验前提

| 项目 | 默认设置 |
| --- | --- |
| seeds | `7,13,23` |
| 默认总输入 | `n_R+n_S=800,000` |
| 默认表大小 | `n_R=400,000`, `n_S=400,000` |
| 默认表比例 | `1:1` |
| 默认记录宽度 | `64B` |
| 默认并行度 | `parallelism=16` |
| 默认热点模型 | Zipf / hot-key mixed synthetic workload |
| 通信模型 | 使用统一 RDMA transport cost model |
| 默认通信倍率 | `network_cost_scale=1.0`，主实验固定 |
| 默认拓扑 | `topology_profile=two_rack_rdma`，且 `same_rack_cost < cross_rack_cost` |
| 主指标 | `end_to_end_time = sum(stage_time) + control_time` |

辅助指标包括 `max_worker_time`、`avg_worker_time`、`tail_ratio`、`load_cv`、`total_simulated_work`、`simulated_network_time`、`shuffle_bytes`、`remote_read_bytes`、`detection_time`、`validation_time`、`routing_time`、`stolen_task_count`、`rdma_operation_count`、`join_work`、`output_size` 和 per-key 正确性指标。

分布式完成时间按墙钟时间定义，而不是把全局计算量和全局通信量简单相加。对每个执行阶段，先计算 worker 内部时间：

```text
worker_stage_time_i =
    compute_time_i
  + simulated_network_time_i
  + rdma_operation_time_i
  + output_time_i
```

阶段时间由最慢 worker 决定：

```text
stage_time = max_i(worker_stage_time_i)
end_to_end_time = sum(stage_time) + control_time
```

辅助总代价只用于解释资源消耗构成，记为：

```text
total_simulated_work =
    total_compute_time
  + total_simulated_network_time
  + total_rdma_operation_time
  + total_output_time
```

其中，worker 内部的计算、通信、RDMA 操作和输出代价只用于解释阶段时间构成。`total_simulated_work` 不作为主性能指标，只用于观察总模拟工作量。主性能结论必须基于 `end_to_end_time`、`max_worker_time`、`tail_ratio` 和 `load_cv`，避免把热点 straggler 被平均后的总代价误写成墙钟时间。

核心派生指标统一按以下公式计算：

```text
speedup(baseline, ours) = T_baseline / T_ours
mean_speedup = (1 / |C|) * sum_{c in C} T_baseline(c) / T_ours(c)
load_cv = std(worker_time_i) / mean(worker_time_i)
tail_ratio = max_worker_time / avg_worker_time
normalized_time_by_work = end_to_end_time / join_work
normalized_time_by_output = end_to_end_time / output_size
```

其中 `C` 表示参与同一组比较的 case 集合，`T` 默认使用 `end_to_end_time`。`local_join_time` 必须明确统计口径：主文默认解释为关键路径或 stage 聚合后的 local join 时间；若实验代码输出的是平均值或最大值，必须在 descriptor 中记录 `local_join_time_type=mean/max/stage_critical`，避免把平均本地连接开销误写成关键路径开销。

所有正式实验必须检查输出正确性。只比较 `output_size` 不足以排除“漏掉一部分 pair、重复另一部分 pair”的情况，因此还必须保存 `output_checksum`、`per_key_output_count` 和 `per_key_output_checksum`。如果实验不物化全部 join 结果，则至少保存 `sum_k f_R(k)f_S(k)` 与 `per_key_join_work` 作为按 key 的正确性代理。任意 baseline 或消融方法如果 per-key 输出不一致，该 case 只能标记为 invalid，不得进入性能结论。

## 1. 主方法集与 baseline qualification

### 1.1 主 baseline

| 实验名 | method_id | 来源 | 资格与角色 |
| --- | --- | --- | --- |
| AMJoin-style | `amjoin_style` | TODS 2025, CCF-A | 分布式等值连接负载均衡主 baseline；使用热点键预分配、双侧热点拆分和单侧热点广播/复制思想 |
| RDMA-OneSize | `rdma_onesize` | DASFAA 2024, CCF-B | shared-nothing skewed hash join dispatcher baseline；在 RDMA 成本模型下选择 GraHJ/PRPD/PnR 风格策略 |
| Topology-aware Parallel Join | `topology_aware_parallel_join` | PODS 2024, CCF-B | topology-aware parallel join data assignment baseline；强调执行位置和通信代价共同影响 join 分配 |
| AQE-style Join Re-selection | `aqe_join_reselection` | PVLDB 2024, CCF-A | runtime statistics driven join strategy re-selection baseline；根据运行时统计重选 broadcast/shuffle/skew-split 类策略 |
| Ours-Full | `ours` | 本文 | 热点感知预分布 + 分布式热点识别 + RDMA 运行时再平衡 |

正式主方法集固定为：

```text
amjoin_style,
rdma_onesize,
topology_aware_parallel_join,
aqe_join_reselection,
ours
```

主 baseline 的筛选标准是：论文必须明确面向分布式 join、parallel join、skewed hash join 或运行时 join 执行优化问题，并且原则上来自近 2-3 年 B 会以上正式论文。`AQE-style Join Re-selection` 不是 key-level skew load balancing baseline，而是运行时统计驱动的 join 策略重选择对照，用于回答“只在 join strategy 层面重优化是否足够”。`NOCAP` 虽然是近年 A 会 join partitioning 工作，但它主要面向 storage-based / PK-FK join 的 correlation-aware partitioning，不是 shared-nothing 分布式 join 负载均衡算法，因此不进入主 baseline。

### 1.2 不进入主 baseline 的方法

以下方法不进入主表：

```text
zero_sided_rdma_style,
modularis_rdma_join,
nocap_style_partitioning,
pystachio_chunked_rdma,
bala_join_style,
pactree_style_placement,
reljoin_style_selector
```

原因如下。`Zero-sided RDMA` 和 `Modularis` 是 RDMA shuffle/query execution 线索，不是 join-key skew load balancing 主算法。`NOCAP` 属于 correlation-aware partitioning / storage-based join 路线，不是本文所需的分布式 join 负载均衡 baseline。`PystachIO` 和 `BalaJoin` 当前本地标注为 arXiv-only 或候补，不作为正式主 baseline。`PAC-Tree` 与 `RelJoin` 和 distributed join 优化相关，但 CCF-B+ 状态未确认，适合作为相关工作或扩展对照。`Patch-Based Shuffling` 技术上与 skewed repartition join 相关，但本地标注为 NonCCF IEEE Access，因此可放入附录或诊断，不进入主表。

### 1.3 消融方法

| method_id | 目的 |
| --- | --- |
| `ours` | 完整三阶段方法 |
| `ours_no_hotspot_predistribution` | 去掉热点感知预分布，验证跨轮控制信息复用贡献 |
| `ours_no_global_hot_key_verification` | 去掉全局热点验证，验证分布式热点识别贡献 |
| `ours_no_rebalance` | 去掉 RDMA 运行时再平衡，验证 QueueState/OSP 贡献 |
| `ours_no_bounded_probing` | 去掉受限候选探测，验证低控制面 RDMA 探测策略 |

消融只进入机制分析，不进入主 baseline 表。

## 2. Workload descriptor

每次运行必须保存完整 workload descriptor。至少包含：

```text
experiment_id
method_id
seed
n_R
n_S
table_ratio
record_size
key_domain
skew_param
hot_key_count
hot_overlap
quadrant_mix
parallelism
network_cost_scale
topology_profile
same_rack_cost
cross_rack_cost
rdma_cost_model_version
selected_join_strategy
strategy_reselection_count
reoptimized_stage_count
reuse_count
hotset_drift
query_template_change_rate
new_hotkey_ratio
validation_sample_rate
local_join_time_type
slow_worker_ratio
slowdown_factor
task_cost_estimation_error
hotspot_location_drift
microtask_cost_variance
stolen_work
cas_failure_count
snapshot_refresh_count
control_time
local_rank_gap
oversampling_factor
candidate_boundary_width
global_hotkey_mass
precision_at_k
recall_at_k
candidate_count
verification_time
misclassified_hot_key_count
hotkey_misclassification_rate
full_hit_ratio
partial_hit_ratio
miss_ratio
page_warmup_policy
join_work
output_size
output_checksum
per_key_output_count
per_key_output_checksum
per_key_join_work
```

其中 `join_work` 用于解释不同 key 频率组合带来的潜在匹配规模，`topology_profile` 用于保证 Topology-aware baseline 有非均匀通信环境。`selected_join_strategy`、`strategy_reselection_count` 和 `reoptimized_stage_count` 用于记录 AQE-style baseline 在运行时做出的 join 策略调整。`cache_status` 是代码字段，论文中统一解释为“预分布命中状态”；`cache_hit_rate` 不作为 workload 输入变量，而是由第三机制验证逻辑自然产生并记录为输出指标。实验解释不能只看输入大小或 shuffle 字节数。

### 2.1 Workload generator

合成 workload 按三步生成。第一步，生成左右关系的 key 频率分布 $f_R(k)$ 和 $f_S(k)$。默认使用 Zipf / hot-key mixed 生成器：`skew_param` 控制频率集中程度，`hot_key_count` 控制候选热点数量，`key_domain` 控制整体 key 空间。第二步，根据 `hot_overlap` 和 `quadrant_mix` 构造 HH/HC/CH/CC 连接语义。`hot_overlap=0.5` 表示左右热点集合中约一半 key 重叠；`hh_dominant` 增加双侧热点占比，`cross_dominant` 增加 HC/CH 单侧热点占比。第三步，根据 `record_size` 生成 payload，并根据 `table_ratio` 分配 $|R|$ 与 $|S|$。

对于 E1B，生成器必须使用受控 join work 模式。该模式先固定目标 `join_work=sum_k f_R(k)f_S(k)`，再移动热点质量在 key 之间的集中程度，使不同 `skew_param` 下的 `join_work` 近似一致。允许存在小幅误差，但每个 case 必须记录实际 `join_work` 并在汇总时报告误差范围。

## 3. 实验矩阵

每个实验小节都遵循同一写法：问题、唯一变化变量、固定条件、方法矩阵、指标和理论解释规则。除被明确声明的变量外，其他参数必须固定。

### E0. Cost model calibration

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 如何校准所有方法共用的计算、shuffle 和 RDMA 单位成本 |
| 唯一变化变量 | microbenchmark 类型 |
| 校准对象 | local compute、join output、shuffle、RDMA READ、RDMA WRITE、RDMA atomic、fixed RDMA op |
| 输出文件 | `rdma_cost_model.json`, `cost_model_calibration.csv`, `notes.md` |
| 后续用途 | 所有正式 baseline 和 Ours 共用同一套成本模型 |

必须校准并记录以下参数：

```text
local_compute_cost_per_tuple
join_output_cost_per_pair
shuffle_cost_per_byte
rdma_read_cost_per_byte
rdma_write_cost_per_byte
rdma_atomic_cost_per_op
rdma_fixed_op_cost
```

E0 的作用是把 `compute_time` 和 `simulated_network_time` 的来源说清楚。正式主实验固定 `network_cost_scale=1.0`，并使用 E0 产出的 `rdma_cost_model_version`。如果后续需要改变 RDMA 成本倍率，只能进入可选诊断或附录，不进入主实验矩阵。

默认成本模型采用非重叠保守估计，即每个 worker 的计算、数据传输、RDMA 操作和输出代价按串行方式累加。若实现支持计算与通信重叠，可以在诊断实验中引入 `overlap_factor` 做敏感性分析；正式主实验固定使用同一非重叠模型，以保证 baseline 之间可比。

### E1. Skew strength

E1 拆成两个版本。E1A 观察自然 Zipf 倾斜下的真实性能变化；E1B 控制总体 `join_work` 近似不变，只改变热点集中程度，用于更直接验证负载均衡能力。

#### E1A. Natural skew sweep

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 真实 Zipf 倾斜增强时，Ours 是否比静态预分配和 dispatcher 更能降低尾部节点时间 |
| 唯一变化变量 | `skew_param` |
| 建议取值 | `0.0,0.5,0.8,1.0,1.2` |
| 固定条件 | `n_R+n_S=800,000`, `table_ratio=1:1`, `record_size=64B`, `parallelism=16`, `hot_overlap=1.0` |
| 方法矩阵 | 4 个外部 baseline + `ours` |
| 主指标 | `end_to_end_time`, `output_size`, `join_work`, `max_worker_time`, `avg_worker_time`, `tail_ratio`, `load_cv`, `normalized_time_by_output`, `normalized_time_by_work` |

#### E1B. Controlled join-work skew sweep

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 当总体连接工作量近似固定时，热点集中程度变化是否仍导致尾部节点差异 |
| 唯一变化变量 | `skew_param` |
| 建议取值 | `0.0,0.5,0.8,1.0,1.2` |
| 固定条件 | `n_R+n_S=800,000`, `table_ratio=1:1`, `record_size=64B`, `parallelism=16`, `target_join_work` 固定 |
| 方法矩阵 | 4 个外部 baseline + `ours` |
| 主指标 | `end_to_end_time`, `join_work`, `join_work_error`, `max_worker_time`, `avg_worker_time`, `tail_ratio`, `load_cv`, `normalized_time_by_work` |

派生指标固定为：

```text
tail_ratio = max_worker_time / avg_worker_time
normalized_time_by_output = end_to_end_time / output_size
normalized_time_by_work = end_to_end_time / join_work
```

理论解释规则：E1A 说明真实倾斜 workload 下谁更快，但不能单独证明负载均衡能力，因为 `skew_param` 可能同时放大 `output_size` 和 `join_work`。E1B 是负载均衡能力的关键证据：当 `join_work` 近似固定时，如果 Ours 仍能降低 `tail_ratio`、`load_cv` 和 `max_worker_time`，才能说明收益来自更好的工作分布和执行期纠偏，而不是任务本身变小。

### E2. Workload shape

E2 用于回答不同输入形态下方法边界是否稳定。每个子实验必须单独汇总，不得把多个变量混合成一个结论。

#### E2A. Table ratio

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 大小表比例变化时，热点处理策略和广播/复制代价如何影响性能 |
| 唯一变化变量 | `table_ratio=n_R:n_S` |
| 建议取值 | `1:1,1:2,1:4,1:8` |
| 固定条件 | `n_R+n_S=800,000`, `record_size=64B`, `skew_param=0.65`, `parallelism=16` |
| 方法矩阵 | 主方法集 |
| 主指标 | `end_to_end_time`, `simulated_network_time`, `max_worker_time`, `output_size` |

理论解释规则：AMJoin-style 对 Small-Large 场景有针对性策略；OneSize 会根据成本选择子策略。Ours 的优势应来自运行前热点语义和执行期再平衡的组合，而不是盲目广播。

#### E2B. Record size

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 记录宽度增大时，通信成本是否改变各方法优势边界 |
| 唯一变化变量 | `record_size` |
| 建议取值 | `16B,64B,256B,1KB` |
| 固定条件 | `n_R=n_S=400,000`, `skew_param=0.65`, `parallelism=16` |
| 方法矩阵 | 主方法集 |
| 主指标 | `end_to_end_time`, `simulated_network_time`, `shuffle_bytes`, `output_size` |

理论解释规则：记录越大，复制和广播的代价越高。若某 baseline 依赖大量复制，其网络时间应更敏感。Ours 需要用 RDMA 成本模型解释再平衡收益是否被额外远端操作抵消。

#### E2C. Hotspot structure

| 子实验 | 实验问题 | 唯一变化变量 | 建议取值 |
| --- | --- | --- | --- |
| Hot key count | 热点数量增加时，控制面和路由构造成本是否上升 | `hot_key_count` | `16,64,256,1024` |
| Hot overlap | 左右热点重叠程度变化时，双侧热点和单侧热点策略边界如何变化 | `hot_overlap` | `0.0,0.5,1.0` |
| Quadrant mix | HH/HC/CH/CC 占比变化时，不同方法是否偏向特定连接语义 | `quadrant_mix` | `balanced,hh_dominant,cross_dominant` |

固定条件为 `n_R+n_S=800,000`、`table_ratio=1:1`、`record_size=64B`、`parallelism=16`。主指标为 `end_to_end_time`、`max_worker_time`、`load_cv`、`detection_time` 和 `routing_time`。

理论解释规则：HH 主导时，热点键的局部匹配规模更容易放大；HC/CH 主导时，广播或复制策略更关键；CC 占比较高时，运行时剩余任务覆盖范围会影响尾部表现。`hot_key_count=10000` 只作为控制面压力诊断，不能进入主实验结论；`100000` 不作为主实验取值，因为在 `80w` 默认输入下它更接近高基数普通键场景，而不是热点键场景。

### E3. Scale and parallelism

#### E3A. Input scale

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 输入规模增大时，方法是否保持可扩展性 |
| 唯一变化变量 | `scale` |
| 建议取值 | `1x,2x,4x,8x,16x,32x,64x,128x` |
| 基准规模 | `1x = 800,000` total records |
| 固定条件 | `table_ratio=1:1`, `record_size=64B`, `parallelism=16` |
| 方法矩阵 | 主方法集 |
| 主指标 | `end_to_end_time`, `max_worker_time`, `load_cv`, `failure_status` |

执行规则：按升序运行；第一次 failure、timeout 或 OOM 后停止更大规模，并记录最后成功规模。

#### E3B. Strong scaling

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 固定输入规模时，增加并行度是否降低尾部完成时间 |
| 唯一变化变量 | `parallelism` |
| 建议取值 | `4,8,16,32` |
| 固定条件 | `n_R+n_S=800,000`, `table_ratio=1:1`, `record_size=64B` |
| 方法矩阵 | 主方法集 |
| 主指标 | `end_to_end_time`, `max_worker_time`, `load_cv`, `worker_utilization` |

理论解释规则：如果方法只做静态分配，增加并行度后热点键仍可能压在少数节点上。Ours 应通过剩余任务让渡提高高并行度下的利用率。

#### E3C. Weak scaling

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 每线程输入规模固定时，方法是否随并行度保持稳定 |
| 唯一变化变量 | `parallelism` 与总输入同步放大 |
| 建议取值 | `4,8,16,32` |
| 固定条件 | 每线程输入规模固定，`record_size=64B` |
| 方法矩阵 | 主方法集 |
| 主指标 | `end_to_end_time`, `max_worker_time`, `load_cv`, `simulated_network_time` |

理论解释规则：弱扩展主要检验控制面、网络成本和任务再平衡是否随节点数放大而失控。

### E4. FastLock-Verify global-hotspot validation

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 候选发现与受限验证是否能识别“局部不显著、全局累积后成为热点”的连接键 |
| 构造目标 | 某些 key 在每个节点的局部 top-k 之外，但跨节点合并后进入全局热点集合 |
| 变量组 | `local_rank_gap`, `oversampling_factor`, `candidate_boundary_width`, `global_hotkey_mass` |
| 方法矩阵 | `ours`, `ours_no_global_hot_key_verification`, 可选 `local_topk_only` |
| 固定条件 | 默认 `80w` workload，`parallelism=16`，中高倾斜输入 |
| 主指标 | `precision@k`, `recall@k`, `candidate_count`, `verification_time`, `misclassified_hot_key_count`, `load_cv`, `max_worker_time` |

子实验规则：

| 子实验 | 唯一变化变量 | 建议取值 | 固定条件 |
| --- | --- | --- | --- |
| Local rank gap | `local_rank_gap` | `0,8,32,128` | 全局热点质量固定 |
| Oversampling | `oversampling_factor` | `1,2,4,8` | 局部候选边界固定 |
| Boundary width | `candidate_boundary_width` | `0%,5%,10%,25%` | 过采样因子固定 |
| Global hot-key mass | `global_hotkey_mass` | `low,medium,high` | 局部 rank gap 固定 |

理论解释规则：去掉全局验证不应影响 join 结果正确性；只要热点路径和普通路径都保持完备性与互斥性，误分类最多导致性能退化。E4 关注的是热点召回率、热点语义可靠性、路由质量和尾部时间。如果 `ours_no_global_hot_key_verification` 在默认负载下更快，但在局部摘要误判场景中出现更低 `recall@k`、更多 `misclassified_hot_key_count` 或更高 `load_cv/max_worker_time`，则说明全局验证的价值是保护分布式热点分类可靠性，而不是保护连接谓词本身。

### E5. Ablation and mechanism analysis

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 三阶段机制分别贡献什么 |
| 唯一变化变量 | `method_id` |
| 方法矩阵 | `ours`, `ours_no_hotspot_predistribution`, `ours_no_global_hot_key_verification`, `ours_no_rebalance`, `ours_no_bounded_probing` |
| 固定条件 | 默认 `80w` workload，并补充一个高倾斜 case |
| 主指标 | `end_to_end_time`, `detection_time`, `validation_time`, `routing_time`, `max_worker_time`, `load_cv`, `rdma_operation_count` |

消融矩阵固定如下：

| 方法 | 去掉的机制 | 回答的问题 | 必看指标 | 预期解释边界 |
| --- | --- | --- | --- | --- |
| `ours` | 无 | 完整三阶段路径的参照 | 全部指标 | 作为所有消融的归一化基线 |
| `ours_no_hotspot_predistribution` | 热点感知预分布 | 跨轮稳定热点模式是否能减少重复控制面开销 | `validation_time`, `detection_time`, `routing_time`, `end_to_end_time` | 只在 E7 重复或相似连接 workload 中要求体现收益 |
| `ours_no_global_hot_key_verification` | 全局热点验证 | 仅依赖局部摘要是否会误判全局热点语义 | `precision@k`, `recall@k`, `load_cv`, `max_worker_time`, `detection_time` | 若输出不一致，说明消融实现破坏了路由完备性或互斥性；正常边界是热点误分类和负载退化 |
| `ours_no_rebalance` | RDMA 运行时再平衡 | 静态预分配后是否仍存在执行期尾部任务 | `max_worker_time`, `load_cv`, `stolen_task_count`, `end_to_end_time` | 高倾斜、输出集中或 E6 运行时扰动场景应重点观察尾部节点时间 |
| `ours_no_bounded_probing` | 受限候选探测 | 不限制远端探测是否增加 RDMA 控制面成本 | `rdma_operation_count`, `routing_time`, `end_to_end_time` | 若性能接近但操作数显著上升，说明 bounded probing 的价值是控制面可扩展性 |

E5 至少运行两类 case。第一类是默认 `80w`、中等倾斜 case，用于观察普通场景下各机制的额外开销。第二类是高倾斜或 HH-dominant case，用于观察热点识别和 RDMA 运行时再平衡在长尾场景中的贡献。第三机制的主收益应以 E7 为准，E5 只报告它在默认 case 下是否引入额外开销。

理论解释规则：去掉热点感知预分布应主要增加重复查询中的控制面开销；去掉全局热点验证应影响热点语义可靠性和负载分布；去掉 RDMA 再平衡应提高尾部节点时间；去掉 bounded probing 应增加 RDMA 控制面操作数。消融结论必须与对应指标一致，不能只用端到端时间解释所有机制。

### E6. Runtime perturbation and residual-tail rebalancing

| 项目 | 内容 |
| --- | --- |
| 实验问题 | RDMA 运行时再平衡是否能处理静态分配之后出现的运行时变化和剩余长尾 |
| 变量组 | `slow_worker_ratio`, `slowdown_factor`, `task_cost_estimation_error`, `hotspot_location_drift`, `microtask_cost_variance` |
| 方法矩阵 | 主方法集 + `ours_no_rebalance` |
| 固定条件 | 默认 `80w`, `parallelism=16`, 中高倾斜 workload，除当前变量外其他参数固定 |
| 主指标 | `end_to_end_time`, `max_worker_time`, `load_cv`, `stolen_task_count`, `stolen_work`, `cas_failure_count`, `snapshot_refresh_count`, `control_time`, `rdma_operation_count` |

子实验规则：

| 子实验 | 唯一变化变量 | 建议取值 | 固定条件 |
| --- | --- | --- | --- |
| Slow-worker ratio | `slow_worker_ratio` | `0%,6.25%,12.5%,25%` | `slowdown_factor=2x` |
| Slowdown factor | `slowdown_factor` | `1.5x,2x,4x` | `slow_worker_ratio=12.5%` |
| Cost estimation error | `task_cost_estimation_error` | `0%,10%,25%,50%` | 慢节点比例固定 |
| Hotspot location drift | `hotspot_location_drift` | `static,mild,severe` | 估计误差固定 |
| Microtask variance | `microtask_cost_variance` | `low,medium,high` | 中高倾斜输入 |

理论解释规则：如果 `Topology-aware` 或 `AQE-style` 在静态场景更快，但在运行时扰动下 `max_worker_time/load_cv` 上升，而 `Ours` 通过 `stolen_work`、快照刷新和有限 CAS 失败降低尾部时间，则 E6 支撑本文“运行时纠偏”的核心主张。若 `ours_no_rebalance` 与 `ours` 接近，说明该扰动不足以制造执行期剩余长尾；需要提高慢节点比例、减速倍率或微任务代价方差。

### E7. Repeated or similar join workload

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 重复或相似连接查询中，热点感知预分布是否能减少重复热点识别和路由构造 |
| 变量组 | `reuse_count`, `hotset_drift`, `query_template_change_rate`, `new_hotkey_ratio`, `validation_sample_rate` |
| 方法矩阵 | `ours`, `ours_no_hotspot_predistribution`, `ours_hot_reuse_plan_no_validation` |
| 主指标 | `end_to_end_time`, `detection_time`, `validation_time`, `routing_time`, `validation_overhead_ratio`, `cache_status`, `full_hit_ratio`, `partial_hit_ratio`, `miss_ratio`, `cache_hit_rate`, `cache_fallback_rate`, `hotkey_misclassification_rate`, `join_work`, `output_size`, `output_checksum` |

子实验规则：

| 子实验 | 唯一变化变量 | 建议取值 | 固定条件 |
| --- | --- | --- | --- |
| Reuse count | `reuse_count` | `1,2,4,8,16` | 热点集合稳定 |
| Hotset drift | `hotset_drift` | `0,0.1,0.25,0.5,1.0` | reuse_count 固定 |
| Query template change | `query_template_change_rate` | `0.0,0.1,0.25,0.5` | 热点漂移低 |
| New hot-key ratio | `new_hotkey_ratio` | `0.0,0.05,0.1,0.25` | reuse_count 固定 |
| Validation sample rate | `validation_sample_rate` | `0.01,0.03,0.05,0.1` | 重复查询固定 |

公平性要求：

- 预分布机制不得缓存连接结果或输入中间结果。
- `FULL_HIT`、`PARTIAL_HIT` 和 `MISS` 的 `join_work/output_size/per_key_join_work` 必须一致，或在结果中报告差异。
- 所有 baseline 使用相同的数据页预热策略，并记录 `page_warmup_policy`。
- `FULL_HIT` 时间必须包含完整 join 执行时间，只能省去热点检测和路由构造。
- `cache_status` 是代码字段，论文中写作“预分布命中状态”。

理论解释规则：第三机制只应在稳定重复查询中体现收益。`FULL_HIT` 只能证明稳定热点模式下的控制面复用；随着 `hotset_drift`、`query_template_change_rate` 或 `new_hotkey_ratio` 升高，应观察到 `PARTIAL_HIT/MISS` 比例上升，而不是强行复用历史计划。`cache_hit_rate` 和 `cache_fallback_rate` 是验证逻辑产生的输出指标，不能作为输入变量直接指定。如果 no-validation 版本更快但在 drift 场景中产生热点误分类或明显退化，只能说明验证边界必要。

### 可选诊断：AQE behavior diagnostics

| 项目 | 内容 |
| --- | --- |
| 实验问题 | 运行时统计驱动的 join 策略重选择在连接键倾斜场景中能解决哪些问题，不能解决哪些问题 |
| 唯一变化变量 | `method_id` 或 AQE 触发阈值 |
| 方法矩阵 | `aqe_join_reselection`, `ours`，必要时加入 `rdma_onesize` 作为策略选择对照 |
| 固定条件 | 默认 `80w` workload，并补充一个 HH-dominant 高倾斜 case |
| 主指标 | `end_to_end_time`, `selected_join_strategy`, `strategy_reselection_count`, `reoptimized_stage_count`, `max_worker_time`, `load_cv`, `shuffle_bytes`, `output_size` |

理论解释规则：AQE-style baseline 的价值是利用运行时统计重选 broadcast、shuffle 或 skew-split 类 join 策略。它可以减少错误物理策略带来的数据移动或 stage 代价，但它不导出 HH/HC/CH/CC 这类 key-level 连接语义，也不执行 RDMA 尾部任务让渡。因此，该诊断只用于解释 AQE 的行为边界，不作为新的主 claim。如果 AQE 改善了部分 workload 的 `shuffle_bytes` 或 `selected_join_strategy`，但 `max_worker_time` 和 `load_cv` 仍然偏高，应解释为 join strategy 级重优化不能完全替代 key-level 热点识别和执行期再平衡。

## 4. 可选诊断：RDMA cost sensitivity

| 项目 | 内容 |
| --- | --- |
| 实验问题 | RDMA 成本模型参数变化是否影响方法边界 |
| 唯一变化变量 | `network_cost_scale` |
| 建议取值 | `0.25,0.5,1.0,2.0,4.0` |
| 固定条件 | 默认 `80w` workload，`record_size=64B`, `parallelism=16` |
| 方法矩阵 | 主方法集 |
| 主指标 | `end_to_end_time`, `simulated_network_time`, `remote_read_bytes`, `shuffle_bytes`, `rdma_operation_count` |

该实验不属于正式主矩阵。正式主实验默认所有方法都使用统一 RDMA 通信环境，`network_cost_scale=1.0` 固定不变。只有当需要解释某个 baseline 对复制、广播或远端操作特别敏感时，才把本实验作为附录或诊断实验补充。

理论解释规则：通信成本降低时，更多数据移动可能变得可接受，但 join-key skew 造成的局部计算和输出长尾仍然存在。通信成本升高时，过度广播或复制的 baseline 应更敏感。

## 5. 输出与验收

每个 experiment_id 必须生成：

| 文件 | 内容 |
| --- | --- |
| `runs.csv` | 每个 seed、每个 method 的原始运行记录 |
| `descriptors.jsonl` | 每次运行的 workload descriptor |
| `summary.csv` | 聚合后的均值、标准差和失败状态 |
| `notes.md` | 异常、失败、降级、解释规则 |

正式结果验收：

- 主表只接受属于主方法集的 method。
- FastLock-Verify 直接验证只接受 E4 方法集。
- 消融只接受 E5 方法集。
- 运行时扰动结论只来自 E6，不得用纯静态 workload 证明执行期纠偏。
- 第三机制结论只来自 E7，不得用随机 workload 证明普适加速。
- `network_cost_scale` 在正式主实验中固定为 `1.0`；RDMA cost sensitivity 只作为可选诊断或附录结果。
- E0 必须先完成并生成 `rdma_cost_model_version`，否则主实验只能标记为 pilot。
- Topology-aware baseline 必须在 `topology_profile=two_rack_rdma` 且 `same_rack_cost < cross_rack_cost` 的非均匀拓扑下进入主表；均匀拓扑只作为诊断对照。
- AQE-style baseline 必须记录 `selected_join_strategy`、`strategy_reselection_count` 和 `reoptimized_stage_count`；不得把它写成 key-level skew load balancing 方法。
- 正确性检查必须包含 `output_size`、`output_checksum`、`per_key_output_count` 和 `per_key_output_checksum`。如果不物化全部结果，至少保存 `sum_k f_R(k)f_S(k)` 和 `per_key_join_work`。
- 任意方法若 per-key 输出不一致，即使总 `output_size` 相同，也不得进入性能结论。
- `pilot`、`diagnostic`、`system_reference` 和 `pending` 不进入正式结论。
- `zero_sided_rdma_style`、`modularis_rdma_join`、`nocap_style_partitioning`、`pystachio_chunked_rdma`、`bala_join_style`、`pactree_style_placement`、`reljoin_style_selector` 不进入默认主表。
