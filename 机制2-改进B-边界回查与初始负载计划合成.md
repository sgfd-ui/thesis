# 机制2改进B：边界回查与初始负载计划合成

## 1. 问题定位

残余范围双侧候选识别只能得到候选键的频率估计或频率区间。要把这些统计转化为可靠的初始负载计划，还需要判断哪些键是明确热点，哪些键是明确普通键，哪些键因摘要误差或边界接近而需要精确回查。

本改进固定第 6 章后半部分的职责：从频率区间出发形成两侧热点状态组合、内在连接工作量和候选策略代价，最终合成当前轮完整初始负载计划。

## 2. 成立前提

对候选集合 $C$ 中任意键 $k$，系统需要得到左右关系上的规划频率区间：

$$
L_X^{plan}(k)\le f_X^{plan}(k)\le U_X^{plan}(k).
$$

残余分片扫描通常给出精确的 $f_X^{residual}(k)$。稳定分片贡献若来自精确表，则区间收敛为单点；若来自 sketch 或摘要，则形成上下界：

$$
L_X^{plan}(k)=L_X^{stable}(k)+f_X^{residual}(k),
$$

$$
U_X^{plan}(k)=U_X^{stable}(k)+f_X^{residual}(k).
$$

只有当稳定贡献可精确获得时，才能写作 $f_X^{plan}(k)=f_X^{stable}(k)+f_X^{residual}(k)$。

## 3. 边界回查规则

设某侧热点阈值为 $\theta_X$。若：

$$
L_X^{plan}(k)\ge \theta_X,
$$

即使考虑误差，该键也一定进入 $H_X$。若：

$$
U_X^{plan}(k)<\theta_X,
$$

即使取上界也达不到热点阈值，该键可排除出 $H_X$。若：

$$
L_X^{plan}(k)<\theta_X\le U_X^{plan}(k),
$$

该键进入边界精确回查集合：

$$
A_X=\{k\mid L_X^{plan}(k)<\theta_X\le U_X^{plan}(k)\}.
$$

若使用全局 top-$h$，则可先计算候选中第 $h$ 大频率的下界 $T_X$，只有满足 $U_X^{plan}(k)\ge T_X$ 的键才需要继续确认。

## 4. 精确回查实现

精确回查不应逐键扫描。系统应把所有待确认键合并为集合 $A$，一次性下发到相关节点。若稳定分片存在键索引、列存字典或局部计数表，节点直接查询这些键的稳定贡献；若不存在索引，则批量扫描相关稳定分片的连接键列，并只为 $A$ 中的键维护计数。

如果底层是列式文件，只读连接键列通常低于完整连接输入扫描，但仍然可能产生大量 I/O。因此，精确回查必须纳入成本判断。当边界键过多或需要读取大部分稳定分片时，直接 `REBUILD` 通常更合理。

## 5. 负载语义与计划合成

精确回查后，系统根据确认频率分别导出 $H_R$ 与 $H_S$，并形成两侧热点状态组合：

$$
z(k)\in\{HH,HC,CH,CC\}.
$$

状态组合只描述左右关系热点成员关系，不能因为 $f_R(k)f_S(k)$ 或 $W(k)$ 较高而反向修改 $H_R/H_S$。随后，系统估计内在连接工作量 $W(k)$，它只包含两侧频率、匹配规模、结果输出和输入本身相关局部处理量。策略相关代价单独写作 $Cost(k,a)$，用于评估候选策略 $a$ 下的数据移动、轻侧复制、键内拆分、资源占用和构建探测代价。

初始计划合成分四步。第一，构造规划视图：历史稳定部分、残余识别结果、高工作量边界候选和普通键默认规则。第二，生成候选执行策略：分类路由、轻侧复制、键内拆分和常规哈希。第三，依据 $z(k)$、$W(k)$、$Cost(k,a)$ 和资源上下文实例化路由、拆分数量、任务边界和节点归属。第四，输出完整初始负载计划，包括特殊规划键显式任务、普通键默认任务，以及历史部分和当前轮识别部分的合并结果。

## 6. 正确性边界

边界回查和频率区间只影响热点状态判断和初始负载计划质量。若某个热点被漏报，它仍可沿普通路径执行；这可能导致负载不均衡，但不应改变连接输出。连接结果正确性依赖最终计划满足记录覆盖、任务互斥、默认路径完备和唯一输出约束。

高工作量边界键可以进入特殊规划对象集合，但这只是执行策略选择，不改变 `HH/HC/CH/CC` 定义。这样可以避免把热点状态、工作量估计和执行路径混为同一概念。

## 7. 开销与退化路径

计划合成成本由候选集合规模、边界回查集合规模、候选策略数量和资源上下文复杂度决定。候选集合越大，越可能增加策略评估和参数实例化开销。边界回查集合越大，越可能抵消增量统计收益。

系统应在进入精确回查前估计：

$$
C_{partial}=C_{meta}+C_{scan}(\Delta)+C_{merge}+C_{query}(C)+C_{verify}(A),
$$

并与完整重建成本比较。若 $C_{partial}\ge C_{rebuild}$，或边界不确定性无法通过有限回查解决，则进入 `REBUILD`。

## 8. 是否建议并入正文

建议并入第 6.5、6.6、6.7 和 6.8。正文需要明确频率区间、边界精确回查和计划合成四步，但不必展开具体索引实现。实验前可先把它作为机制设计文档，用于确定残余比例、候选容量和回查比例三个核心变量。

