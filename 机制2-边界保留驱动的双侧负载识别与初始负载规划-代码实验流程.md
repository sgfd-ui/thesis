# 机制2：代码实验流程

## 1. 实验目标

验证边界保留驱动的双侧负载识别与初始负载规划是否提高残余变化场景下的热点召回率。实验关注热点语义可靠性，而不是连接结果正确性；所有配置仍需通过 output/work 校验。

## 2. 实验组成

### M2_residual_candidate_boundary

该实验验证残余范围双侧候选识别的收益。对比对象是只检查历史待确认键的 `kcheck-only` 版本；改进版本在残余输入范围内重新执行候选发现，并合并历史稳定键、待确认键、稳定摘要候选和残余候选。

结果：

- `kcheck-only recall@k`：0.669
- 改进后 `recall@k`：0.949
- 提升：+0.280

### M2_boundary_lookup_plan_synthesis

该实验验证边界回查与初始负载计划合成的收益。对比对象是不充分处理频率区间和边界键的 `kcheck-only` 版本；改进版本根据频率上下界执行边界回查，并把负载语义、内在工作量和策略代价合成为初始计划。

结果：

- `kcheck-only recall@k`：0.800
- 改进后 `recall@k`：0.949
- 提升：+0.150

## 3. 审计要求

实验必须检查：

- 公式检查无 mismatch。
- 没有 output/work mismatch。
- 没有 join result cache。
- 候选补全和边界回查只影响热点状态与计划质量，不改变连接谓词。

## 4. 论文归属

这两个实验共同支撑机制2的主张：残余范围候选补全和边界回查能够提高当前轮热点召回率。它们不构成两个新机制，而是“边界保留驱动的双侧负载识别与初始负载规划”的两个内部组成。

