# 机制3：快照驱动的剩余工作再平衡

## 1. 机制定位

快照驱动的剩余工作再平衡处理初始负载计划执行后仍可能出现的运行时长尾。即使第 5/6 章已经完成历史复用、当前轮负载识别和初始任务规划，实际输出规模、节点执行速度、资源扰动和任务代价估计误差仍可能使少数节点长期持有未完成工作。

本机制接收的是初始任务计划、可继续划分的任务边界和执行过程中维护的本地进度状态。它不接收热点键集合，也不依赖执行前预先生成的微任务集合。微批次只在运行时从尚未领取的剩余工作中按需划分。

接入本机制的初始任务需要满足四个条件：剩余范围可继续切分；切出的区间可由其他节点执行；执行所需数据或状态可访问；不同区间的输出不会重复。

## 2. 核心思想

机制3将再平衡拆分为两个动作：

1. 读取按需发布的集群范围剩余工作快照，获得候选供给节点及访问顺序。
2. 针对供给节点的实时 `QueueState_i` 发起 RDMA CAS，原子领取其尾部区间。

快照只提供候选选择，不决定任务所有权。最终能否领取任务由供给节点实时队列状态决定。旧快照最多导致探测过期候选或 CAS 失败，不能绕过实时状态。

## 3. 可让渡任务表示

每个节点维护本地任务进度区间：

$$
[Head_i,Tail_i)
$$

`Head_i` 表示本地已经领取或声明到的位置，`Tail_i` 表示仍可被远端领取的尾部边界。区间 `[Head_i,Tail_i)` 只表示尚未被本地领取、也尚未被远端接管的可让渡范围。已领取但未完成的本地任务、已接管但未完成的远端微批次由 `InFlight_i` 记录，不重新进入可让渡范围。

核心算法默认初始计划将可让渡工作组织为近似等代价逻辑任务，因此：

$$
W_i=Tail_i-Head_i
$$

表示节点 $i$ 的可让渡剩余工作量估计，而不是完整剩余执行代价。若使用加权任务，需要通过 `LocateBoundary(Head_i,Tail_i,C_transfer)` 先定位新尾部，再执行 CAS 更新，不能直接用 `Tail_i-C_transfer`。

## 4. 快照化负载视图

快照是集群范围可见的剩余工作视图，不是强一致全局最优状态。系统按需发布快照，而不是持续广播全局状态。

每版快照写入一个槽：

```text
SnapshotSlot_s = <ViewState, JFI, K_pool, CandidateCount, Candidates>
```

`ViewState` 包含三种状态：

- `ACTIVE`：存在可让渡候选，请求节点探测候选。
- `WAITING`：当前没有可让渡候选，但仍有小任务或 `InFlight_i`，请求节点退避等待或刷新。
- `TERMINATED`：全部 `QueueState_i` 为空且全部 `InFlight_i=0`，确认全局结束。

全局控制字只保存：

```text
AtomicControlWord = [Version | Lock | Leader_ID | Slot]
```

发布节点写完整 shadow slot，确认可见后，通过 CAS 原子切换 `Version` 和 `Slot`。`Version` 只服务于快照读取校验，不参与单次任务领取正确性判断。

`JFI` 表示当前快照中可让渡剩余工作的节点间离散程度。当 $\sum_i W_i=0$ 时，约定 `JFI=1`；是否终止由 `ViewState` 与 `InFlight[1:N]` 决定。

## 5. 请求节点与任务领取

存在空闲执行槽且本地没有可领取任务的节点称为请求节点。请求节点按照双读校验过程读取当前快照。若 `ViewState=ACTIVE`，它从 `Candidates` 中探测候选供给节点；若 `WAITING`，则退避等待或触发刷新；若 `TERMINATED`，则确认全局执行结束。

请求节点开始一次获取活动前，先增加本节点 `InFlight_i` 并等待该更新对远端可见。若 CAS 成功，`InFlight_i` 保持不变，直到接管任务执行和输出完成后递减。若 CAS 失败但仍在同一获取活动内重试或切换候选，可以保持该占位；只有最终放弃获取且没有获得任务时才递减。

CAS 成功后，被接管区间从供给节点 `QueueState_i` 中永久移除，不会重新进入可让渡范围。`InFlight_i` 只记录该任务尚未执行和输出完成，用于终止判断。

## 6. 按需快照发布

当系统尚无有效快照、候选耗尽或快照失效时，请求节点尝试进入 OSP 发布路径。核心无故障路径包含：

| 操作 | CAS 期望状态 | CAS 写入状态 |
| --- | --- | --- |
| `Entry CAS` | `[V,0,0,S]` | `[V,1,MyID,S]` |
| `Exit CAS` | `[V,1,MyID,S]` | `[V+1,0,0,S']` |

发布节点读取全部 `QueueState_i` 和全部 `InFlight_i`，构造 `ACTIVE/WAITING/TERMINATED` 视图。发布空视图前，需先读取全部 `QueueState_i`，再读取全部 `InFlight_i`，必要时重复确认一轮；只有队列与活动计数均为空时才发布 `TERMINATED`。

本章核心算法只讨论无故障发布路径。若要支持发布节点失效和发布权接管，需要额外 epoch 或 fencing，防止旧发布者继续写 shadow slot；该扩展不纳入核心正确性证明。

## 7. 正确性与开销边界

本章核心正确性分析假设执行节点在任务领取和执行期间不发生失效；节点故障导致的任务恢复、重新发布和输出去重属于容错扩展。

正确性来自两层。实时 `QueueState_i` CAS 保证本地领取与远端领取不重叠，并使成功接管的区间从供给节点可让渡范围中单调移除。快照发布协议保证请求节点读取到的是完整视图，且快照只指导候选选择，不决定任务所有权。

开销边界来自按需发布和有限探测。稳态下节点不持续上报；请求节点读取一次快照并探测有限候选；视图重构只在快照失效、候选耗尽或需要判断等待/终止状态时发生。主要开销来自读取全部 `QueueState_i`、读取全部 `InFlight_i` 和写入 shadow slot。

