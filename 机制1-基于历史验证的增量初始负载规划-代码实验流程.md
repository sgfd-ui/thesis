# 机制1：代码实验流程

## 1. 实验目标

验证基于历史验证的增量初始负载规划是否降低重复或相似查询中的检测、统计和计划构造成本。实验不缓存 join 输出，不复用上一轮物理任务计划，只复用控制面状态和统计摘要。

## 2. 实验组成

### M1_history_reuse_state

该实验验证历史统计状态与复用判定的收益。对比对象是完整重建路径，实验记录 `FULL_REUSE`、`PARTIAL_REUSE` 和 `REBUILD` 下的完成时间、检测时间和验证开销。

结果：

- baseline ours 平均完成时间：10.991 s
- `FULL_REUSE` 平均完成时间：3.467 s，约 3.17x 加速
- `PARTIAL_REUSE` 平均完成时间：5.664 s，约 1.94x 加速

### M1_versioned_incremental_stats

该实验验证版本化分片增量统计的收益。系统复用稳定分片统计，只扫描新增、替换或无法确认稳定的残余分片。

结果：

- baseline ours 平均完成时间：11.368 s
- `FULL_REUSE` 平均完成时间：3.391 s，约 3.35x 加速
- `PARTIAL_REUSE` 平均完成时间：6.406 s，约 1.78x 加速

## 3. 审计要求

实验必须检查：

- 没有 output/work mismatch。
- 没有引入 join result cache。
- `FULL_REUSE/PARTIAL_REUSE` 仍执行完整 join，只减少控制面检测和统计开销。
- 复用的是历史策略模板、分片统计摘要和验证结果，不是上一轮连接输出。

## 4. 论文归属

这两个实验共同支撑机制1的主张：在重复或相似查询中，历史状态复用和版本化分片增量统计可以降低检测/统计成本。它们不构成两个新机制，而是“基于历史验证的增量初始负载规划”的两个内部组成。

