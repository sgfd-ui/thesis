# 第三机制代码实现说明：热点感知预分布

## 1. 文档定位

本文档说明热点感知预分布机制在当前实验代码中的实现接入方式。论文机制名称使用“热点感知预分布机制”；代码中的 `Hot-key Reuse Plan Cache` 是实现该机制的缓存与验证模块。

本文档不是论文结果段，也不是正式实验结论。它用于记录方法入口、缓存状态、执行路径、验证逻辑、输出指标和验收标准。第三机制是否进入主结论，由重复或相似连接查询实验决定。

## 2. 方法入口与语义

当前代码入口按以下语义整理：

| method_id | 论文语义 | 用途 |
| --- | --- | --- |
| `ours` | 三阶段完整方法的默认入口 | 主方法；启用热点感知预分布、FastLock-Verify 和 QueueState/OSP |
| `ours_hot_reuse_plan` | 热点感知预分布主实现 | 显式测试预分布命中、验证和回退路径 |
| `ours_hot_reuse_plan_no_validation` | 不做验证直接复用历史计划 | 诊断项，只用于证明验证必要性 |
| `ours_no_hotspot_predistribution` | 去掉热点感知预分布 | 消融项，验证第三机制贡献 |
| `ours_no_global_hot_key_verification` | 去掉全局热点验证 | 消融项，验证 FastLock-Verify 贡献 |
| `ours_no_rebalance` | 去掉 RDMA 运行时再平衡 | 消融项，验证 QueueState/OSP 贡献 |
| `ours_no_bounded_probing` | 去掉受限候选探测 | 消融项，验证低控制面探测策略 |

如果当前代码中仍保留 `ours_hot_pre_migration` 或 `ours_hot_reuse_plan_plus_premigration`，它们只作为历史候选或扩展诊断，不进入默认主矩阵。第三机制的正式实现口径是计划复用与轻量验证，不是热点数据预迁移。

## 3. 核心状态

实现中需要维护的逻辑对象包括：

```text
HotKeyReusePlanCache
cache_status
validation_result
routing_plan_reused
quadrant_map_reused
placement_map_version
```

`HotKeyReusePlanCache` 至少应包含以下信息：

```text
query_signature
H_R_cached
H_S_cached
quadrant_map_cached
freq_R_cached
freq_S_cached
join_work_cached
routing_plan_cached
placement_hint_cached
total_count_R_cached
total_count_S_cached
cache_version
last_full_detection_round
confidence_score
placement_map_version
```

其中，`query_signature` 对应论文中的 $Sig^t$，用于绑定输入关系、连接键、过滤条件、时间窗口粒度和数据版本。`routing_plan_cached` 和 `placement_hint_cached` 不能表示上一轮具体任务区间的直接复用，只能表示热点键路由规则、分裂参数和初始放置偏好。

`cache_status` 取值固定为：

```text
FULL_HIT
PARTIAL_HIT
MISS
```

## 4. 执行路径

整体执行路径为：

```text
load or generate input
  -> load HotKeyReusePlanCache
  -> check query_signature
  -> lightweight validation
  -> FULL_HIT / PARTIAL_HIT / MISS dispatch
  -> build current-round task ranges
  -> join execution
  -> QueueState/OSP residual rebalancing
  -> metrics output
  -> refresh cache state
```

分支语义如下：

```text
FULL_HIT:
  reuse H_R / H_S
  reuse HH / HC / CH / CC semantics
  reuse routing rules and split parameters
  skip full FastLock-Verify

PARTIAL_HIT:
  split key space into verified stable hot keys and unconfirmed keys
  reuse stable hot-key routing
  run local supplementary detection or full detection for unconfirmed keys
  merge into a complete current-round routing plan

MISS:
  run complete FastLock-Verify
  rebuild hot-key sets, semantics, and routing plan
```

`QueueState/OSP` 始终保留。即使 `FULL_HIT` 成立，执行期仍可能出现输出规模偏移、节点进度差异或剩余尾部任务，因此需要 RDMA 运行时再平衡兜底。

## 5. Validation 逻辑

Validation 按保守原则执行：无法确认缓存有效时，返回 `MISS` 或 `PARTIAL_HIT`，不得强行进入 `FULL_HIT`。

必须检查以下信号：

```text
query_signature_match:
  当前查询是否与 cache 绑定的查询签名一致

environment_compatible:
  执行节点数、并行度、RDMA 注册内存规模和拓扑是否允许复用 placement hint

size_delta:
  当前输入规模相对 cache 输入规模的变化

side_hot_retention:
  H_R_cached 在 R 侧、H_S_cached 在 S 侧是否仍保持热点身份

weighted_frequency_drift:
  基于归一化频率和 join_work 权重的热点漂移

quadrant_consistency:
  HH/HC/CH/CC 连接语义是否保持一致

new_hot_key_risk:
  非历史键残余质量、未知键占比或新热点候选是否过高

state_freshness:
  cache 是否超过版本过期或置信度衰减阈值
```

建议判定语义：

```text
FULL_HIT:
  query_signature_match = true
  environment_compatible = true
  side_hot_retention high
  weighted_frequency_drift low
  quadrant_consistency high
  new_hot_key_risk low
  state_freshness valid

PARTIAL_HIT:
  query_signature_match = true
  stable hot keys exist
  but some boundary keys, placement hints, or unknown keys are uncertain

MISS:
  query signature mismatch
  or major input/environment drift
  or high new-hot-key risk
  or state expired
  or validation uncertainty too high
```

`ours_hot_reuse_plan_no_validation` 只用于诊断验证边界。该方法不得进入主结论。

## 6. 输出指标

每次运行至少输出：

```text
end_to_end_time
join_only_time
detection_time
validation_time
routing_time
cache_status
cache_hit_rate
cache_fallback_rate
validation_overhead_ratio
output_size
run_status
```

建议同时输出：

```text
query_signature_match
environment_compatible
routing_plan_reused
quadrant_map_reused
hot_retention_R
hot_retention_S
weighted_freq_drift
quadrant_consistency
new_hot_key_risk
sample_rate
validation_sample_size
load_cv
max_worker_time
stolen_task_count
rdma_operation_count
```

派生指标：

```text
speedup = baseline_time / candidate_time
detection_saved = full_detection_time - validation_time
routing_saved = full_routing_time - candidate_routing_time
validation_overhead_ratio = validation_time / full_detection_time
```

论文结果解释时必须同时报告 `validation_time` 和 `detection_time`。不能只用 join 执行时间证明第三机制有效。

## 7. 验收标准

第三机制进入论文主结论前，至少需要满足：

1. `output_size` 全部与 `ours` 一致。
2. `FULL_HIT/PARTIAL_HIT/MISS` 的分支语义与日志一致。
3. 重复或相似连接查询中，`ours_hot_reuse_plan` 相比去掉预分布的消融版本具有稳定收益。
4. `validation_time` 明显小于完整 `detection_time`。
5. 当 `hotset_drift` 或 `new_hot_key_risk` 升高时，`cache_fallback_rate` 应上升。
6. `ours_hot_reuse_plan_no_validation` 如果更快但存在风险，只能用于说明验证必要性，不得作为正式方法。

若重复查询实验无法证明稳定收益，第三机制应降级为扩展机制或附录结果，不进入主结论。
